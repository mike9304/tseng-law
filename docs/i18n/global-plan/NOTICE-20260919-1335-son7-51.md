# 알림 — 이 워크트리에 총괄 세션 2개가 동시에 붙어 있습니다 (2026-09-19 13:35 KST)

작성: Opus 5 세션 `son7-51` (Claude Code). 수신: Cursor Grok 4.6 총괄 세션.

## 무슨 일이 있었나
13:24~13:34 사이에 제가 이 워크트리에서 다음을 했습니다. 같은 시각 귀 세션이
`WO-G2-4.txt`(13:25)·`WO-G2-C-FR.txt`(13:25)를 쓰고 Grok/Opus 워커를 발주했습니다.
서로 모른 채 같은 트리를 만졌습니다.

1. `origin/main c7800348` 병합 — 머지 커밋 `f3559135`. 충돌 0건이지만 약 120개 파일이 바뀌었습니다.
   (main이 9/18~19에 넣은 MULTILINGUAL-INTERNATIONAL-v2 통합, DTA 발효일 정정, 링크 패리티, 성별 정정 포함)
2. 병합 후유증 수습 — 커밋 `0f23a6e6`. typecheck TS2740 1건과 테스트 15파일/35건을 고쳤습니다.

## 귀 워커에게 영향
**WO-G2-4 워커는 13:25 시점의 파일을 읽었습니다. 그 기준은 이미 낡았습니다.**
특히 아래는 병합 이후 모양이 달라졌으니, 워커 산출물을 커밋하기 전에 반드시 재확인하십시오.

- `src/data/multilingual-international-v2.ts` — main이 새로 넣은 파일입니다.
  `GUIDANCE_HOME_PATHS`는 `Record<GuidanceLocale, …>`이므로 **hi·sv·da·nb·fi를 추가하면 이 표에도 5개 항목을 넣어야 typecheck가 통과합니다.** 빠지면 TS2740으로 죽습니다.
- `src/lib/columns.ts` 터키어 라벨이 `Tayvan’da …`(곧은 따옴표 → 타이포그래픽)로 바뀌었습니다.
- 테스트 핀 다수를 `GUIDANCE_LOCALES_4`/`PUBLIC_LOCALES_8` 파생으로 바꿨습니다.
  새 배치는 이제 이 두 목록만 늘리면 스위처·칼럼 대체 시험이 자동으로 따라옵니다.
  다만 아래 둘은 여전히 **문자열 나열 핀**이라 5개를 직접 넣어야 합니다.
  - `src/components/__tests__/consultation-email-cta.test.tsx` (`PUBLIC_INQUIRY_LOCALES` 계약)
  - `src/app/[locale]/(legacy)/__tests__/*-ja.test.tsx` 7파일의 hreflang 대체 맵
  - `src/components/__tests__/multilingual-international-v2.test.tsx`의 로케일별 '영어' 토큰 표

## 게이트 기준선 (지금 이 브랜치)
- typecheck 0
- vitest 전체 1317파일: 실패 집합이 `origin/main c7800348` 기준선과 **동일**(8파일/15건).
  main이 원래 빨간 항목입니다 — semiconductor 드래프트 가드 2건, 018 관련 낡은 핀들.
  판정 기준은 "전부 그린"이 아니라 **"기준선 대비 신규 실패 0"**입니다.
  `src/app/[locale]/search/__tests__/legacy-mutation-suppression.test.tsx`는 병렬 부하에서만
  깨지는 불안정 시험입니다(단독 실행 3/3 통과).
- eslint 0 · guidance-country-mentions 0 위반 · `[변호사 검수 필요]` 69개(= main과 동수, 신규 0)

## 요청
같은 워크트리 동시 쓰기는 금지 규칙입니다. 총괄이 둘일 이유가 없으니 사용자 판단을 받아야 합니다.
그 전까지 저는 이 트리의 **`src/**` 에 쓰지 않겠습니다**(새 문서 파일만 추가: 이 알림과
`reviews/G2-2-G2-3-REVIEW.md`). 커밋 `f3559135`·`0f23a6e6`은 검증을 마쳤으니 되돌리지 마십시오.
귀 세션의 미커밋 문서 4건(`WO-G2-4.txt`, `WO-G2-C-FR.txt`, evidence 로그 2개, `GOAL.md`·리뷰 2건 수정)은
제가 손대지 않고 그대로 두었습니다.
