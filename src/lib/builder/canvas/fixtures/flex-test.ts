import type { Locale } from '@/lib/locales';
import type { BuilderCanvasDocument, BuilderCanvasNode } from '@/lib/builder/canvas/types';
import { createDefaultCanvasNodeStyle } from '@/lib/builder/canvas/types';

export function createFlexTestDocument(locale: Locale): BuilderCanvasDocument {
  const updatedAt = '2026-04-28T00:00:00.000Z';
  const nodes: BuilderCanvasNode[] = [
    {
      id: 'flex-test-container',
      kind: 'container',
      rect: { x: 240, y: 80, width: 800, height: 200 },
      style: createDefaultCanvasNodeStyle({ borderRadius: 24 }),
      zIndex: 0,
      rotation: 0,
      locked: false,
      visible: true,
      content: {
        label: 'Flex test container',
        background: '#f8fafc',
        borderColor: '#2563eb',
        borderStyle: 'dashed',
        borderWidth: 2,
        borderRadius: 24,
        padding: 24,
        layoutMode: 'flex',
        flexConfig: {
          direction: 'row',
          wrap: true,
          justifyContent: 'center',
          alignItems: 'center',
          gap: 24,
        },
      },
    },
    {
      id: 'flex-test-child-1',
      kind: 'text',
      parentId: 'flex-test-container',
      rect: { x: 720, y: 140, width: 200, height: 60 },
      style: createDefaultCanvasNodeStyle({ borderRadius: 14 }),
      zIndex: 1,
      rotation: 0,
      locked: false,
      visible: true,
      content: {
        text: 'Flex child 1',
        fontSize: 18,
        color: '#0f172a',
        fontWeight: 'bold',
        align: 'center',
        lineHeight: 1.25,
        letterSpacing: 0,
        fontFamily: 'system-ui',
        verticalAlign: 'center',
        backgroundColor: '#dbeafe',
        textTransform: 'none',
      },
    },
    {
      id: 'flex-test-child-2',
      kind: 'text',
      parentId: 'flex-test-container',
      rect: { x: 16, y: 900, width: 200, height: 60 },
      style: createDefaultCanvasNodeStyle({ borderRadius: 14 }),
      zIndex: 2,
      rotation: 0,
      locked: false,
      visible: true,
      content: {
        text: 'Flex child 2',
        fontSize: 18,
        color: '#0f172a',
        fontWeight: 'bold',
        align: 'center',
        lineHeight: 1.25,
        letterSpacing: 0,
        fontFamily: 'system-ui',
        verticalAlign: 'center',
        backgroundColor: '#dcfce7',
        textTransform: 'none',
      },
    },
    {
      id: 'flex-test-child-3',
      kind: 'text',
      parentId: 'flex-test-container',
      rect: { x: 1040, y: -120, width: 200, height: 60 },
      style: createDefaultCanvasNodeStyle({ borderRadius: 14 }),
      zIndex: 3,
      rotation: 0,
      locked: false,
      visible: true,
      content: {
        text: 'Flex child 3',
        fontSize: 18,
        color: '#0f172a',
        fontWeight: 'bold',
        align: 'center',
        lineHeight: 1.25,
        letterSpacing: 0,
        fontFamily: 'system-ui',
        verticalAlign: 'center',
        backgroundColor: '#fef3c7',
        textTransform: 'none',
      },
    },
  ];

  return {
    version: 1,
    locale,
    updatedAt,
    updatedBy: 'flex-test-fixture',
    stageWidth: 1280,
    stageHeight: 420,
    nodes,
  };
}
