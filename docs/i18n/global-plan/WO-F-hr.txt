WO-F-hr — Apply the accepted native-review fixes to the Croatian content (Grok 4.7 fix lane)

Workspace (yours alone): /Users/son7/Projects/tseng-law-fix-hr-20260922
Do NOT git add/commit/push. Write only inside: src/data/international-guidance-*.ts, src/data/international-inquiry-copy.ts, src/lib/llms-txt.ts (the Croatian/`hr` strings only — never another locale's), src/content/columns-hr/*.md, and your log docs/i18n/global-plan/evidence/grok-F-hr.log. If a test pins a Croatian string you changed (e.g. a FAQ question or a title in src/lib/__tests__/guidance-*hr*.test.ts), update that test's expected string to the new wording — nothing else in tests.
node: export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH

## Input
Read, in this order:
1. docs/i18n/global-plan/reviews/native/RULEBOOK-FIX.md — cross-locale rules R2–R10 (R1/R11 are already done). Apply every rule to all 18 columns and the Croatian guidance pack.
2. docs/i18n/global-plan/reviews/native/ACCEPTED-hr.md — the supervisor's decisions: which reviewer rows to apply, skip, or modify.
3. docs/i18n/global-plan/reviews/native/hr-a.md and hr-b.md — the native reviews (verdict + systemic patterns + finding rows with verbatim quotes and suggested rewrites).
Apply every P1 and P2 row unless ACCEPTED-hr.md says otherwise. The reviewer's suggested wording is a strong hint, not sacred: produce the most natural Croatian a native legal editor would write, keeping the meaning of the English source (src/content/columns-en/<same file name>) and the product facts below. "Systemic patterns" in the verdicts describe a pattern — sweep every file for every instance, not only the quoted one. P3 rows: apply when cheap.

Where the Croatian guidance strings live: src/data/international-guidance-*.ts (find the export for Croatian, e.g. `grep -n "GuidanceContent" src/data/international-guidance-*.ts`), src/data/international-guidance-team.ts / -offices.ts / -answers.ts and src/data/international-inquiry-copy.ts (the `hr` key), src/lib/llms-txt.ts (GUIDANCE_LLMS_NOTICES.hr). The dump docs/i18n/global-plan/reviews/native/input/hr-guidance.txt maps every path → current text; edit the .ts sources, not the dump.

## Hard rules (unchanged product facts)
- Consultation languages are English, Chinese (中文), Japanese, Korean only. Never imply Croatian consultation, interpreter, reply-time, free first meeting, 24/7, result guarantee, win rate, "best/only".
- Attorney Wei Tseng (曾雋崴) is a woman — feminine forms wherever the language marks gender.
- Keep Traditional Chinese glosses of Taiwanese legal terms in parentheses.
- Do not change frontmatter keys, `url`, `lastmod`, `featured_image`, image paths, links, numbers, dates or the count/order of body blocks. Do not change wording that was not flagged unless it is the same systemic pattern.
- `[변호사 검수 필요]` markers: 0. No Korean characters outside `url` values.
- Guidance pack: keep the TypeScript structure; only string values change. Strings must remain valid TS string literals (escape quotes as the file already does).

## Verify (run all, paste output into your log)
cd /Users/son7/Projects/tseng-law-fix-hr-20260922 && export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH
node scripts/check-column-translation.mjs --dir src/content/columns-hr --lang hr 2>&1 | tail -8
grep -rn "\[변호사 검수 필요\]" src/content/columns-hr src/data | wc -l
npx tsc --noEmit -p tsconfig.json && echo TSC_OK
npx vitest run src/lib/__tests__/ src/data/__tests__/ --pool=forks --poolOptions.forks.maxForks=1 2>&1 | grep -E "Tests |Test Files|×" | head -20
git status --short | wc -l

Pass criteria: checker `summary PASS 18/18`, markers 0, TSC_OK, tests passed. If the checker fails on a number/date, prefer the form the checker accepts and note it in the log.

## Report (docs/i18n/global-plan/evidence/grok-F-hr.log only)
- rows applied / rows skipped (with reason) / systemic patterns and how many instances changed
- §Verify output verbatim
- anything you believe the reviewer got wrong (do not apply those; list them)
