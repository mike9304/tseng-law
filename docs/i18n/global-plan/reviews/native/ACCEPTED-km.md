# ACCEPTED-km — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `km-a.md` and `km-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1 ឧបត្ថម្ភ → serve/support meaning as suggested; also the bio headings (P2 #5). a#2 llms notice → the four denials. a#3 009 fault sentence → apply. a#4 Hangul url → skip (R16).
- German template bones ("ផ្នែកភាសាខ្មែរនេះ", "បញ្ជីទំព័រ", "ក្នុងនីតិវិធីតែមួយ", "ឃ្លាំងវត្ថុ"), "ប្រើមិនបានភាសា" calque, Rechtsform calque → sweep pack.
- Orthography: standardize on ឱ្យ and ្ដ (coeng-ដ) throughout pack + columns (R-Orthography). Digits: Khmer digits in the pack are fine; columns keep Arabic digits with comma thousands — do not mix inside one file (R14).
- 條例 → statute word (ច្បាប់ / បទបញ្ញត្តិច្បាប់) not sub-statutory; 大法官 → Constitutional Court justices (R8); labor/health insurance as statutory schemes.
- Blog skeleton in 003 Q16–20/005/006/008/009 → R7 (keep blocks); "OOO" → R15.

## Part b
Apply every P1/P2 row of `km-b.md` under the same skip rules. Additional — R17-class rewrite: column 018 sections 3–5 (comparison table cells, the limited-company paragraph, shareholder/audit thresholds, the Investment Commission approval rule, the work-permit/ARC rule) were stripped to bare numbers and names — rewrite every one of those blocks in full from src/content/columns-en/018 keeping the block count, the numbers (2/3, 100%, TWD 30,000,000, TWD 100,000,000, Article 386) and the Chinese glosses; no cell may end mid-word. b#1 告訴乃論 → "បទល្មើសដែលត្រូវចោទប្រកាន់តែពេលមានពាក្យបណ្ដឹង" (R8). b#2 finished advertisement (not product). b#3 "proceeded to litigation" → natural Khmer. b#4 院 → "សភា/យ័ន" never យួន — sweep 016 (司法院 = តុលាការកំពូល/សភាតុលាការ per reviewer). b#5 差額 → ចំណែកខុសគ្នា/ភាពខុសគ្នា. b#6 English statute text (not បណ្ដិត). 對象 → "គោលដៅ" only for acquisition target; otherwise "ភាគី/អ្នកទទួល". ឧបត្ថម្ភ → part-a rule. Orthography ឱ្យ/្ដ sweep 010–018. Blog skeleton 013/015 → R7 with final ។.
