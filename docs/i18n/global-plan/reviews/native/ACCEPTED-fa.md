# ACCEPTED-fa — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `fa-a.md` and `fa-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1–a#4 apply (erhebt → "چه اطلاعاتی را جمع‌آوری می‌کند", Anzeige → آگهی, نقشه‌ای with ZWNJ, Datei → فایل). a#5 004 Korea sentence → R2 (home-country outbound rules; Korea only as a labelled example). a#6 008 closing → R3 wording (severance exists only where the law provides it; no entitlement promise). a#7 Hangul url → skip.
- ZWNJ (nim-fasele) sweep: می‌/نمی‌ prefixes, ها plurals and compounds in every string of the pack (meta descriptions, footer, FAQ, llms notice) and all columns — this is the biggest systemic item. Persian comma ، and «» quotes consistently; Persian letterforms ی/ک (no Arabic ي/ك).
- حکم کردن for "assess" → ارزیابی کردن sweep. Minguo years → R13 ("سال ۱۱۲ تقویم جمهوری چین (民國112年)"; no added Gregorian number). English role lines → R12.
- Blog voice in 003 Q16–20/005/006/008/009 → R7 (feminine narrator: Persian is unmarked, keep "وکیل" + name). Numbers: comma thousands (R14); Persian digits allowed if consistent per file; "TWD 1.57M" pinned.
