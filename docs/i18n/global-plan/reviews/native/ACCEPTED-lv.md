# ACCEPTED-lv — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `lv-a.md` and `lv-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- All 22 P1 rows apply as suggested (agreement katrs/pats, debitive objects, apgabaltiesa → rajona tiesa sweep incl. Tainan/Taoyuan, 008 table cell and subject reversal, "Pilnvarojums" → "Kvalifikācija").
- a#5, a#27 English roles → R12. a#22 OOO → R15 ("[vārds]"). a#23 "1.57M" → skip (pinned); other amounts keep dot thousands (R14).
- City names: Latvian forms (Taipeja, Gaosjuna, Taidžuna …) with the Chinese gloss, consistently in pack and columns.
- Systemic calques (Objektspeicher, Anzeigesprache → "Lapas valoda", "what these pages can do") → sweep. Blog voice R7 with feminine narrator.

## Part b
Apply every P1/P2 row of `lv-b.md` under the same skip rules. Additional: b#1 apgabaltiesa → rajona tiesa (part-a rule) sweep 010. b#4 statūti → "likums (外國人投資條例)" sweep 013/017. b#5, b#13, b#14, b#17 FAQ-answer openings under topic headings → R7b (make the heading the question or the first sentence self-standing; keep heading count). b#8 messenger records (ziņojumapmaiņas ieraksti). b#9 "Q." → Latvian form. b#11 negation scope. b#12 beneficial owner → "patiesais labuma guvējs" sweep 016. b#15 logs → "iesniegšanas kārtība / iestādes" sweep 017. b#19 distributor (izplatītājs). mēraukla declension; calques (esība, pozīcijas, uzbūve) → natural Latvian. Dates: "13. septembrī 2025" locative stamp → "2025. gada 13. septembris" only if the checker still parses (Latvian year-first pattern is registered) — otherwise keep. Numbers keep dot thousands (R14).
