# WIX-PERFECT — session completion record (2026-05-29 → 06-03)

Authoritative log of MY changes this session (the shared WIX-PERFECT-PROGRESS.md is now co-managed by the project's auto changelog). Plan: WIX-PERFECT-PLAN-2026-05-29.md. Audit+backlog: qa-reports/PHASE1-AUDIT-GRADES.md (13-agent synthesis; full JSON /tmp/synthesis.json).

## 2026-06-03 — #5 follow-ups (user-directed)
- **All 30 templates visual-covered:** the template visual-regression test now derives `TEMPLATE_IDS` from the registry (`getAllTemplates().filter(-home)`) so every industry home (30) is auto-covered, future ones included. Generated 60 baselines (30 × desktop+mobile): generation run 30/30 passed. Stability re-pass (no --update) at the 3% threshold: **30/30 passed, exit 0, zero diffs — flake-free** (/tmp/vr30-stable.log).
- **CI gate added** (`.github/workflows/builder-visual.yml`): dedicated job running INSIDE the pinned Playwright Docker image `mcr.microsoft.com/playwright:v1.59.1-noble` so the CI Linux render matches the baseline render. Why Docker: `playwright.config.ts` `snapshotPathTemplate` omits `{platform}`, so baselines are single-render and darwin-authored baselines would false-fail on ubuntu — this is exactly why the existing visual suite was never CI-gated. Job: build → boot app on :3000 → generate template baselines in-container → re-run to verify same-render stability → upload diffs artifact on failure. Triggers on template/published/image/spec/baseline path changes. Companion `scripts/visual-baselines-docker.sh` regenerates committed baselines in the same image locally (Docker required).
- **HONEST CAVEAT:** Docker is NOT installed on this machine, so I could not (a) regenerate Linux baselines locally nor (b) execute the workflow to confirm it runs green on real GitHub Actions. The workflow + script are syntax/structure-validated (bash -n, js-yaml parse OK) and the Playwright invocation mirrors the proven local one, but the FIRST real CI run needs a push to confirm end-to-end. Until Linux baselines are committed, the job self-generates them in-container each run (drift caught within-run, not across PRs) — drop `--update-snapshots` from step 1 once Linux baselines are committed. Local darwin baselines (60 PNGs under tests/visual/baseline/) remain valid for `npm run test:builder-visual` on macOS.

## DONE & VERIFIED (7 backlog items) — all gates green at each step

| # | Item | What | Verify |
|---|------|------|--------|
| #1 | Broken template images (P0) | `scripts/generate-template-images.mjs` generates 82 industry-themed mesh-gradient assets (sharp) for all `/images/placeholder-*` paths that 404'd. Reads paths live from templates; reproducible. | 82/82 resolve; sharp metadata valid |
| #7 | Track-C lorem templates (P0, design) — STRUCTURALLY DONE, real-app render UNCONFIRMED | 13 byte-identical 714-line/0-image lorem homes → rebuilt via new `templates/_shared/industry-home.ts` builder (hero+stats+3 image cards+feature split+3-step process+testimonial+CTA, ~54 nodes, 11 sections, palette+image driven). Configs from a 12-agent max-thinking workflow; applied via `scripts/apply-industry-homes.mjs`. agency hand-written. | registry 529/529, tsc 0, 12 distinct palettes. **CAVEAT: visuals verified ONLY via my own `render-template-preview.mjs` (a reimplemented renderer that had 2 bugs I fixed mid-flight) — NOT through the real app pipeline. Templates load via in-editor TemplateGalleryModal (auth+client), no simple server render route; real-app/gallery render needs Playwright editor automation, deferred. Confidence: structure+data correct & tested; pixel-exact "Wix-grade" in the actual editor unconfirmed.** |
| #3 | Leaked dev/English placeholder copy (P1) | `scripts/fix-proof-placeholder-copy.mjs` exact-string-replaced 6 leaked strings ("Wix-grade proof system", "Visual proof area", etc.) → Korean across 248 files. className motion-hints untouched. CORRECTED synthesis: `+1960` stageHeight is REAL section height (measured), not empty padding — did NOT strip. | 0 leaked remain, 250 section-label classNames intact, registry 529/529, tsc 0 |
| #4 | service-areas dynamic 0-records (P0 false-green) | `dynamic-item-pages.ts` slugField `'href'`→`'slug'` (records expose bare slug as recordId). | tsc 0 |
| #9 | F61 milestone copy on live checkout (P2 false-green) | `PublicCheckout.tsx` ko/zh/en confirmationHelp → real copy. | grep F61=0 |
| #16 | Hardcoded signatureVerified (P1 false-green) | `payment-webhooks-engine.ts:296` `true`→`input.signatureVerified !== false` (field+normalizer already existed; only engine ignored). | tsc 0, commerce vitest 0, 0 hardcoded remain |
| #6 | Data-driven CMS binding — SLICE 1+2+3 DONE; full create→bind→render loop works (P0, biggest functional gap) | **Slice 1:** additive `cms-collection-datasets.ts` — generic resolver + bindable-target enumerator over `site.cmsCollections`, target scheme `cms.<id>.list`, same filter→sort→limit contract + row shape as built-ins. **Slice 2 (강행 per user 06-02):** added optional `cmsCollectionId?: string` to `BuilderPageDatasetBinding` (ADDITIVE — did NOT widen the closed `targetId`/`collectionId` unions, so 0 cascade across the 22 switch sites). `readBuilderDatasetSampleRecordsInternal` now branches FIRST on `binding.cmsCollectionId` → routes to the Slice-1 resolver → user collection's published records render through `readBuilderDatasetRepeaterItems` (the real seam: preview route + scene.ts). Preserved the field through `cloneBuilderPageDatasetBinding` + `normalizeBuilderDatasetBinding` (with `sanitizeCmsBindingFilters/Sort` since user-collection field keys ≠ built-in filterFields). **Slice 3 (강행 per user 06-02):** the create→bind UI + API. NEW pure helper `cms-binding-request.ts` (`buildDatasetBindingPatch`) — node-tested oracle (6 tests) that validates built-in vs CMS bindings and produces the persist patch; built-in path unchanged (strict collection/mode checks), CMS path carries `cmsCollectionId`. PUT route `datasets/route.ts` now funnels through it + verifies the user collection exists via `readSiteDocument`+`findCmsCollection`. Editor `BuilderDatasetBindingEditor.tsx` gets an additive "CMS collection (your data)" `<select>` (separate local state, built-in draft machinery untouched) that sends `cmsCollectionId` in the save+preview bodies; parent `datasets/page.tsx` threads `cmsBindableTargets` from `listCmsCollectionBindableTargets(readSiteDocument(...))`. Added `cmsCollectionId` to `BuilderPageDatasetBindingPatch`. | **tsc 0 (whole tree), 553 tests pass** (4 #6 test files: binding-request 6, cms-collection-datasets 11, datasets, registry 529). Scope additive: types.ts/datasets.ts + new helper+test + route/editor/parent. **INTERACTIVE SELECTOR NOW VERIFIED IN REAL BROWSER (06-02):** `scripts/smoke-cms-selector.mjs` (Playwright) seeded a `smoke-recipes` collection (2 published records) via the real API, loaded `/ko/builder/home/datasets`, and asserted 9/9: selector renders → seeded collection appears → selecting sets value → **preview POST re-runs with `cmsCollectionId` and returns the 2 records (was 4 built-in)** → Save binding fires → **PUT body carries `cmsCollectionId:"smoke-recipes"`** → **PUT persists (200, ok:true)** → live preview shows 김치찌개/불고기. Screenshot /tmp/smoke-selector.png confirms visually. Test data cleaned up (collection deleted, no stale binding). So the full create→bind(via in-app selector)→preview→persist→render loop is proven end-to-end in a browser, not just node tests. (Smoke-test script itself had 2 listener/timing bugs found+fixed; an isolated DOM probe confirmed the feature worked before the script did.) Remaining optional: user-collection dynamic-item detail page; per-field filter/sort dropdowns sourced from the collection's own fields. |

| #5 | Template visual-regression (P1, design QA) | REFRAME (real-Wix goldens impossible/meaningless — Wix's law page ≠ ours): self-baseline regression instead. EXTENDED the existing gated harness `tests/builder-editor/visual-regression.playwright.ts` (not a new standalone — advisor: don't baseline a renderer I wrote). New `ensurePublishedTemplateVisualPage()` creates+publishes a page from a template's REAL `document` (page-create accepts `document`→normalizeCanvasDocument→published render) and screenshots the real published route. 4 representative templates (law/saas/restaurant/portfolio) × desktop+mobile = 8 baselines under tests/visual/baseline/. Added `stabilizeForScreenshot()` (scroll to fire reveal/lazy-load, force reveal end-state, wait all imgs decoded) + `animations:'disabled'` — cut a 16% flake to ~1%; then per-test `maxDiffPixelRatio: 0.03` to absorb the residual ~1% live-chrome/image-decode jitter (far below the ~18% a real change produces). | **PROVEN: 3 consecutive clean runs (4/4 each, exit 0, zero diff output) = stable; regression-detection — deliberately changed saas hero #0b1020→#7a1020, baseline went RED (18% diff, exit 1); reverted → green.** Oracle = real published render (trustworthy), gated by `npm run test:builder-visual`. CAVEAT: baselines include site header/footer chrome + scroll-reveal whitespace (faithful to what visitors see); 3% per-test tolerance is the deliberate noise/signal tradeoff for a live-page self-baseline. Follow-up (task #5): CI-gate test:builder-visual (audit's other half); expand beyond 4 templates. |

| #2 | Computed qaScore (P2) | metadata.ts: `computeTemplateQaScore()` derives 0–100 from real document signals (node-count in 40–70 band, image count, kind variety, section richness) + `tierForScore()`. `getTemplateMetadata`: curated per-template qaScore/qualityTier win; else COMPUTED (not the category blanket 'standard'). Rewards 13 image-rich rebuilds, preserves 5 hero literals. | qa-score.test.ts 4/4 (rich>thin, law-home keeps 88, Track-C ≥70, all 0–100), registry 529/529, my tsc errors=0 |

Milestone gate after #1/#3/#4/#7/#9: full `vitest run`=0, `tsc`=0, image-gen=0 (at the time, 219 suites/1842 tests).
NOTE (2026-06-02 autonomous tick): a concurrent edit to `SiteSettingsGeneralTab.tsx` (others' WIP, 7+/22−, never touched by me) introduced 2 pre-existing tsc errors now in the tree — independent of my work. My changed surface stays tsc-clean. NOTE: tree later grew to 292 suites/2070 tests via CONCURRENT other work; final run shows 6 failures in `bookings/calendar-sync` (sync-engine.ts carries 84 lines of others' uncommitted WIP) + 1 localization test (untracked, others' work) — **none import my modules; my changed surface is 541 tests green (cms-collection-datasets + registry + payment-webhooks). I did not regress anything; the tree was not clean when I started.**

## NEW TOOLING (durable, reusable)
- `scripts/generate-template-images.mjs` · `scripts/apply-industry-homes.mjs` · `scripts/fix-proof-placeholder-copy.mjs`
- `scripts/visual-capture.mjs` (multi-page/viewport screenshots) · `scripts/render-template-preview.mjs` + `scripts/dump-template-doc.mjs` (standalone template→PNG)
- `src/lib/builder/templates/_shared/industry-home.ts` (parameterized Wix-grade home builder)
- `src/lib/builder/cms-collection-datasets.ts` (+ test)

## REMAINING (not commits per user; backlog in PHASE1-AUDIT-GRADES.md)
- #6 Slice 2: wire `listCmsCollectionBindableTargets` into editor binding UI (`resolveBuilderPageBindableTargets`) + repeater render so a bound user collection expands on canvas/public. Slice 3: user-collection dynamic-item page. BOTH touch datasets.ts/UI → snapshot first, stable I/O only.
- #5 visual-regression vs real-Wix goldens (reuse render/capture scripts). #2 computed qaScore (only 5 literals). Deeper: #8 storefront Stripe, #11 booking calendar grid, #12 CMS spreadsheet grid, #13 booking concurrency, #18 per-industry tokens all 30, #20 indexed persistence.

## HARD-WON LESSONS (for next session)
1. **Vite cache:** ALWAYS `rm -rf node_modules/.vite node_modules/.cache/.vite-node` before re-dumping/re-testing a just-edited template — vite-node serves stale modules → false validation (first agency render showed old lorem despite correct disk).
2. **git is NOT a safety net here:** types.ts/datasets.ts/cms.ts (+ ~302 files) are pre-existing uncommitted Wave work. `git checkout` would DESTROY it. Snapshot to /tmp before editing core files. Snapshots this session: /tmp/wix-snapshot-20260602/, dangling SHA 0872a31.
3. **Tool-I/O degrades within long turns** (doubled echoes → empty Bash/Read → blank image reads). Mitigation that works: ≤6 calls/turn, route results through files + `echo EXIT=$? > f`, background long jobs, ScheduleWakeup to reset. Disk WRITES stay reliable (verify by read-back). A spurious `TSC_EXIT=2` with 0 `error TS` lines = the flakiness, not a real failure; re-run clean to confirm.
4. **Sub-agents can't Write to project paths** — workflow agents return structured data, MAIN writes files (confirmed; see memory feedback_subagent_write_denied).
5. **Verify synthesis claims by measuring** — auditor over-claimed qaScore-on-all (only 5) and +1960-empty-padding (it's real content). Render/measure before mass-editing.
