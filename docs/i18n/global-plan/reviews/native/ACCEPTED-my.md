# ACCEPTED-my — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `my-a.md` and `my-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1 "none of the four languages" → rewrite as the reviewer says (the reader who has none of EN/中文/JA/KO), also FAQ question, form notice and form option. a#2 llms notice → four denials. a#3 bio: judgment obtained in the student's favour (keep "TWD 1.57M" byte-identical). a#4 資遣費 → the reviewer's employer-termination severance term, sweep pack + 002/008/009 incl. titles. a#5 contact title → "how to send a message to the firm" (also links in 001/002/005). a#6 securities market. a#7 林 → transliterate as a surname (လင် is "husband") — use the reviewer's form + gloss, sweep 006. a#8 民法/民事 → တရားမ (as 003 does), sweep 007. a#9 fault sentence. a#10 Hangul url → skip.
- 發行 → issuance term (not "publish") sweep 004/005. Dates: use month names ("2023 ခုနှစ် ဒီဇင်ဘာ 27 ရက်") — the checker parses Burmese month names; keep the same digits.
- Blog skeleton 003 Q16–20/005/006/008/009 → R7 with feminine narrator (ကျွန်မ is right). Numbers: comma thousands (R14); Myanmar digits allowed if consistent per file.

## Part b
Apply every P1/P2 row of `my-b.md` under the same skip rules. Additional: b#1 beginner (not founder) incl. alt text. b#2 docket "字第7號" → "အမှုအမှတ် 7" style (keep the Chinese docket string). b#3 fall on premises (not workplace injury). b#9 contact title → same fix as part a, sweep 013–017 links. b#10 資遣費 → part-a term. b#12 民法 → တရားမဥပဒေ sweep 016. b#13 證券 → securities term (ငွေချေးသက်သေခံလက်မှတ် or the reviewer's), sweep 016/017/018 — never the guarantee word. 對象 → "ဦးတည်" not ပစ်မှတ်. Calendar dates: month names (R14 note in part a) sweep 010–018. b#14 closing conditions of the acquisition. Blog voice 013/015 → R7.
