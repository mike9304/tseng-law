# ACCEPTED-hr — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `hr-a.md` and `hr-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1, a#2 Hangul url → skip (R16). a#3, a#4 apply.
- „Odgojna“ → „informativne/obrazovne naravi“ sweep (001, 002, 004, 007). 戶籍 → one term („evidencija kućanstava (戶籍)“ / „ured za evidenciju kućanstava (戶政機關)“), never matica — sweep pack + 007. 條例 → zakon, 辦法 → pravilnik (statut only for 章程) — sweep 002/004/008 and the 011/013/018 titles in part b.
- „Prestati s ugovorom“ → „otkazati ugovor o radu / dati otkaz“; blog line-breaks in 006/008/009 → R7 (keep blocks, feminine narrator). Slovenian „uporabljiv“ in the inquiry form → Croatian.
- Numbers: keep dot thousands, decimal comma (R14); dates „13. rujna 2025.“ are right; "1.57M" pinned.

## Part b
Apply every P1/P2 row of `hr-b.md` under the same skip rules. Additional: b#2, systemic „Statut“ for 條例 → „Zakon o ulaganjima stranih osoba (外國人投資條例)“ sweep 013/017 (statut only for 章程 in 018). b#3 遺贈 → „legat/oporučno ostavljanje (遺贈)“. b#4 018 disclaimer → the English meaning (a template does not make the deal lawful and does not prevent disputes; no outcome promise). „Odgojna“ sweep 011/014/016/017 (part-a rule). „Jedinstveno“ → „općenito/paušalno“. „Prestati s ugovorom“ → „otkazati ugovor“. FAQ answers opening a section without the question → R7b.
