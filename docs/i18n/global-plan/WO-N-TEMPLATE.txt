WO-N-{LOC}-{PART} — Translate columns {RANGE} into {LANG} for tseng-law.com (new locale `{LOC}`, readers in {COUNTRY})

Workspace (yours alone): {WT}
Do NOT git add/commit/push/stash/checkout. Do not open .env or customer files. Helper scripts only inside {WT}/.tmp-{LOC}/.
node: export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH

## Output
`src/content/columns-{LOC}/` — {COUNT} files, same file names as the Korean source `src/content/columns/` ({FILES}).
Write ONLY these files (and your log). The guidance pack and every other locale are off limits.

## Sources and references
- Structure/parity source (the checker compares against it): the Korean file `src/content/columns/<same name>` — frontmatter keys and order, `url`/`lastmod`/`featured_image` byte-identical, `faq` count, body block count and order, images (translate alt text only), numbers (same set of numerals; thousands separators per {LANG} convention: {THOUSANDS}; dates as {DATEFORM}).
- Meaning reference: `src/content/columns-en/<same name>` (attorney-reviewed English).
- Register/adaptation reference (already native-reviewed and fixed — imitate its choices, not its words): `src/content/columns-{REF}/<same name>`. It shows how Korea-centric passages were generalized (R2), how advertising-risk sentences were softened (R3), how blog leftovers were removed (R7), how orphan FAQ answers were made self-standing (R7b) and how Taiwan legal terms are glossed (R8). Apply the same decisions in {LANG}.
- Links: internal links map to `/{LOC}/...` exactly as in the {REF} file (`/ko/columns|contact|services|faq|pricing` → `/{LOC}/...`; the Korean landing/detail links → the {LOC} core pages with that page's title from the {LOC} guidance pack as the label; Korean-blog body links → `/{LOC}/columns/<slug>`; the Invest Taiwan link with `lang=eng`). External links otherwise byte-identical to the source.

## Quality standard — read docs/i18n/global-plan/reviews/native/RULEBOOK-FIX.md first (R2–R10 apply from the start)
- Write as a {LANG} legal editor for readers in {COUNTRY}: natural syntax, no calques from English/Korean/{REF}, the locale's quotation marks and typography, formal address of {LANG}.
- Product facts: consultation only in English, Chinese (中文), Japanese, Korean; never imply {LANG} consultation, interpreter, reply time, free consultation, 24/7, results, win rates, "best/only". Attorney Wei Tseng (曾雋崴) is a woman — feminine forms in every first-person passage and byline (`**<Attorney title, feminine> Wei Tseng (曾雋崴)**`). Keep the Latin name; never transliterate it.
- Taiwan legal terms: {LANG} term + Traditional-Chinese gloss in parentheses on first use (工作許可, 資遣費, 外僑居留證, 勞動基準法, 拘役, 緩刑, 監察人, 股東 …). Do not name the reader's country or state its law (the checker's nationality gate fails on {NATIONALITY}); generalize to "a foreign investor / the investor's home country"; Korea stays only as a labelled example.
- `date_display` in {LANG} ({DATEFORM}); `read_time` in {LANG} ("N min" form of {LANG}); `categories` value must be exactly one of the three labels in `src/lib/columns.ts` for `{LOC}` (formation/legal/case — pick the one the English file's category maps to).
- No Korean characters outside the `url` value; no untranslated {REF}/English sentences; no `[변호사 검수 필요]` markers; no comment CTAs or emoji.

## Verify (run after EVERY file, then all together; paste the final output into your log)
cd {WT} && export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH
ls src/content/columns-{LOC}/*.md | wc -l
node scripts/check-column-translation.mjs --dir src/content/columns-{LOC} --lang {LOC} 2>&1 | tail -20
grep -rn "\[변호사 검수 필요\]" src/content/columns-{LOC} | wc -l
git status --short | wc -l

Pass criteria: `summary PASS {COUNT}/{COUNT} files` for your files (the other part's files may be absent — that is fine), markers 0. Do not report while any file FAILs; fix and rerun. Finish file 001 (or 010) first and run the checker before continuing.

## Report
docs/i18n/global-plan/evidence/grok-N-{LOC}-{PART}.log only: file count, §Verify output verbatim, 5–10 key term choices with the Chinese gloss, unresolved points.
