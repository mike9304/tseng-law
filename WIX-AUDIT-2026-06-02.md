# Wix Builder 감사 리포트

## Executive Summary (요약)

이번 감사에서 가장 심각한 문제는 `memberEmailAliases`라는 **사용자 자기수정 가능 커스텀 필드**가 인증 경계로 오용되는 계열의 결함입니다. `PATCH /api/members/me`가 키 허용목록 없이 임의의 customFields를 병합하므로, 인증된 회원이 피해자의 이메일을 별칭으로 주입한 뒤 예약 취소·환불·일정변경(Critical IDOR)과 청구서/예약 PII 탈취·결제링크 재발급(High 다수)을 수행할 수 있습니다. 또한 `/api/booking/cancel`은 인증·소유권 검증이 전혀 없어 누구나 bookingId만으로 타인의 예약을 취소하고 Stripe 환불을 강제할 수 있습니다. CMS 콘텐츠 매니저의 Cut은 현재 페이지만 클립보드에 담으면서 전체 선택을 삭제해 페이지 경계를 넘는 **무음 데이터 손실**을 일으키며, 변호사 프로필 부분 패치는 동일 로케일의 다른 필드를 덮어써 **lost-update**가 발생합니다. 그 밖에 AI 생성기 발행 오류 무음 처리, 다수의 i18n 와이어업 누락, 로케일별 색상/문자열 휴리스틱 버그가 확인되었습니다.

핵심 근본원인 하나(self-writable 별칭)가 1건의 Critical과 다수의 High로 분기되므로, 이 보고서는 심각도 우선 정렬이지만 해당 계열은 단일 공유 수정안으로 함께 해결할 것을 권고합니다.

---

## Critical

### Members/Account — Bookings

**Cross-account booking takeover via attacker-controlled, unverified email aliases (IDOR)** (critical, security) — `src/app/[locale]/account/bookings/[bookingId]/cancel/route.ts` (동일 패턴 `.../reschedule/route.ts`)

```ts
const memberEmails = getMemberPortalEmails(member);
...
const booking = (await listBookings({ includeCancelled: true }))
  .find((item) => item.bookingId === params.bookingId && memberEmails.includes(item.customer.email.trim().toLowerCase()));
```

**Why:** 취소/일정변경의 인가가 오직 `memberEmails.includes(booking.customer.email)`로만 결정됩니다. `getMemberPortalEmails`(members-engine.ts:198)는 회원의 기본 이메일에 더해 `parseEmailAliases(member.customFields?.memberEmailAliases)`를 반환합니다. 별칭은 검증 없이 자기수정 가능합니다 — `POST/PATCH /api/members/me`가 `customFields: z.record(z.string(), z.string().max(500))`(키 허용목록 없음)를 받아 `updateMemberProfile`(members-engine.ts:270)이 `...patch.customFields`를 그대로 병합합니다. 따라서 인증된 회원이 `customFields.memberEmailAliases = '["victim@example.com"]'`를 PATCH한 뒤 bookingId로 피해자의 예약을 취소(이때 computeRefundForCancel/applyRefundOutcome로 **환불 강제**)하거나 일정변경할 수 있습니다. **검증자 판정: confirmed** — 전 경로(별칭 주입 → 세션 재조회 시 반영 → 소유권 필터 통과)가 end-to-end로 확인됨.

**Fix:** 자기수정 가능한 customFields를 인가 경계로 사용하지 말 것. (a) 소유권 체크에 사용하는 별칭 집합에서 `memberEmailAliases`를 제거하고 booking에 저장된 memberId(또는 검증된 기본 이메일)로만 매칭하거나, (b) 별칭을 인정하기 전 이메일 소유권 확인(확인 토큰)을 요구하고 `/api/members/me` customFields 스키마에서 `memberEmailAliases` 등 특권 키를 거부. 예약 조회는 서버가 신뢰하는 member↔booking 링크로 인가해야 함.

> 근본원인 노트: 아래 High의 #2/#4/#5/#6는 모두 동일한 self-writable `memberEmailAliases` 결함의 분기입니다. 단일 공유 수정(특권 customFields 키 거부 + 이메일이 아닌 서버 신뢰 memberId로 인가)으로 함께 해결하는 것을 권고합니다.

---

## High

### Members/Account — Bookings & Billing (memberEmailAliases IDOR 계열)

**Another member's booking PII exfiltration via unverified email aliases (CSV/ICS export IDOR)** (high, security) — `src/app/[locale]/account/bookings/export/route.ts` (동일 패턴 `.../[bookingId]/calendar/route.ts`)

```ts
const portal = await getCustomerBookingPortal(member.email, locale, undefined, getMemberPortalEmails(member));
```

**Why:** CSV export와 per-booking .ics 라우트가 `getMemberPortalEmails(member)`를 getCustomerBookingPortal에 전달해 `targetEmails.includes(normalizeEmail(booking.customer.email))`(customer-portal.ts:57)로 결과를 스코핑합니다. `memberEmailAliases`가 미검증 자기수정 필드이므로, 피해자 이메일을 별칭으로 추가한 공격자는 export/calendar 응답으로 피해자의 예약(서비스, 담당자, 예약시각, 결제상태, meetingLink)을 받습니다. calendar 라우트의 `.find((item) => item.bookingId === params.bookingId)`는 특정 피해자 예약을 id로 타게팅할 수 있게 합니다. **판정: confirmed.**

**Fix:** 읽기 엔드포인트를 서버 신뢰 신원(memberId 또는 검증된 기본 이메일만)으로 스코핑. 미검증 `memberEmailAliases`로 매칭 집합을 확장하지 말 것. 심층방어로, 매칭된 예약이 회원의 검증된 신원에 속하는지 반환/내보내기 전에 재확인.

---

**IDOR: billing-document access scoped only by self-assertable email aliases (memberEmailAliases)** (high, security) — `src/app/[locale]/account/billing/documents/[documentId]/payment-link/route.ts`

```ts
const memberEmails = getMemberPortalEmails(member);
  const row = (await listCustomerBillingDocuments(member.email, { locale }, memberEmails))
    .find((item) => item.documentId === params.documentId);
```

**Why:** 청구서 접근이 오직 문서의 recipientEmail이 `memberEmails`에 있는지로만 인가됩니다. 별칭은 미검증 자기수정 가능하므로, 공격자가 `customFields.memberEmailAliases = ["victim@example.com"]`를 설정한 뒤 이 라우트로 피해자 인보이스의 결제링크를 재발급/갱신(renew)할 수 있습니다 — 타 고객의 인보이스, 잔액, 작동하는 결제링크에 도달. **판정: confirmed.**

**Fix:** 인가를 자기수정 가능한 customFields에서 파생하지 말 것. 회원이 소유권을 증명한 후에만 별칭을 포함(서버 제어 `verifiedEmails` 목록). 최소한 `/api/members/me` PATCH의 customFields에서 `memberEmailAliases`(및 인가 영향 키)를 제거하고 청구서 조회를 검증된 이메일로만 스코핑.

---

**IDOR: booking/billing-document email action scoped only by self-assertable email aliases** (high, security) — `src/app/[locale]/account/bookings/[bookingId]/documents/[documentId]/email/route.ts`

```ts
const memberEmails = getMemberPortalEmails(member);
  const portal = await getCustomerBookingPortal(member.email, locale, undefined, memberEmails);
  const sourceBooking = (await listBookings({ includeCancelled: true }))
    .find((item) => item.bookingId === params.bookingId && memberEmails.includes(item.customer.email.trim().toLowerCase()));
```

**Why:** 예약 청구문서 이메일 발송 인가가 `memberEmails.includes(item.customer.email...)`로만 결정됩니다(`portal` 변수는 할당되나 가드로 쓰이지 않음). 별칭 주입으로 타 고객의 예약 bookingId에 도달해 `sendBookingBillingDocument` + `markBookingBillingDocumentEmailed`를 트리거 — PII/금액/문서상태 노출 및 피해자에게 이메일 발송. 응답에 피해자 booking/document 객체도 직접 반환됨. **판정: confirmed.**

**Fix:** 예약 접근을 회원이 소유권을 검증한 이메일로 스코핑. `/api/members/me`의 회원 수정 가능 customFields에서 `memberEmailAliases`를 제거해 인가를 self-grant 할 수 없게 할 것.

---

**Unverified self-service email change enables read-only PII disclosure of arbitrary guest bookings (IDOR via email-keyed portal)** (high, security) — `src/app/api/members/me/route.ts`

```ts
const profileSchema = z.object({
  email: z.string().trim().email().max(180).optional(),
```

**Why:** PATCH 핸들러가 세션 쿠키만으로 인증한 뒤 회원이 자신의 `email`을 임의 값으로 덮어쓸 수 있게 합니다(소유권 확인 메일/토큰 없음, 비밀번호 재인증 없음). `updateMemberProfile`은 이미 등록된 회원과 충돌할 때만 거부하며, 임의의 비회원 주소로 설정하는 것은 허용. 예약 포털이 이메일로만 키잉되고(customer-portal.ts:57) `/api/booking/book`로 누구나 임의 이메일의 게스트 예약을 만들 수 있으므로, 공격자는 계정 이메일을 피해자 게스트 주소로 바꿔 `GET /api/members/bookings`로 피해자 예약 PII를 읽을 수 있습니다. 이전 이메일이 `memberEmailAliases`로 누적되어 여러 피해자 이력을 점진 수집 가능. read-only(`managePath` 제거됨)라 critical이 아닌 high. **판정: confirmed** (단, 노출 필드는 name/phone이 아니라 serviceName/staffName/시각/상태/meetingLink/timezone로 정정).

**Fix:** 미특권 프로필 PATCH로 `email`을 변경 불가하게 하거나, 변경 시 (a) 비밀번호 재인증 + (b) 신규 이메일 확인 토큰 플로우(pending 필드에 저장 후 토큰 확인 시에만 commit, verified=false 리셋)를 강제. 포털이 미검증·가변 이메일/별칭을 소유권 증명으로 취급하지 않도록 하고 구 이메일 자동 누적을 중단.

---

### Bookings — Public API

**Missing authentication / IDOR: any anonymous caller can cancel any booking (and trigger a Stripe refund) by supplying its bookingId** (high, security) — `src/app/api/booking/cancel/route.ts` (~lines 38-68)

```ts
export async function POST(request: NextRequest) {
  const ip = clientIp(request);
  const rate = await checkRateLimit(`booking-cancel:${ip}`, 8, 60_000);
  ...
  const parsed = payloadSchema.safeParse(await request.json().catch(() => null));
  ...
  const booking = await getBooking(parsed.data.bookingId);
  if (!booking) {
    return NextResponse.json({ error: 'Booking not found' }, { status: 404 });
  }
  ...
  const policy = await evaluateBookingSelfServicePolicy(booking, service);
  if (!policy.canCancel) { ... }
```

**Why:** POST `/api/booking/cancel`가 body의 `bookingId`만으로 취소합니다. 관리자 가드(requireBuilderAdminAuth/guardMutation)도, 고객 소유권 체크도 없으며, 게이트는 IP 레이트리밋(8/min)과 시간창 정책(신원 무관)뿐입니다. 정당한 경로(`manage/[token]`)는 `verifyBookingManageToken` + 이메일 일치를 검사하지만 이 라우트는 둘 다 없음. bookingId는 약한 `Date.now()`+`Math.random()` 6자로 추측/열거 가능하고 확인 응답에 반환되므로 베어러 자격으로 부적합. 결과: 타인의 활성 예약 취소(캘린더 DoS)와 가맹점 대상 Stripe 환불 강제(computeRefundForCancel → attemptStripeRefund). **판정: confirmed** (환불은 원결제자에게 반환되므로 공격자 수취는 아님 — 피해는 가맹점 손실 + 표적 취소).

**Fix:** 취소 전 소유권 증명을 요구. 서명 토큰 메커니즘(`verifyBookingManageToken` + 이메일 일치, manage/[token]처럼)을 사용하거나 관리자 취소는 requireBuilderAdminAuth/guardMutation. 미인증 body의 bare bookingId를 수락 금지. 두 경로가 필요하면 검증 토큰 vs 인증 관리자 세션으로 분기하고 나머지는 401/403.

---

### CMS / Datasets

**Copy/Cut only serialize the visible page but Cut deletes the entire selection (silent data loss)** (high, data-integrity) — `src/components/builder/cms/ContentManagerClient.tsx`

```ts
const rows = visibleRecords
      .filter((record) => selectedRecordIdSet.has(record.recordId))
      .map((record) => [
```

**Why:** `buildSelectedRecordsClipboardText`가 현재 페이지(`visibleRecords`)만 순회하지만, 선택(`selectedRecordIds`)은 페이지를 넘어 확장됩니다 — `selectMatchingRecords()`가 `recordQueryResult.filteredRecordIds`(전체 필터 결과)를 선택으로 설정하고, 선택은 페이지네이션 간 유지됩니다. `cutSelectedRowsTsv()`는 보이는 부분만 클립보드에 쓴 뒤 `bulkDeleteSelectedRecords()`로 **전체 선택을 삭제**하므로, 페이지 밖 선택 레코드는 클립보드에 담기지 않은 채 영구 손실됩니다. copy도 `selectedRecordIds.length`를 보고하면서 더 적은 행을 복사해 수치/내용 불일치. **판정: confirmed** (copy의 count/content 불일치는 직접 확인, cut의 delete 대상은 강한 추론).

**Fix:** 클립보드 행을 전체 선택 기준으로 구성 — `detail.records.filter((r) => selectedRecordIdSet.has(r.recordId))`를 사용해 페이지네이션과 무관하게 모든 선택 id를 포함. 그러면 cut 삭제 집합과 복사 내용이 동기화됨.

---

### Builder — Lawyers / Source

**Partial localized attorney patch wipes previously-saved fields for that locale (lost update)** (high, data-integrity) — `src/lib/builder/lawyers/source.ts`

```ts
function mergeLocalizedOverride(
  current: BuilderAttorneyProfileSourceOverride['localized'],
  patch: BuilderAttorneyProfileSourceOverride['localized'],
): BuilderAttorneyProfileSourceOverride['localized'] {
  return normalizeLocalizedOverride({ ...(current ?? {}), ...(patch ?? {}) });
}
```

**Why:** 스프레드 `{ ...current, ...patch }`가 로케일 레벨에서 병합됩니다(필드 레벨 아님). 각 로케일 값은 중첩 객체 `{ name, role, title, description, summary }`인데, 한 필드만 패치하면(`{ localized: { ko: { name: '새 이름' } } }`) `patch.ko`가 `current.ko`를 통째로 덮어쓰고 `normalizeLocalizedOverride`가 병합값만으로 재구성하므로 기존 title/role/description/summary 오버라이드가 무음 손실됩니다. 형제 service-area 코드(`mergeOverrideText`)는 로케일 값이 단일 문자열이라 동일 패턴이 무손실 — 이 비대칭이 본 결함의 증거. `normalizeLocalizedOverride`의 `{}` 할당이 부분 패치가 타입상 유효함을 증명. **판정: confirmed.**

**Fix:** 정규화 전 각 로케일 객체를 deep-merge: current/patch 양쪽에 존재하는 모든 로케일에 대해 `{ ...(current?.[locale] ?? {}), ...(patch?.[locale] ?? {}) }`를 만들어 넘길 것. 미수정 로케일 필드가 보존됨.

---

### AI Generator

**Publish/schedule fetch rejections are swallowed: no user error and unhandled promise rejection** (high, robustness) — `src/components/builder/ai-generator/AiGeneratorWizard.tsx`

```ts
    try {
      const res = await fetch(`/api/builder/site/pages/${encodeURIComponent(pageEntry.pageId)}/publish`, {
        method: 'POST',
        credentials: 'same-origin',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      const payload = (await res.json().catch(() => ({}))) as PublishCreatedPageResponse;
      if (!res.ok || !payload.ok) {
        setPublishError(payload.message ?? payload.error ?? `${pageEntry.slug || pageEntry.pageId} 발행 실패`);
        return;
      }
      setPublishedPageIds((current) => Array.from(new Set([...cu
```

**Why:** publishCreatedPage / scheduleCreatedPagePublish / cancelCreatedPageSchedule가 catch 없는 try/finally만 사용합니다. 네트워크 자체가 reject되면(offline/DNS/abort/TLS) `await fetch`가 res.ok 검사 전에 throw하고, finally는 in-progress 플래그만 해제하므로 버튼이 idle로 돌아가지만 setPublishError가 호출되지 않아 사용자 피드백이 전무합니다. 게다가 `onClick={() => { void publishCreatedPage(...) }}`로 fire-and-forget이라 unhandled promise rejection이 됩니다. 형제 함수(refreshSitePages/refreshSiteNavigation/loadPublishPreflight)는 catch가 있어 불일치. **판정: confirmed.**

**Fix:** 세 핸들러 모두 finally 앞에 catch 추가: `} catch { setPublishError(`${pageEntry.slug || pageEntry.pageId} 발행 중 네트워크 오류가 발생했습니다.`); } finally { ... }` (schedule/cancel에 상응 메시지). 실패를 노출하고 unhandled rejection을 방지.

---

## Medium

### Bookings — Public API

**PII / financial-data exposure: full booking object and refund amount returned to an unauthenticated caller** (medium, security) — `src/app/api/booking/cancel/route.ts` (~lines 92-99)

```ts
return NextResponse.json({
    ok: true,
    booking: updated,
    refundDecision: outcome.decision,
    refundResult: outcome.refundResult,
    refundAmountCents: outcome.refundAmountCents,
    hoursUntilStart: outcome.hoursUntilStart,
  });
```

**Why:** 엔드포인트에 인증/소유권 체크가 없으므로(상위 High 참조) 성공 응답이 전체 `updated` 예약(customer.email/.phone/.name 등 PII)과 refundAmountCents/refundResult를 bookingId를 제공한 누구에게나 반환합니다. 이번 diff가 `refundAmountCents`를 추가해 노출되는 금융 데이터가 확대됨.

**Fix:** 상위 finding처럼 게이트한 뒤, 소유자(토큰 이메일 일치)/관리자로 검증된 경우에만 예약 상세 반환. 비소유자에게는 본문 없는 일반 404/401. 가능하면 전체 레코드 대신 최소 페이로드(`{ ok, status, refundDecision }`) 반환.

### CMS / Datasets

**Cut reports success even when the delete confirmation is cancelled** (medium, bug) — `src/components/builder/cms/ContentManagerClient.tsx`

```ts
      await bulkDeleteSelectedRecords();
+      setMessage(`Cut selected rows (${selectedCount}).`);
```

**Why:** `bulkDeleteSelectedRecords()`는 자체 `window.confirm`에서 취소 시 삭제·throw 없이 early-return합니다. 그러면 cutSelectedRowsTsv()가 무조건 `'Cut selected rows (N).'`을 표시 — 레코드는 남아있는데 UI는 잘라냈다고 주장하고 클립보드엔 복사본이 남는 혼란 상태.

**Fix:** `bulkDeleteSelectedRecords()`가 boolean을 반환(취소/early-return false, 성공 true)하게 하고 true일 때만 'Cut' 메시지를 표시. 또는 confirm을 cutSelectedRowsTsv에서 한 번만 수행하고 내부 confirm을 스킵하는 플래그 전달.

**Failed collection load via URL param can retry indefinitely** (medium, robustness) — `src/components/builder/cms/ContentManagerClient.tsx`

```ts
    if (!detail || detail.collectionId !== queryCollectionId) {
+      void loadDetail(queryCollectionId, { preserveEditingState: Boolean(queryRecordId) });
+      return;
+    }
+  }, [busy, detail, queryCollectionId, queryRecordId, selectedCollectionId, sourceCollections]);
```

**Why:** URL에 editable collection의 `collectionId`가 있으면 effect가 loadDetail을 호출하는데, 실패 시 detail이 갱신되지 않고 busy=false 전환에서 effect가 재실행되어 `!detail || detail.collectionId !== queryCollectionId`가 여전히 참 → 요청이 계속 실패하는 한 무한 재시도(예: 일시적 500 또는 서버가 거부하는 id). `cmsQueryOpenKeyRef` 같은 실패 가드가 없음.

**Fix:** 마지막으로 시도/실패한 collectionId를 ref(`lastLoadAttemptRef`)로 추적해 동일 queryCollectionId 재호출을 스킵하거나, effect가 재시도 전에 검사하는 실패 상태를 설정.

### Bookings — Admin

**Search box overwrites local query state with the trimmed URL value, eating leading/edge whitespace mid-typing** (low → see Low; listed once) 

**Localized error messages render with the success (green) color because the color check still tests for the Korean substring '실패'** (medium, bug) — `src/components/builder/backups/BackupsAdmin.tsx`

```ts
{message ? <span style={{ marginLeft: 'auto', fontSize: 12, color: message.includes('실패') ? '#dc2626' : '#16a34a' }}>{message}</span> : null}
```

**Why:** 상태 메시지는 로케일별 COPY 맵으로 지역화됐지만 색상 결정은 여전히 `message.includes('실패')`를 하드코딩합니다. en/zh-hant 로케일의 실패 메시지엔 '실패'가 없으므로 실패(res.ok===false)가 성공 녹색(#16a34a)으로 표시 — 실패한 백업/복원이 성공처럼 보임. ko만 정상.

**Fix:** 색상을 명시적 상태로 결정. `setMessage({ text, tone: 'error' })` 또는 `isError` boolean을 각 `!res.ok` 분기에서 설정하고 `color: isError ? '#dc2626' : '#16a34a'`.

**Sync status message colored by substring-matching localized text, now inconsistent with localized summary copy** (medium, bug) — `src/components/builder/bookings/CalendarSyncAdmin.tsx`

```ts
{message ? <div style={{ fontSize: 12, color: message.includes('오류') || message.includes('失敗') || message.includes('錯誤') || message.includes('Error') ? '#dc2626' : '#16a34a' }}>{message}</div> : null}
```

**Why:** 색상이 지역화 메시지의 부분문자열 매칭으로 결정됩니다. (1) ko/zh 성공이 `· 오류 없음`/`· 無錯誤`로 끝나 '오류'/'錯誤'를 포함 → 성공이 오류색(빨강). (2) 영어 실패는 `errors`(소문자)/`Sync failed`로 끝나는데 검사는 대문자 `Error`를 case-sensitive로 보므로 매칭 실패 → 영어 실패가 성공색(녹색).

**Fix:** 렌더된 지역화 텍스트로 성공/실패를 추론하지 말 것. `{ text, tone: 'ok' | 'error' }` 상태를 두고 `res.ok`/`payload.result`/`errs.length` 결과로 tone을 결정론적으로 설정한 뒤 색상을 tone에서 선택.

### Bookings — Admin (i18n 와이어업)

**Per-locale c.policyLabel computed but English-only cancellationPolicyLabel rendered in policy dropdown** (medium, wiring) — `src/components/builder/bookings/BookingServicesAdmin.tsx`

```tsx
{cancellationPolicyLabel(policy)}
```

**Why:** 로케일별 `c.policyLabel`(ko '수수료 N%'/'비활성', zh-hant '手續費 N%'/'停用')이 모든 로케일에 정의됐으나 호출처가 없음(grep 확인). 정책 `<option>`이 모듈 레벨 `cancellationPolicyLabel`을 렌더해 영어 'fee N% · inactive'를 하드코딩 → ko/zh 관리자가 영어를 봄.

**Fix:** `{cancellationPolicyLabel(policy)}`를 `{c.policyLabel(policy)}`로 변경.

**Per-locale c.toolbar(count) computed but hardcoded English ', including inactive drafts.' rendered** (medium, wiring) — `src/components/builder/bookings/BookingServicesAdmin.tsx`

```tsx
<div className={styles.muted}>{services.length} {c.cardLabel}{services.length > 1 && locale === 'en' ? 's' : ''}, including inactive drafts.</div>
```

**Why:** 모든 로케일에 `c.toolbar(count)`가 정의됐으나 호출되지 않고, 툴바가 수동 조합으로 영어 리터럴 ', including inactive drafts.'를 하드코딩 → ko/zh 관리자가 '5 서비스, including inactive drafts.'(한영 혼합)를 봄.

**Fix:** `<div className={styles.muted}>{c.toolbar(services.length)}</div>`로 교체.

### Bookings — Member-facing

**'Clear search' link only clears the query, not the date range, despite being shown for dates** (medium, bug) — `src/components/members/MemberBookingsSearchForm.tsx`

```tsx
{query || dateFrom || dateTo ? (
          <Link className={styles.accountLink} href={buildHref(locale, current, { query: '' })}>
            {labels.clearSearch}
          </Link>
        ) : null}
```

**Why:** 링크는 query OR dateFrom OR dateTo가 설정되면 표시되지만 href 오버라이드는 `{ query: '' }`뿐입니다. `buildHref`가 `current`(dateFrom/dateTo가 로컬 입력 상태에서 옴)로 URL을 만들어 날짜 범위가 그대로 유지 → 날짜만 설정된 사용자가 클릭하면 아무것도 지워지지 않음.

**Fix:** 세 입력 모두 오버라이드: `href={buildHref(locale, current, { query: '', dateFrom: '', dateTo: '' })}`.

**Email-document URL uses a display label (ownerLabel) as a booking-id path segment** (medium, bug) — `src/components/members/BillingPortalView.tsx`

```tsx
href={`/${document.locale}/account/bookings/${document.ownerLabel}/documents/${document.documentId}/email`}
```

**Why:** 파일 내 다른 `*Label` 필드는 모두 표시 텍스트로만 쓰이고 형제 `RenewPaymentLinkButton`은 id(`document.documentId`)로 경로를 만듭니다. `ownerLabel`(표시 라벨)을 booking 식별자 경로 세그먼트로 보간하면 URL-safe하지 않고 라우트가 기대하는 booking id와 일치하지 않을 가능성 → 잘못된/존재하지 않는 예약을 가리키는 URL.

**Fix:** 이 세그먼트에 표시 라벨 대신 라우트가 기대하는 booking id 필드를 사용(`documentId`가 쓰이는 방식과 일치).

**Partial-refund message no longer shows the actual refund percentage (partialRefundPercent now unused)** (medium, improvement) — `src/components/builder/bookings/BookingManageClient.tsx`

```ts
if (policy.refundDecision === 'partial') return `${labels.partialRefundAvailable}${feeText}`;
```

**Why:** 구버전은 `${policy.partialRefundPercent}% refund available...`로 구체값을 표시했으나, 재작성된 refundLabel은 일반 문자열 `labels.partialRefundAvailable` + 선택적 수수료%만 출력합니다. `policy.partialRefundPercent`는 타입에 선언되고 API가 전달하지만 컴포넌트 어디서도 참조되지 않음 → 고객이 실제 부분환불 비율을 못 봄(금전/취소 화면의 정보 손실).

**Fix:** partial 분기에 실제 비율을 렌더: `return `${labels.partialRefundAvailable} (${policy.partialRefundPercent}%)${feeText}`;` 또는 의도적으로 드롭이라면 페이로드 타입에서 필드 제거.

**New refundAmountCents field added to payload type but never displayed (missing wire-up)** (medium, wiring) — `src/components/builder/bookings/BookingManageClient.tsx`

```ts
refundAmountCents?: number;
```

**Why:** diff가 `refundAmountCents?: number`를 정책 페이로드 타입에 추가해 API가 정확한 환불액(cents)을 반환함을 시사하나, 파일 어디서도 읽히지 않음(refundLabel은 refundDecision/cancellationFeePercent만 소비). 서버가 계산한 정확한 환불액이 고객에게 도달하지 않음.

**Fix:** refundDecision이 'full'/'partial'일 때 refundAmountCents를 통화로 포맷해 노출. 그렇지 않으면 타입에서 필드 제거.

**publishSnapshot revisions are not refreshed after a successful publish, breaking a second publish under optimistic concurrency** (medium, data-integrity) — `src/components/builder/BuilderPagePublishReadiness.tsx`

```ts
      setPublishStatus('success');
      await runChecks();
```

**Why:** publishPage가 publishSnapshot prop에서 낙관적 동시성 가드(expectedDraftRevision 등)를 만듭니다. 성공 시 runChecks()만 호출하고 publishSnapshot(prop, 상태로 보유·재페치 안 됨)을 갱신하지 않습니다. 한 번 발행 후 서버 revision이 진행됐는데 컴포넌트는 stale 스냅샷을 보유 → 같은 세션에서 재발행 시 stale expected 값으로 동시성 충돌 거부, 전체 리로드 전까지 정당한 재발행 차단.

**Fix:** 발행 엔드포인트가 새 draft/published revision+savedAt를 반환하게 하고 publishSnapshot을 컴포넌트 상태로 보유해 성공 시(가능하면 runChecks에서도) 갱신.

### Bookings — Analytics

**buildBookingTrendSeries crashes (RangeError) on malformed from/to options** (medium, robustness) — `src/lib/builder/bookings/analytics.ts`

```ts
  const endMs = options.to ? Date.parse(options.to) : nowMs;
+  const startMs = options.from ? Date.parse(options.from) : endMs - ((days - 1) * 24 * 60 * 60 * 1000);
+  const buckets = new Map<string, BookingTrendPoint>();
+
+  for (let index = 0; index < days; index += 1) {
+    const bucketDate = new Date(startMs + index * 24 * 60 * 60 * 1000);
+    const day = bucketDate.toISOString().slice(0, 10);
```

**Why:** endMs/startMs가 `Date.parse(options.to/from)`에서 직접 옵니다. 파싱 불가 문자열이면 NaN → `new Date(NaN).toISOString()`이 'RangeError: Invalid time value'를 throw. diff가 추가한 `dayKeyUtc()`는 booking 타임스탬프에 NaN 가드를 두지만 외부 from/to 필터엔 가드가 없어 잘못된 필터값이 buildBookingAnalyticsBundle 전체를 다운시킴.

**Fix:** 사용 전 검증: `const parsedTo = options.to ? Date.parse(options.to) : NaN; const endMs = Number.isFinite(parsedTo) ? parsedTo : nowMs;` startMs도 유사하게 가드해 유한하지 않으면 nowMs 기반 창으로 fallback.

### Members/Account

**memberEmailAliases try/catch guards parse errors but not non-array JSON, can crash Server Component** (medium, robustness) — `src/app/[locale]/account/page.tsx`

```ts
const memberEmailAliases = (() => {
    try {
      return member.customFields?.memberEmailAliases ? JSON.parse(member.customFields.memberEmailAliases) : [];
    } catch {
      return [];
    }
  })() as string[];
```

**Why:** try/catch가 파싱 에러만 막고 배열이 아닌 유효 JSON은 막지 못합니다. 따옴표 문자열/숫자/객체면 JSON.parse 성공, catch 미발동, `as string[]` 캐스트가 거짓 → 하류 `.length`(문자열은 truthy), `.map`(숫자/객체는 undefined → TypeError). async Server Component이므로 throw 시 계정 페이지가 500.

**Fix:** 파싱 후 형태 검증: `const parsed = ...; return Array.isArray(parsed) ? parsed.filter((v) => typeof v === 'string') : [];` (try 내부, catch는 [] 반환).

### Builder — Migrations / Canvas

**run() result message (including failure detail) is immediately clobbered by refresh()** (medium, bug) — `src/components/builder/migrations/MigrationsAdmin.tsx`

```ts
      if (!res.ok || !payload.ok) {
        setMessageTone('error');
        setMessage(copy.runFailed(payload.failed?.id ?? '', payload.failed?.error ?? ''));
      } else {
        setMessageTone('success');
        setMessage(copy.runSummary(payload.applied.length, payload.skipped.length));
      }
      await refresh();
```

**Why:** 이번 diff가 refresh()에 `setMessageTone('success'); setMessage(copy.refreshStatus);`를 추가했습니다. run()이 마이그레이션 결과 메시지(실패 상세 또는 applied/skipped 요약)를 설정한 뒤 `await refresh()`를 호출 → 성공 GET 시 refresh()가 메시지를 copy.refreshStatus로, tone을 'success'로 무조건 덮어씀. 운영자가 요약을 못 보고 **실패한 마이그레이션이 녹색 'Refreshed'로 가려짐**(이번 변경의 회귀).

**Fix:** run()의 일부로 호출될 때 refresh()가 상태 메시지를 설정하지 않게 할 것. refresh()의 setMessage를 제거하고 독립 새로고침 버튼 핸들러에서만 설정하거나, run()이 자체 메시지를 refresh() 후 재적용(또는 suppress 플래그 전달).

**locale param accepted but ignored — image alt and button label are not localized unlike the sibling text node** (medium, wiring) — `src/components/builder/canvas/CanvasNode.tsx` (lines 323, 375)

```tsx
// createRepeaterTemplateImageNode body:
      alt: 'Bound image',   // hardcoded English
// createRepeaterTemplateButtonNode body:
      label: 'Bound button', // hardcoded English
// sibling createRepeaterTemplateTextNode correctly does:
const fallbackText = getRepeaterTemplateCopy(locale).fallbackText;
```

**Why:** `createRepeaterTemplateImageNode`/`createRepeaterTemplateButtonNode`가 `locale: Locale`를 받고 builderLocale로 호출되지만 `getRepeaterTemplateCopy(locale)`를 호출하지 않습니다. 하드코딩 영어 'Bound image'/'Bound button'이 ko/zh 로케일 활성 시 빌더 캔버스 placeholder로 표시. 형제 text 노드는 올바르게 지역화함.

**Fix:** `RepeaterTemplateCopy`에 boundImageAlt/boundButtonLabel(지역화)을 추가하고 두 함수에서 `getRepeaterTemplateCopy(locale)`로 사용.

### Builder — Bookings config

**BookingEmailTemplateType imported but not used to type-check config keys — template key safety lost** (medium, wiring) — `src/lib/builder/bookings/email-template-config.ts` (line 1)

```ts
import type { BookingEmailTemplateType } from '@/lib/builder/bookings/types';
...
const bookingEmailTemplateConfigByLocale = {
  ko: {
    'customer-confirmation': { ... },
    'admin-notification': { ... },
    ...
  },
  en: { ... },
} as const;
```

**Why:** `BookingEmailTemplateType` 유니온이 import됐으나 `bookingEmailTemplateConfigByLocale`의 타입 제약으로 적용되지 않음. 어노테이션이 없어 BookingEmailTemplateType에 템플릿 타입을 추가/변경해도 config가 동기화되지 않으면 컴파일 에러가 없음.

**Fix:** 내부 config 형태에 타입 적용: `const bookingEmailTemplateConfigByLocale: Record<'ko' | 'zh-hant' | 'en', Record<BookingEmailTemplateType, { label: string; description: string; subject: string; body: string }>> = { ... } as const;` → 모든 로케일이 모든 템플릿 타입을 커버하도록 강제.

### Bookings — Member-facing pages

**searchParams typed as string but Next.js can deliver string[] for repeated keys, .trim() then throws** (medium, robustness) — `src/app/[locale]/account/bookings/page.tsx`

```ts
q: searchParams?.q?.trim().toLowerCase() ?? '',
```

**Why:** `searchParams`가 string|undefined로 선언됐지만 Next.js 14 App Router는 반복 키(`?q=a&q=b`, URL로 손쉽게 생성)에 대해 런타임에 string[]를 전달합니다. 배열에 `.trim()` 호출 시 'q.trim is not a function' 서버 TypeError → force-dynamic 페이지가 500. 동일한 무가드 `.trim()`이 `buildBookingsHistoryExportHref`에도 있음. 선언 타입은 프레임워크가 강제하지 않는 가정.

**Fix:** 문자열 연산 전 배열값 정규화: `firstParam(v) => Array.isArray(v) ? v[0] : v` 헬퍼를 두고 searchParams 필드 타입을 `string | string[] | undefined`로. normalizeBookingHistory* 헬퍼에 넘기기 전 q 등에 firstParam 적용.

### Builder — Datasets (admin)

**Email-document email route — redundant getCustomerBookingPortal call — portal result is never consumed** (medium, bug) — `src/app/[locale]/account/bookings/[bookingId]/documents/[documentId]/email/route.ts` (line 21)

```ts
const memberEmails = getMemberPortalEmails(member);
  const portal = await getCustomerBookingPortal(member.email, locale, undefined, memberEmails);
  const sourceBooking = (await listBookings({ includeCancelled: true }))
    .find((item) => item.bookingId === params.bookingId && memberEmails.includes(item.customer.email.trim().toLowerCase()));
```

**Why:** `portal`이 (bookings/services/staff를 다시 가져오는 3-way Promise.all) 비싼 async 호출로 계산되나 읽히지 않습니다 — 핸들러는 `listBookings`로 직접 재조회하고 자체 memberEmails 체크를 인라인 수행. 매 이메일 요청마다 데이터 페치 비용이 2배. **주의:** 이 finding의 제안(인라인 체크가 인가에 충분)은 위 High IDOR(`memberEmailAliases`)와 모순됩니다 — 그 인라인 체크 자체가 취약점이므로, `portal` 호출 제거는 성능 정리일 뿐 인가를 고치지 않습니다. 인가 수정은 High finding을 따를 것.

**Fix:** 미사용 import와 `const portal = ...` 라인을 제거(성능 정리). 단, 인가 안전성은 별도로 High IDOR finding의 수정(서버 신뢰 신원 기반 인가)으로 처리해야 함.

---

## Low

> 아래 Low는 모두 unverified(검증자 판정 없음)입니다.

### Bookings — Admin

**Search box overwrites local query state with the trimmed URL value, eating leading/edge whitespace mid-typing** (low, robustness) — `src/components/builder/bookings/BookingDashboardAdmin.tsx`

```ts
const params = new URLSearchParams(searchKey);
                if (next.trim()) params.set('q', next.trim()); else params.delete('q');
```

**Why:** onChange가 TRIMMED 값을 URL에 쓰지만 UNtrimmed 값을 state에 저장. URL이 바뀌면 effect가 `normalizeBookingDashboardQuery(...)`로 state를 trimmed URL값으로 리셋해 방금 입력한 선행/말단 공백을 지움 → 제어 입력이 입력값과 불일치.

**Fix:** write/read를 일관되게 — state에 trimmed 값 저장(`setQuery(next.trim())`)하거나 debounce/blur 경계에서만 URL push하고 read와 같은 정규화로 비교. 가장 간단히 onChange와 URL을 동일 값(추가 trim 없이)으로.

### AI Generator

**All newly-appended navigation rows are assigned the same indexPath** (low, data-integrity) — `src/components/builder/ai-generator/AiGeneratorWizard.tsx`

```ts
        indexPath: navEntry?.indexPath ?? (willAppend ? `${navigationEntries.length + 1}` : '-'),
```

**Why:** map 내에서 append될 모든 행이 상수 `${navigationEntries.length + 1}`로 indexPath를 계산(navigationEntries.length는 map 중 불변). 둘 이상 append 시 동일 nav index가 렌더되고 diff-index 속성이 첫 행을 제외하고 중복/오류.

**Fix:** append된 행 수를 추적(running ordinal)하거나 willAppend slug를 pre-filter해 그 인덱스를 사용: `navEntry?.indexPath ?? (willAppend ? `${navigationEntries.length + appendOrdinal + 1}` : '-')`.

**Controlled schedule input default is recomputed (drifting) on every render** (low, robustness) — `src/components/builder/ai-generator/AiGeneratorWizard.tsx`

```ts
                      const scheduledInputValue = scheduledPublishInputByPageId[pageEntry.pageId] ?? defaultScheduledPublishInput();
```

**Why:** `defaultScheduledPublishInput()`가 호출 시점의 now+24h를 반환해 제어 `<input type="datetime-local">`의 value로 직접 사용됩니다. state로 보유하지 않아 무관한 리렌더마다 값이 분 단위로 표류 → 제출 직전 기본 일정 시각이 계속 이동.

**Fix:** 기본값을 한 번 계산해 보유 — created pages 설정 시 `scheduledPublishInputByPageId[pageId]`를 state로 초기화하거나 pageId별 안정 기본값을 memoize.

### Builder — Canvas / Workspace

**Success check uses data.success while sibling page handlers use data.ok — possible response-shape mismatch** (low, bug) — `src/components/builder/canvas/PageSwitcher.tsx`

```ts
const data = (await response.json()) as { success?: boolean; pageId?: string; page?: PageMeta; error?: string };
      const nextPageId = data.pageId ?? data.page?.pageId ?? null;
      if (!data.success || !nextPageId) {
        setErrorMessage(data.error || '누락된 페이지를 생성하지 못했습니다.');
        return;
      }
```

**Why:** 같은 diff의 형제 핸들러(handleUpdateMemberAccess, persistPageOrder)는 `data.ok`로 성공을 판단하나 handleCreateMissingPage만 `data.success`. POST `/api/builder/site/pages`가 `{ ok: true }` 컨벤션을 따르면 성공 시 `data.success`가 undefined → `!data.success` 분기가 발동해 생성된 페이지가 실패로 보고됨. 응답 본문이 미표시라 in-file 컨벤션 불일치 수준.

**Fix:** POST 응답 계약 확인. `{ ok: true, pageId/page }`라면 가드를 `const succeeded = data.success ?? data.ok; if (succeeded === false || !nextPageId)`로 변경해 양쪽 envelope 수용.

**Nav-card <small> detail text dropped: both <span> and <small> now render the same description** (low, wiring) — `src/components/builder/BuilderWorkspaceDashboard.tsx`

```tsx
<span>{copy.appMarketDescription}</span>
              <small>{copy.appMarketDescription}</small>
```

**Why:** 리팩터 전 카드는 두 줄(<span>Install and manage</span> + <small>Manifest catalog and lifecycle</small> 등)이었으나, 리팩터가 <span>과 <small> 모두를 단일 copy 값에 연결해 두 줄이 동일 텍스트가 되고 원래 상세 문자열이 손실됨. 상세용 copy 키 미추가 → 잊힌 와이어업.

**Fix:** 전용 copy 키(appMarketDetail/aiGeneratorDetail) 추가 후 `<small>{copy.appMarketDetail}</small>` 등으로 렌더.

### Admin — Consultation

**Attorney-queue Section title duplicates its first sub-heading and its description is mis-wired to an unrelated form hint** (low, wiring) — `src/app/[locale]/admin-consultation/page.tsx`

```tsx
title={copy.knowledgeGapTitle}
        description={copy.knowledgeForm.answerHint}
```

**Why:** Section title이 `copy.knowledgeGapTitle`로 설정됐는데 이는 아래 첫 <h3>('로그 기반 공백 후보')와 동일 값 → 섹션 헤더와 첫 서브헤딩이 동일 텍스트. description은 폼 답변 작성 힌트(`knowledgeForm.answerHint`)에 잘못 연결됨. 전용 title/description copy가 ConsultationCopy에 미추가되어 설명 텍스트가 드롭·미스매치 콘텐츠로 대체.

**Fix:** 전용 copy 키(knowledgeQueueTitle/knowledgeQueueDescription)를 추가하고 `title={copy.knowledgeQueueTitle} description={copy.knowledgeQueueDescription}`, `copy.knowledgeGapTitle`은 내부 <h3>에만 유지.

### Bookings — Member-facing pages

**Unguarded property access on getStaffAvailability result** (low, robustness) — `src/app/[locale]/account/bookings/[bookingId]/page.tsx` (~line 303)

```ts
const officeTimezone = staffAvailability.timezone;
```

**Why:** `getStaffAvailability(sourceBooking.staffId)`의 awaited 결과의 `.timezone`을 직접 역참조. null/undefined 반환 가능(스태프 삭제/가용성 미설정) 시 TypeError로 예약상세 페이지 렌더 크래시. 형제 lookup(getPackage/getPackageCredit)은 방어적으로 null 처리해 비대칭. (시그니처 미표시라 confidence low.)

**Fix:** 가드 + fallback: `const officeTimezone = staffAvailability?.timezone ?? booking.customerTimezone ?? 'UTC';` (또는 가용성 레코드 없을 때 빈 office-time 블록 표시).

### Builder — Bookings config / Resources

**weeklyFromTemplate accesses template.weekly[day] without the nullish guard used elsewhere** (low, robustness) — `src/components/builder/bookings/BookingResourcesAdmin.tsx`

```ts
dayOfWeeks.map((day) => [day, template.weekly[day].map((block) => ({ ...block }))]),
```

**Why:** 같은 파일 `cloneWeekly`는 `(weekly[day] ?? []).map(...)`로 방어하나 `weeklyFromTemplate`는 무가드. 어떤 템플릿이 7개 dayOfWeek 중 하나를 누락하면(평일전용 템플릿이 토/일 미설정) `template.weekly[day]`가 undefined → `.map` TypeError로 리소스 에디터 크래시. cloneWeekly와의 불일치가 증거.

**Fix:** cloneWeekly의 가드 미러링: `dayOfWeeks.map((day) => [day, (template.weekly[day] ?? []).map((block) => ({ ...block }))])`.

**Per-locale c.reminderSummary and c.inactiveResources defined but never called** (low, wiring) — `src/components/builder/bookings/BookingServicesAdmin.tsx`

```ts
reminderSummary: (hours: Array<1 | 24>) => (hours.length > 0 ? `Reminder ${hours.join('h + ')}h` : 'Reminders off'),
```

**Why:** 로케일별 reminderSummary/inactiveResources 헬퍼가 정의됐으나 호출처 없음(grep). reminder 칩이 `${c.reminderLabel} ...h`/`off`를 인라인해 ko/zh에 영어 'h'/'off' 접미사 누출. prepared-but-unwired 지역화 값(영향 작음).

**Fix:** 미사용 항목을 dead code로 제거하거나 reminder 칩을 `c.reminderSummary(service.reminderOffsetsHours ?? [24])`에 연결.

### Builder — Bookings (policies)

**toggleActive has no error handling: unhandled rejection on network failure and silent failure on non-ok response** (low, robustness) — `src/components/builder/bookings/BookingPoliciesAdmin.tsx`

```ts
const toggleActive = async (policy: BookingCancellationPolicy) => {
    const res = await fetch(`/api/builder/bookings/cancellation-policies/${policy.policyId}`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'same-origin',
      body: JSON.stringify({ isActive: !policy.isActive }),
    });
    if (res.ok) {
      const data = (await res.json()) as { policy: BookingCancellationPolicy };
      setPolicies((current) => current.map((item) => (item.policyId === policy.policyId ? data.policy : item)));
    }
  };
```

**Why:** 같은 컴포넌트의 `save`는 try/catch로 실패 시 error를 설정하나 `toggleActive`는 try/catch 없음. reject된 fetch(네트워크 오류)가 onClick에서 직접 호출되어 unhandled promise rejection이 되고, non-ok 응답은 피드백 없이 무음 처리 → 버튼이 아무것도 안 한 것처럼 보임.

**Fix:** fetch를 try/catch로 감싸 `setError(c.saveFailed)`로 노출(및 `!res.ok` 분기 처리), `save`의 패턴과 일치.

### Bookings — Member-facing (flow)

**Deposit booking no longer shows the actual remaining balance amount** (medium → categorized; see note) — 본 finding은 confidence high이나 검증자 판정 없음. 심각도 medium으로 분류.

> 정정: 위 finding은 medium입니다 — Medium 섹션 누락 방지를 위해 여기에 명기. 본문은 아래.

**Deposit booking no longer shows the actual remaining balance amount** (medium, wiring) — `src/components/builder/bookings/BookingFlowSteps.tsx` (~line 883/885)

```ts
                        ? `${formatBookingAmount(selectedPrice.amountDueNow, selectedPrice.currency)} ${copy.labels.serviceDueNow} · ${copy.labels.depositDue} ${copy.labels.laterBalance}`
```

**Why:** 변경 전 deposit 분기는 구체적 잔액(`잔액 ${formatBookingAmount(selectedPrice.balanceDueAfterOnlinePayment, ...)}`)을 렌더했으나, i18n 재작성이 정적 라벨 `copy.labels.depositDue`/`copy.labels.laterBalance`로 대체. 이들은 금액을 받는 함수가 아닌 일반 속성 read이므로 `selectedPrice.balanceDueAfterOnlinePayment`가 결제 패널 어디서도 렌더되지 않음 → deposit 서비스 고객이 정확한 잔액을 못 봄(금융 투명성 회귀).

**Fix:** deposit 메시지에 잔액 금액 재도입 — `copy.labels.laterBalance`를 포맷된 잔액 문자열을 받는 함수로 만들거나(`copy.hud.recordOf(...)` 패턴) 라벨 뒤에 `formatBookingAmount(selectedPrice.balanceDueAfterOnlinePayment, selectedPrice.currency)`를 덧붙임.

### Builder — Datasets (admin) / robustness

**Unvalidated filters/sort arrays cast and passed into dataset binding/query logic** (low, robustness) — `src/app/api/builder/sites/[siteId]/pages/[pageKey]/datasets/preview/route.ts`

```ts
const filters = Array.isArray(record.filters) ? record.filters : undefined;
const sort = Array.isArray(record.sort) ? record.sort : undefined;
...
filters: filters as BuilderPageDatasetFilter[] | undefined,
sort: sort as BuilderPageDatasetSort[] | undefined,
```

**Why:** targetId/collectionId/mode는 allowlist 검증하나 `filters`/`sort`는 배열 여부만 확인하고 타입 캐스트해 binding/query builder로 전달. 요소 형태(필드명, 연산자, 방향) 미검증으로 임의 객체가 도달. guardBuilderRead(빌더 관리자 전용)·in-memory preview·비영속이라 영향 제한적이나 malformed 항목이 예상치 못한 쿼리 동작/에러 유발 가능.

**Fix:** binding 구성 전 zod 스키마로 `filters`/`sort` 검증(허용 필드명, 닫힌 연산자 집합, asc/desc 방향), targetId/collectionId/mode의 기존 allowlist 검사를 미러링.

### Analytics

**Bookings with an unrecognized source are silently misattributed to 'web'** (low, data-integrity) — `src/lib/builder/bookings/analytics.ts`

```ts
    const bucket = breakdown.get(booking.source) ?? breakdown.get('web')!;
```

**Why:** breakdown Map이 'web'/'admin'만 시드. 둘 다 아닌 source(레거시/임포트/미래값/손상 레코드)는 `??`로 'web' 버킷에 떨어져 web 총계/매출을 부풀림. 형제 funnel(buildBookingSourceFunnelBreakdown)은 실제 source로 그룹화해 두 리포트가 동일 데이터에 불일치.

**Fix:** 미지 source를 skip(`const bucket = breakdown.get(booking.source); if (!bucket) continue;`)하거나 실제 source 값으로 lazily 버킷 생성해 funnel과 일관 유지.

### Members/Account (client)

**memberEmailAliases JSON.parse result is not validated as an array before .length/.map** (low, robustness) — `src/components/members/MemberProfileClient.tsx`

```ts
const memberEmailAliases = (() => {
    try {
      return current.customFields?.memberEmailAliases ? JSON.parse(current.customFields.memberEmailAliases) : [];
    } catch {
      return [];
    }
  })() as string[];
```

**Why:** try/catch가 JSON 구문 오류만 막고 배열 아닌 유효 JSON은 못 막음. 문자열 리터럴(`"foo"`)이면 `.length > 0`이 truthy이고 후속 `.map`이 throw해 렌더 크래시. 숫자/객체는 length 검사를 무음 통과하나 잘못된 타입.

**Fix:** 파싱 후 형태 검증: `const parsed = JSON.parse(...); return Array.isArray(parsed) ? parsed.filter((v) => typeof v === 'string') : [];`.

### Builder — Datasets page (admin)

**faqContent imported and gated on but its content is never rendered** (low, wiring) — `src/app/(builder)/[locale]/builder/[pageKey]/datasets/page.tsx`

```tsx
      {pageKey === 'home' && faqContent[locale] ? (
        <section style={{ marginTop: 20, color: '#64748b', fontSize: 13 }}>
          <p style={{ margin: 0 }}>
            {copy.helper}
          </p>
        </section>
      ) : null}
```

**Why:** faqContent가 import·로케일 인덱싱되나 truthiness 게이트로만 쓰이고 실제 데이터는 미렌더 → 불완전 와이어업(FAQ 데이터를 표시하려던 의도이거나 import 자체가 불필요).

**Fix:** 의도된 `faqContent[locale]` 데이터를 섹션에 렌더하거나, import를 제거하고 helper 섹션을 더 의미 있는 조건으로 게이트.

### Lint triage (ESLint unused-var / dead-code)

**Stale getSdkCopy call inside page component — copy computed but never rendered** (low, bug) — `src/app/(builder)/[locale]/admin-builder/_dev/functions/page.tsx` (line 19)

```ts
const copy = getSdkCopy(params.locale as 'ko' | 'zh-hant' | 'en');
  return (
    <main ...>
      <header ...>
        <h1 ...>{shellCopy.title}</h1>
        <p ...>{shellCopy.description}</p>
      </header>
      <FunctionsAdmin locale={params.locale as 'ko' | 'zh-hant' | 'en'} />
    </main>
  );
```

**Why:** generateMetadata가 이미 자체 getSdkCopy를 호출. line 19의 `copy`는 속성이 전혀 참조되지 않는 dead assignment(JSX는 shellCopy를 렌더). 페이지 콘텐츠가 SDK 문서→functions 관리로 전환된 잔재.

**Fix:** line 19 삭제: `const copy = getSdkCopy(params.locale as 'ko' | 'zh-hant' | 'en');`.

**Unused Metadata import — generateMetadata was never implemented** (low, robustness) — `src/app/(builder)/[locale]/admin-builder/bookings/calendar/page.tsx` (line 1)

```ts
import type { Metadata } from 'next';
import BookingsAdminShell from '@/components/builder/bookings/BookingsAdminShell';
```

**Why:** `Metadata`를 import하나 generateMetadata가 없음 → stale stub. 관리자 캘린더 뷰에 메타데이터 title/description이 없어 SEO/탭 제목에 영향.

**Fix:** import 제거하거나 getBookingsAdminCopy(locale).pages.calendar.title/.subtitle로 `{ title, description }`을 채우는 generateMetadata export 추가.

**managePath intentionally stripped from JSON but ESLint sees it as unused — consider Omit<> to avoid lint noise** (low, robustness) — `src/app/api/members/bookings/route.ts` (lines 25-26)

```ts
upcoming: portal.upcoming.map(({ managePath, ...booking }) => booking),
    past: portal.past.map(({ managePath, ...booking }) => booking),
```

**Why:** `managePath`를 응답에서 제외하려고 구조분해. 패턴은 올바르나(manage 토큰 경로 누출 방지) 변수 미참조로 no-unused-vars 발동. 삭제하면 안 됨(제거 시 managePath가 응답에 포함되어 토큰 URL 노출).

**Fix:** 구조분해 변수를 `_managePath`로 rename해 argsIgnorePattern으로 경고 억제하면서 의도된 omission 보존: `portal.upcoming.map(({ managePath: _managePath, ...booking }) => booking)`.

**resourceById Map built but never read — resources accessed directly by iterating requiredResources** (low, wiring/bug) — `src/lib/builder/bookings/availability.ts` (line 144)

> (logic 배치와 lint-triage에 중복 보고 → 1건으로 병합.)

```ts
const requiredResources = resources.filter((resource) => requiredResourceIds.includes(resource.resourceId));
  const resourceById = new Map(requiredResources.map((resource) => [resource.resourceId, resource]));
  const bufferBefore = service.bufferBeforeMinutes;
```

**Why:** `resourceById`가 `requiredResources`에서 구성되나 어디서도 참조되지 않음. 슬롯 생성 로직은 `requiredResources`를 직접 순회(hasResourceBlockedTime/WeeklyConflict/CapacityConflict)하고 id 조회를 안 함. 잊힌 와이어업 또는 dead code(슬롯당 할당 오버헤드).

**Fix:** 미사용 `resourceById` 선언을 제거하거나, 리소스를 id로 해석해야 하는 충돌 로직(예: resourceOverlapCount에서 `booking.resourceIds`를 순회해 버퍼/용량 조회)에 연결.

**_summaryCurrencyTotals intentional omission — underscore prefix convention not recognized by ESLint config** (low, robustness) — `src/lib/builder/payment-analytics.ts` (line 931)

```ts
const { currencyTotals: _summaryCurrencyTotals, ...summaryWithoutCurrencyTotals } = summary;
        const currencyTotals = Array.from(item.currencyTotals.values())
```

**Why:** 최상위 currencyTotals를 의도적으로 버려 `summaryWithoutCurrencyTotals`를 스프레드(per-currency breakdown은 item.currencyTotals에서 재구성). `_` 접두사가 의도를 표시하나 .eslintrc에 varsIgnorePattern:'^_'가 없어 no-unused-vars 발동.

**Fix:** `.eslintrc.json` rules에 `"@typescript-eslint/no-unused-vars": ["error", { "varsIgnorePattern": "^_", "argsIgnorePattern": "^_" }]` 추가. 코드 변경 불필요.

---

## Coverage

**감사 대상 (audited):**
- 변경된 API 라우트 49개
- 최고 churn 로직 파일 130개 (churn ≥ 70)
- 지역화 카피 파일 34개 (light structural pass — 경량 구조 검토)
- ESLint unused-var 사이트 8곳

**심층 감사하지 않은 영역 (NOT deeply audited):**
- 약 578개의 lower-churn 변경 파일
- 동시 진행 중인 Codex live-edit 존의 22개 파일 (의도적으로 제외)

검증자(verdict)는 high/critical findings에만 적용됨. 본 데이터셋에는 `uncertain` 판정이 없으며, refuted 2건(public-page.tsx 서비스/변호사 바인딩, cms-editable.ts reference coercion)은 규칙에 따라 제외함. medium/low는 모두 unverified.

---

## Action buckets

> 본 섹션은 **CONFIRMED finding 9건**(critical 1 + high 8)만 분류합니다. medium/low는 unverified이므로 버킷에서 제외(예: lint-triage의 dead-code/rename 정리는 안전·기계적이나 unverified이므로 A에 넣지 않음).

### A. Quick wins — safe, mechanical fixes
- **없음.** CONFIRMED finding 중 순수하게 안전·기계적인 항목은 없습니다. (안전·기계적 정리 후보인 lint-triage 항목들은 unverified이므로 이 버킷에서 제외합니다.)

### B. Improvements / 보완 — incomplete wiring & missing validation/error handling
- **AI Generator 발행/일정/취소 fetch 오류 무음 처리** — `src/components/builder/ai-generator/AiGeneratorWizard.tsx` (high, confirmed). 세 핸들러에 catch 추가로 사용자 오류 표시 + unhandled rejection 방지. 데이터 무결성/인가를 건드리지 않는 누락된 오류 처리 보완.

### C. Needs careful change — touches core / data-integrity / risky
1. **Cross-account booking takeover (Critical IDOR)** — `.../cancel/route.ts` (+ reschedule). 인가 모델을 서버 신뢰 memberId로 재설계, 환불 경로 영향.
2. **Booking PII exfiltration (CSV/ICS export IDOR)** — `.../bookings/export/route.ts` (+ calendar).
3. **Billing-document IDOR (payment-link)** — `.../billing/documents/[documentId]/payment-link/route.ts`. 결제링크 재발급 영향.
4. **Booking/billing-document email action IDOR** — `.../bookings/[bookingId]/documents/[documentId]/email/route.ts`. 이메일 발송·문서상태 변경 영향.
5. **Unverified self-service email change → guest booking PII disclosure** — `src/app/api/members/me/route.ts`. customFields 키 허용목록 + 이메일 변경 검증 플로우(코어 인증).
6. **Missing auth / IDOR on `/api/booking/cancel`** — `src/app/api/booking/cancel/route.ts`. 토큰/관리자 인가 도입, Stripe 환불 강제 차단.
7. **CMS Cut silent data loss** — `src/components/builder/cms/ContentManagerClient.tsx`. 클립보드 소스를 전체 선택으로 변경(데이터 손실 방지).
8. **Attorney localized partial-patch lost-update** — `src/lib/builder/lawyers/source.ts`. 로케일 객체 deep-merge(데이터 무결성).

> **공유 수정 권고 (1–5의 근본원인):** `memberEmailAliases`는 `PATCH /api/members/me`의 키 허용목록 없는 customFields를 통해 자기수정 가능합니다. 단일 조치로 (i) `/api/members/me` customFields에서 `memberEmailAliases` 등 특권 키를 거부하고, (ii) 예약/청구 인가를 미검증 이메일 목록이 아닌 서버 신뢰 memberId(또는 검증된 기본 이메일)로 전환하면 1–5의 IDOR 계열이 함께 해소됩니다. 6번(booking/cancel)은 별개 메커니즘이므로 토큰/관리자 인가로 따로 수정해야 합니다.

---

## Fixes Applied (Claude, 2026-06-02)

> 동시 편집 중인 Codex 영역(site-settings/cms-datasets 등 22개 라이브 파일)은 건드리지 않았습니다. 아래는 **제가** 변경한 파일 — Codex의 WIP diff와 분리해 추적합니다. 검증: `typecheck` ✅, `lint` ✅ 0 errors, 단위테스트 2105 pass(잔여 실패 2건은 ① secrets-store의 **선재 flaky timing 테스트**[같은 ms 회전→타임스탬프 동일, 미수정 영역], ② container-gallery **Codex 영역 테스트 버그**[아래 '미수정' 참조]).

### 보안 (IDOR 계열 — 주요 벡터 차단, 잔여 경로 1건은 후속 필요)
> ⚠️ **Critical은 "완전 해소"가 아니라 "주요 벡터 차단"입니다.** 근본 설계 결함(예약/청구가 자기주장 가능한 이메일로만 인가, booking에 memberId 없음)은 남아 있습니다. 아래로 가장 쉬운 두 벡터(별칭 1-PATCH 주입, 사후 이메일 변경)는 막았지만, **미검증 신규 가입**(`POST /api/members/signup`이 임의 이메일을 `verified:false`로 즉시 생성, 기존 *회원* 충돌만 차단 — 게스트 예약 이메일은 미차단)으로 피해자 이메일 계정을 만들어 같은 IDOR을 재현하는 경로가 **남아 있습니다.** `member.verified`는 어디서도 인가에 강제되지 않습니다(설정만 됨). **정식 차단에는 이메일 소유권 확인 플로우가 필요**(아래 권고).
- **`src/lib/builder/members/members-engine.ts`** — `updateMemberProfile`에서 클라이언트가 보낸 `customFields.memberEmailAliases`를 strip. 별칭은 서버 관리(이메일 변경 시 이전 이메일 누적)만 허용 → **별칭 주입 벡터(critical + High #2/#3/#4)** 차단(`getMemberPortalEmails` 입력 정화).
- **`src/app/api/members/me/route.ts`** — 셀프서비스 기본 이메일 변경 차단(403 `email_change_requires_verification`). 사후 이메일 변경 벡터(#5) 차단. (+ UI `MemberProfileClient.tsx` 이메일 필드 readOnly화 + 안내 문구.)
- **`src/app/api/booking/cancel/route.ts`** — 서명 manage 토큰(bookingId+이메일 일치) 요구. 익명 예약 취소+Stripe 환불 강제(#6) 차단. (+ Playwright 테스트 `bookings-m26-customer-manage`를 토큰 사용/무토큰 401 검증으로 갱신 — Playwright는 미실행, typecheck만.)
- **잔여(미차단):** 신규 가입 벡터 — `signup`이 미검증 이메일을 수락하고 포털이 `verified`를 강제하지 않음. 안전한 quick-fix 없음(가입 시 이메일 확인 토큰 필요; 게스트→회원 전환 정상 케이스를 깨지 않으려면 검증 플로우 필수).

### 데이터 무결성 / 견고성 (확정 high)
- **`src/components/builder/cms/ContentManagerClient.tsx`** — Cut 클립보드를 `visibleRecords`→`detail.records`(전체 선택)로. 페이지 밖 선택 레코드 무음 손실 방지.
- **`src/lib/builder/lawyers/source.ts`** — `mergeLocalizedOverride`를 로케일별 **필드 deep-merge**로. 부분 패치가 기존 title/role/description/summary를 덮어쓰는 lost-update 수정.
- **`src/components/builder/ai-generator/AiGeneratorWizard.tsx`** — publish/schedule/cancel 3개 핸들러에 `catch` 추가. 네트워크 reject 시 사용자 오류 표시 + unhandled rejection 방지.

### 테스트 인프라 / 오류
- **`vitest.config.ts`** — `esbuild.jsx: 'automatic'`(Next 정렬). `React is not defined` 류 잠재 렌더 크래시 일괄 해소.
- **`.../calendar-sync/__tests__/sync-engine.test.ts`** — 카운트 단언 `toEqual`→`toMatchObject`(신규 `reconciliationFeed` 필드 무시, 카운트·부수효과는 그대로 검증). *주의: 피드 내용 정밀검증은 의식적으로 미포함 — 별도 피드 테스트 추가 권장.*
- **`.../integration/wix-parity.smoke.test.ts`** — migration+backup 테스트 `testTimeout` 20s(전체 스위트 I/O 경합 하 5s flaky 해소).

### Lint → 0 errors
- **`.eslintrc.json`** — `no-unused-vars`에 `ignoreRestSiblings` + `^_` ignore 패턴(의도적 omission `managePath` rest-sibling, `_summaryCurrencyTotals` 처리).
- dead code 제거: `%5Fdev/functions/page.tsx`(`copy`), `bookings/calendar/page.tsx`(`Metadata` import), `email-template-config.ts`(미사용 import), `availability.ts`(`resourceById`), `.../documents/[documentId]/email/route.ts`(미사용 `portal`+`locale`+import).
- `CanvasNode.tsx` — 미사용 `locale` 파라미터 2곳 `_locale`로 표기(아래 '권고' 참조).

### 개선/보완 (medium — 안전·확정적 quick wins)
- **`BookingServicesAdmin.tsx`** — i18n 와이어업: 정책 라벨 `cancellationPolicyLabel`(영어 하드코딩)→`c.policyLabel(policy)`, 툴바 수동 영어 조합→`c.toolbar(count)`(미사용 모듈 함수 제거). ko/zh 관리자에게 영어 노출 수정.
- **`MemberBookingsSearchForm.tsx`** — 'Clear search'가 query만 지우던 것을 dateFrom/dateTo도 함께 초기화.
- **`bookings/analytics.ts`** — `buildBookingTrendSeries`의 from/to에 `Number.isFinite` 가드. 잘못된 필터값이 `new Date(NaN).toISOString()` RangeError로 분석 번들 전체를 크래시하던 것 방지.

### 미수정 — 사유 기재
- **container-gallery zh-hant 테스트(잔여 실패 1건)** — `container/Inspector.tsx`는 `欄數`(grid 전용)와 flex 전용 라벨을 상호배타적으로 렌더하는데, 테스트가 `flex` 모드로 둘 다 기대 → **테스트 버그(절대 통과 불가)**. Codex의 신규 untracked 로컬라이제이션 테스트라 충돌 방지차 미수정. 수정안: 컨테이너를 grid/flex 두 번 렌더해 각 모드 라벨을 분리 검증.
- **medium/low 다수(색상 substring 버그, CMS cut-on-cancel, stale publish snapshot, refundAmountCents/partialRefundPercent 미표시 등)** — 실제 결함이나 다중 호출부 리팩터(tone 상태화 등)가 필요해 Codex 동시편집 중 광범위 변경 위험. 본 리포트에 상세·수정안 기재, 후속 처리 권장.
- **secrets-store flaky 테스트** — `lastRotatedAt` 회전이 같은 ms에 일어나면 `not.toBe` 실패. 미터치 영역의 선재 타이밍 결함(회전 시 직전과 다른 타임스탬프 보장 또는 단언 완화 권장).

### 권고 (후속)
- **#5 잔여:** 셀프서비스 이메일 변경은 현재 차단만 함. 정식 **이메일 소유권 확인(토큰) 플로우** 도입 후 재허용 권장.
- **CanvasNode `_locale`:** 리피터 image/button 노드 생성기가 `locale`을 받지만 미사용 — 의도된 미사용이면 그대로, 로컬라이즈 기본값(alt/label) 와이어업이 의도였다면 연결 필요(경미한 보완).
