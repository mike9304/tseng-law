# ACCEPTED-is — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `is-a.md` and `is-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1 fyrirfara → "fremur ólögmæta háttsemi" sweep 008 (table + body). a#2, a#3 non-words → the reviewer's rewrites. a#4 innrétting → "föst starfsstöð" for PE facility, "búnaður" for equipment — sweep 001/004. a#5 建築管理 → "stjórnunarreglur/skipulagsreglur" (not condo rules). a#6 負責人 → "ábyrgðarmaður (負責人)" with the gloss kept where the sister locales have it. a#7 agreement. a#8 008 anecdote → R3 wording.
- Non-word "mishlöð" → "eru mismunandi/breytileg" sweep 001–004; Danish "stemma við" → "samræmast/stemma" per reviewer; "Hið eina að" → "Það eitt að"; "mál" for language → "tungumál" sweep pack (ráðgjafarmál → "tungumál ráðgjafar"). English roles → R12.
- Blog fragments 003 Q16–20/005/006/008/009 → R7 (feminine narrator: lögmaður is unmarked; keep "ég" + name). Numbers: keep dot thousands (R14); "1.57M" pinned.
