# WO-I18N-COL012-EMBEDDINGS — Generated semantic-search synchronization

Date: 2026-07-26 KST
Manager: Codex `/root`
Unit: `012-taiwan-overtaking-accident-liability`

## Goal

Replace the stale committed and private Blob-backed semantic-search records
after the accepted KO, ZH-Hant, and EN column 012 rewrites. The generated store
must retain all 17 columns for each currently supported consultation locale,
with no skipped records, duplicate locale/slug keys, invalid vectors, stale
column 012 titles, or stale snippets.

Japanese is not silently added to this lane. The current consultation
generator supports `ko`, `zh-hant`, and `en`; Japanese consultation retrieval
requires a separate architecture decision.

## Owned files

- `src/content/column-embeddings.json` (generated by the canonical endpoint;
  never manually edited)
- `src/lib/__tests__/column-embeddings-content-sync.test.ts`

Do not edit the generator, consultation API route, locale definitions, article
Markdown, public copy, or unrelated tests in this lane.

## Exact column 012 records

Slug: `taiwan-overtaking-accident-liability`

- KO title: `대만 추월 사고의 책임은 어떻게 판단하나요?`
  - snippet anchors:
    - `앞차가 느리게 달리면 추월이 흔한 선택`
    - `상당한 위험이 따르는 운전 행위`
- ZH-Hant title: `台灣超車事故的責任如何判斷？`
  - snippet anchors:
    - `前方車輛行駛緩慢時，超車常被視為平常的選擇`
    - `這項操作本身伴隨相當風險`
- EN title: `Who Is Liable in an Overtaking Accident?`
  - snippet anchors:
    - `Overtaking can appear routine when a vehicle ahead is moving slowly`
    - `it creates substantial risk`

Reject these stale strings from the three column 012 title/snippet records:

- `추월 하다 사고나면 누구 책임???`
- `추월하다 사고 나면 누구 책임?`
- `추월 사고 책임 분석`
- `超車發生事故，究竟是誰的責任？`
- `超車事故責任如何判斷`
- `超車事故責任分析`
- `Overtaking Accident Liability`
- `대만 추월 규칙과 사고 발생 시 과실·책임 판단 기준을 정리했습니다.`
- `整理台灣超車規則與事故責任判斷實務。`
- `Practical standards for overtaking rules and fault allocation in Taiwan traffic accidents.`

Keep the existing column 010 and 007 synchronization and stale-copy
assertions intact.

## Regression-test contract

Extend the existing focused test without network or mutations. It must:

1. retain the version, model, `dim === 1536`, and valid ISO `builtAt` checks;
2. retain exactly 51 unique records and 17 records per supported locale;
3. retain exact locale/slug/title parity with `getAllColumnPosts(locale)`;
4. retain 1536 finite numbers for every vector;
5. assert the exact column 012 titles and snippet anchors above;
6. reject every stale column 012 string above;
7. retain all existing column 010 and 007 assertions.

## Canonical generation and rollback

Use only the local development endpoint:

```text
POST /api/consultation/build-embeddings
```

Before calling it:

1. require both `OPENAI_API_KEY` and `BLOB_READ_WRITE_TOKEN` without printing
   either value;
2. read the current private Blob at the exact pathname
   `consultation-embeddings/column-embeddings.json` with
   `get(..., { access: 'private', useCache: false })` and the current local
   generated file;
3. abort if either cannot be read;
4. save their exact bytes to mode-0600 `/tmp` backup files and record SHA-256,
   byte length, build timestamp, counts, and ordered locale/slug keys.

Validate the HTTP response separately. Accept only HTTP 200, `success: true`,
`total: 51`, `written: 51`, and `skipped: 0`. The endpoint response also
includes `outputPath`; it does not include payload-level `model` or `dim`.

Then parse the post-build local and Blob payloads and require version `1`,
model `text-embedding-3-small`, `dim: 1536`, a valid ISO `builtAt`, 17 records
in each locale, 51 unique locale/slug keys, exact current titles, exact column
012 title/snippet anchors, no stale strings, and a complete finite 1536-number
vector for every record.

After generation, read the private Blob again with no cache and require exact
byte-for-byte equality with the local generated file. Record the accepted
SHA-256 and byte length.

If the endpoint response is partial, either generated copy is invalid, the
local/Blob bytes differ, or any post-write check fails, restore both exact
pre-build copies:

```ts
const EMBEDDINGS_BLOB_PATH =
  'consultation-embeddings/column-embeddings.json';

await put(EMBEDDINGS_BLOB_PATH, blobBackupBytes, {
  access: 'private',
  allowOverwrite: true,
  contentType: 'application/json',
});
```

Write the local backup bytes back to
`src/content/column-embeddings.json`. Re-read both restored targets and compare
each byte-for-byte with its own backup before reporting failure. Never commit
a partial payload.

## Gates

1. focused embeddings synchronization test passes;
2. consultation knowledge-route and engine regressions pass;
3. TypeScript, scoped ESLint, and `git diff --check` pass;
4. independent read-only implementation review passes;
5. local generated file and private Blob checksum are identical;
6. clean production build remains green.

Do not push or deploy.
