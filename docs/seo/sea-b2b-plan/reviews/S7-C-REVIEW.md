# S7-C independent review — `7f585835` + `8fabaa0f`

Branch: `seo/sea-b2b-20260911`  
Worktree: `/Users/son7/Projects/tseng-law-sea-b2b-20260911`  
Range read: `git diff 3f0c5e1f..8fabaa0f` plus file reads. No build, no `next start`, no Playwright, no full suite.

## (A) Verdict

**PASS — 0 blocking issues.**

The two pages ship inside the language contract, ad-rule, YMYL-restatement, and translation-lane constraints. Remaining items are FIX/NOTE: a one-way language switcher, a weak hreflang pairing on debt-collection, native-copy polish, and tests that miss the actual page.tsx inLanguage wire.

## (B) Findings

| # | Sev | File:line | Quoted text | Problem | Proposed fix |
|---|-----|-----------|-------------|---------|--------------|
| 1 | FIX | `src/lib/public-guidance.ts:372–402` | `if (isGuidanceLocale4(targetLocale)) { const pageKey = guidancePageKeyFromSlugPath(slugPath); … return { href: guidancePublicPath(targetLocale, 'home'), fallback: 'home' }; }` | Hreflang says `/en/taiwan-company-setup-lawyer` ↔ `/vi/company-setup` (and the litigation pair) are alternates. The switcher implements extra→landing (`:412–417`) but the reverse is missing: `/en/taiwan-company-setup-lawyer` → `vi` resolves `pageKey` as null and drops the reader on `/vi`. Same for id/th/fil and for `/taiwan-litigation-lawyer`. Cluster is reciprocal in tags, not in the chrome a reader actually clicks. | In the `isGuidanceLocale4(targetLocale)` branch, before the home fallback, call `guidanceExtraPageKeyFromSiteCounterpartPath(pathWithoutLocale)` (already exported at `:486`) and return `guidancePublicPath(targetLocale, pageKey)` with `fallback: 'exact'`. Add the missing test: `resolvePublicLanguageSwitchTarget('/en/taiwan-company-setup-lawyer', 'vi')` → `/vi/company-setup`. |
| 2 | FIX | `src/data/international-guidance-extra.ts:499` | `การเลือกรูปแบบนิติบุคคล บริษัทย่อย สาขา หรือสำนักงานผู้แทน` | Thai heading runs the topic and the three options together with no colon/em-dash. Reads as a machine-stripped list, unlike the vi/id/fil siblings which keep `:`. Same pattern at `:522` `หลังการจดทะเบียน ธนาคาร ภาษีและบัญชี ถิ่นที่อยู่ เครื่องหมายการค้า และแรงงาน`. | `การเลือกรูปแบบนิติบุคคล: บริษัทย่อย สาขา หรือสำนักงานผู้แทน` / `หลังการจดทะเบียน: ธนาคาร ภาษีและบัญชี ถิ่นที่อยู่ เครื่องหมายการค้า และแรงงาน` |
| 3 | FIX | `src/data/international-guidance-extra.ts:555` | `ควรเลือกสาขาหรือบริษัทย่อย` | Incomplete FAQ question. Existing th FAQ (and this page’s last item) use `…หรือไม่`. Without it the register drops. | `ควรเลือกสาขาหรือบริษัทย่อยหรือไม่` |
| 4 | FIX | `src/data/international-guidance-extra.ts:333` | `Peninjauan awal dapat dimulai dari luar Taiwan melalui surel atau pertemuan video` | Register drift. Core id copy (`international-guidance-content.ts:849, 927, 974`) says `email`, never `surel`. Repeated at `:368`. | Replace `surel` with `email` in both sentences. |
| 5 | FIX | `src/data/international-guidance-extra.ts:732` | `Bersiyong Ingles para sa konsultasyon` | Awkward ligature (`bersiyon` + linker `-ng`). Same string at `:814` and in `guidanceExtraEnglishLandingLabel.fil` (`:957`). | `Bersyon sa Ingles para sa konsultasyon` |
| 6 | FIX | `src/data/international-guidance-extra.ts:698` | `Pagpili ng anyo ng entidad: subsidiary, sangay, o representative office` | Intra-phrase register mix: `sangay` is Filipino, `subsidiary` / `representative office` are left in English with no gloss. Core fil services glosses on first use (`work permit (permit sa trabaho, 工作許可)`, `trademark (marka ng kalakal, 商標)`). Same unglossed cluster in the answer block (`international-guidance-answers.ts:208`). | `Pagpili ng anyo ng entidad: subsidiary (anak na kompanya), sangay, o representative office (opisina ng kinatawan)` — and the same gloss in the answers.ts sentence. |
| 7 | FIX | `src/lib/__tests__/seo-faq-jsonld.test.ts:84–97` | `buildFaqJsonLd(…, 'vi'); expect(node!.inLanguage).toBe('vi')` | The live bug was `columns/[slug]/page.tsx` passing SiteLocale `en`. The test calls the helper with a hardcoded `'vi'` and never imports the page. Reverting the page.tsx argument to `locale` would still pass. Article JSON-LD is correctly on `urlLocale` (`page.tsx:245`) and is untested. | Assert the page module’s argument (or a thin wrapper): FAQPage and Article both receive `urlLocale`, not `locale`. Keep the helper case as a regression for `getLocaleLanguageTag('vi')`. |
| 8 | NOTE | `src/lib/public-guidance.ts:76–78` + `buildGuidanceCoreLanguageAlternates` `:452–462` | `'debt-collection': '/taiwan-litigation-lawyer'` | **Defensibility.** Company-setup ↔ `/taiwan-company-setup-lawyer` is the same question for the same reader, adapted not translated — that cluster is defensible, reciprocal, x-default=`/en/taiwan-company-setup-lawyer`, and no `/{ko,zh-hant,en,ja}/company-setup` URL is emitted. Debt-collection ↔ `/taiwan-litigation-lawyer` is only partly defensible: the EN/KO/ZH/JA landing is a general litigation hub (accident, criminal, family, divorce). Hreflang will tell Google that `/vi/debt-collection` is the Vietnamese edition of that hub, which it is not. Wiring is reciprocal and all eight URLs exist; the defect is semantic, not a 404. | Keep the cluster if the product decision is “B2B commercial dispute is the SEA slice of that landing.” Otherwise drop hreflang between the two and leave the EN path as a related `<a>` only (current body already names `/en/taiwan-litigation-lawyer`). |
| 9 | NOTE | `src/data/international-guidance-extra.ts:136` | `Trang này không công bố con số. Phạm vi công việc được xác định trước, sau đó mức phí và cách tính phí được xác nhận với quý vị trước khi công việc bắt đầu` | Same sentence in all 8 pages. It restates the locale **pricing/FAQ** pages (`international-guidance-content.ts:430` vi, `:903` id, `:1376` th, `:1849` fil), not `intentPages.en['taiwan-company-setup-lawyer']` / `['taiwan-litigation-lawyer']` or the services section. WO required a pricing pointer and forbade NT$ figures — both honoured. Strict YMYL source list in the review brief is the two named corpora only. | Leave as-is (already-published fee *process*, no amount). If the source list is applied letter-for-letter, cut the process clause and keep only `xem /{locale}/pricing`. |
| 10 | NOTE | `src/data/international-guidance-extra.ts:239` | `ảnh, video, chứng từ thanh toán, tài liệu của cơ quan công an … hồ sơ y tế hay sao kê ngân hàng` | Debt-collection first-email list copies the full EN litigation `prepareChecklist` (police, medical). Sourced, so not a new legal claim, but odd on a B2B invoice page. WO allowed the whole checklist. Parallel in id/th/fil. | Optional trim to contract / invoices / correspondence / POA — the items WO named for intent ⑤. |
| 11 | NOTE | `src/app/sitemap.ts:83–85` | `The extra keys' alternates stay inside the guidance four; that rule lives in buildGuidanceCoreLanguageAlternates, not here.` | Stale. R1 expanded extra keys to 8 locales + x-default=en. Runtime uses `buildGuidanceCoreLanguageAlternates`, which is correct. | Rewrite the comment to match R1. |
| 12 | NOTE | `src/data/__tests__/international-guidance-extra.test.ts:222–231` and `:254–268` | `FORBIDDEN_CLAIM_TOKENS` / `GUIDANCE_LANGUAGE_CONSULTATION_TOKENS` | Token greps are real contracts (verbatim FAQ, NT$, marker, column files on disk, 8-way reciprocity in `seo-hreflang-locales.test.ts` + `sitemap.test.ts`) mixed with tautologies (`getGuidancePage` identity, equal section counts, 140–160 description length). Gaps: no assertion that a proposition traces to EN intent or services; consultation greps are exact phrases only (`tư vấn bằng tiếng Việt`, not `tư vấn tiếng Việt`); `seo-faq-jsonld` does not lock page.tsx (finding 7). | Keep verbatim/reciprocity/fs checks. Add one source-trace fixture (a known services sentence must appear; the omitted “three months” EN FAQ must not). |

## (C) Checked and correct

- Translation-lane files `src/data/international-guidance-content.ts`, `src/data/intent-pages.ts`, `src/content/**`: `git diff 3f0c5e1f..8fabaa0f` is empty.
- Closing FAQ question+answer on all 8 pages is byte-identical to `guidanceContent[l].pages.faq.faqs` refusal pairs (vi `:408–410`, id `:881–883`, th `:1354–1356`, fil `:1827–1829`).
- Consultation-language statements on the new pages name only English/Chinese/Japanese/Korean; no offer of interpreting or replies in vi/id/th/fil outside that refusal Q&A.
- `buildGuidanceLegalServiceJsonLd` keeps `availableLanguage: ['en','zh-Hant','ja','ko']`; extra pages go through that helper (`GuidancePageBody.tsx:92–97`). `inLanguage` is the page language.
- No NT$ / grouped thousands / `[변호사 검수 필요]` / win-rate / guarantee / best-only-first-No.1 tokens in extra copy. EN “three months” and EN fee amounts were correctly not carried over.
- Company-setup body restates `intentPages.en['taiwan-company-setup-lawyer']` (entity choice, investment review, remittance, post-registration banking/tax/residence/trademark/labor) plus each locale’s services investment paragraphs, including the 居留 / 工作許可 sentence already published there.
- Debt-collection body restates EN litigation idealFor 5–6, processFlow, FAQ 1/3/4/5/7/8, plus each locale’s services civil paragraphs (contract / tort / consumer). Traffic, divorce, and criminal sections were not copied.
- Extra keys are absent from header/footer chrome (`public-site-chrome.ts` still keys off the original ten).
- `GUIDANCE_CORE_ROUTE_KEYS` includes both slugs; `resolveGuidanceMiddlewareRewrite` allows `/{vi,id,th,fil}/{company-setup,debt-collection}` without a `middleware.ts` edit; `/{ko,zh-hant,en,ja}/company-setup` is not invented.
- Sitemap: 4×2 new URLs, priority 0.8, 9-key alternates, reciprocal to the four site-locale landings (incl. ja entries at `sitemap.ts:346–352`); `reconcileGuidanceLanguageAlternates` cannot emit an unpublished URL; `/en/taiwan-lawyer` is not given vi/id/th/fil tags.
- Locale `llms.txt` catalogs all `GUIDANCE_ALL_PAGE_KEYS` (12) via `getGuidancePage`.
- Related-columns: 8 slugs, titles from `getAllColumnPosts(locale)`, missing files skipped; `debt-collection` list is `[]`. Sampled `columns-{vi,id,th,fil}` files exist for `taiwan-company-establishment-basics` and `taiwan-logistics-business-setup`.
- Column FAQPage inLanguage fix: `page.tsx:215` passes `urlLocale` (`guidanceLocale ?? locale`); Article JSON-LD at `:245` uses the same.
- vi/id copy is in the same formal business register as the existing guidance pages (quý vị / Anda+kantor; consultation-language sentence matches the published FAQ).
- Related-guides block and EN landing path strings are present on every extra page.

---

**File:** `/Users/son7/Projects/tseng-law-sea-b2b-20260911/docs/seo/sea-b2b-plan/reviews/S7-C-REVIEW.md`  
**Verdict:** PASS — 0 blocking issues
