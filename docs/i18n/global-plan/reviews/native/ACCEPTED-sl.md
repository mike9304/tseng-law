# ACCEPTED-sl — supervisor decisions (Fable 5.1, 2026-09-22, round 2)

Scope for the fixer: RULEBOOK-FIX.md (R2–R10 + round-2 addendum R12–R17) + every P1/P2 row of `sl-a.md` and `sl-b.md`, except as listed here. P3 rows: apply when cheap.

## Rows to SKIP in every locale
- Frontmatter `url:` with Hangul → skip (R16). `/ko/...` link rows → already remapped (R1); only make the label natural.
- Rows asking to reformat the pinned bio figure "TWD 1.57M" → skip (test-pinned). Other amounts: R14.
- Rows that would add statements about the law of the reader's country → R2 neutral generalization instead.
- Rows that would change block counts, image paths, external links, numbers or dates → rephrase inside the block instead.

## Locale-specific decisions (part a)
- a#1 uporaben → "ki se uporablja / veljaven" sweep (pack, disclaimer, privacy, 001–004, 007). a#2 matične evidence → "register prebivalstva / evidenca gospodinjstev (戶籍)" wording per reviewer, sweep 007 and pack. a#3–a#7 apply. a#8 Hangul url → skip. a#9 OOO → R15 ("[ime]").
- 本公司 head office → "matična družba / sedež družbe", never "glavna poslovna enota"; 假扣押 → začasni rubež; 自負額 → odbitna franšiza; 大法官 → ustavni sodniki Sodnega sveta (R8 gloss). Criminal penalty naming (kazen zapora) per reviewer.
- Numbers: keep dot thousands "TWD 30.000" (R14); decimal comma. Blog voice in 003/006/008/009 → R7 (feminine narrator stays).

## Part b
Apply every P1/P2 row of `sl-b.md` under the same skip rules. Additional: b#2 uporaben → part-a rule sweep 010/011/016/017. b#3 pričiti → „ki ga ni mogoče pripisati delavcu“ sweep 014. b#4 留任獎金 → „nagrada za ostanek/zadržanje“. b#7 代位繼承 → „vstopna pravica / dedovanje po vstopni pravici (代位繼承)“. b#8 喪失繼承權 → „izguba dedne pravice“. b#9 concluded abroad. b#10 戶籍 → part-a term sweep 016/017. b#11 okno → „pristojni organ / postopek“. b#12 delete the translator's aside (keep the block; replace with the English sentence's meaning). ter → in; sme → lahko; podlage → dokumentacija/evidence, sweep 010–018.
