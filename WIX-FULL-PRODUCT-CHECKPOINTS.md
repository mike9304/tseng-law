# WIX-FULL-PRODUCT-CHECKPOINTS.md

Created: 2026-05-13

Status legend:
- 🔴 not started
- 🟡 in progress / partially verified
- 🟢 automated verification + Codex review passed
- ⚫ blocked / explicitly deferred

Completion gate:
- Full Wix product layer requires 96+ of F01-F120 to be 🟢, unless explicitly waived by the user.
- This is required in addition to the existing W01-W225 visual editor layer.

Current M176 audit snapshot (2026-07-03):
- `npm run audit:release` parses F01-F120 as 120 green / 0 yellow / 0 red / 0 unknown; gate PASS (96 green required). Report: `runtime-data/audit/release-audit-2026-07-03.md`.
- `npm run audit:w-checkpoints` parses W01-W225 as 225 green / 0 yellow / 0 red / 0 unknown; gate PASS (203 green required). Report: `runtime-data/audit/w-checkpoint-audit-2026-07-03.md`.
- Local handoff gate on 2026-07-03 also passed `npm run qa`, a clean production `npm run build` after removing stale `.next-fix-build` type output from `tsconfig.json`, and `NEXT_DIST_DIR=.next-build npm run test:builder-smoke`.
- M176-A fixed audit parsing for W-layer tables where `상태` is not the last column, and treats legacy `✅` verified markers as green so old rows do not disappear into unknown status.
- M176-B added product proof for published CMS dynamic lists: editor-authored filters stay internal to data selection instead of leaking into visitor-facing `filter[...]` URLs, and flex-wrapped repeater height is reserved so visitor controls remain clickable below cards.
- M176-C added product proof for user-created CMS collection dynamic list pages: page creation accepts `dynamicListCmsCollectionId`, publishes custom collection records through repeater child bindings, and preserves safe visitor filter/sort/search/pagination at desktop/mobile widths.
- M176-D added product proof that user-created CMS collection fields drive visitor filter/sort whitelists and public sort controls, including custom `difficulty` sorting on a published dynamic list.
- M176-E extracted the published dynamic-list runtime, query-summary, and repeater height calculation out of `public-page.tsx` while preserving custom collection visitor filter/sort/pagination behavior under unit/build/Playwright/visual proof.
- M176-F added a visible Content Manager action for user-created CMS collections that creates a dynamic list page through the same `dynamicListCmsCollectionId` API path, opens the generated page in the editor, and preserves the published custom collection visitor query contract.
- M176-G added linked dynamic list page status back into the Content Manager collection detail, so admins can see, reopen, and visit custom collection list pages after creation while preserving the same published visitor query proof.
- M176-H fixed the CMS admin mobile/grid overflow found in M176-G visual proof: `/ko/admin-builder/cms` now stacks the Content Manager root at 390px, has a focused no-horizontal-overflow Playwright, and keeps the custom dynamic-list create/reopen/publish visitor query proof green.
- M176-I fixed the CMS collection detail mobile overflow below the root grid: filter/operator/value grids, record status/moderation action rows, record summaries, field grids, moderation, permissions, and CSV controls now have focused 390px no-horizontal-overflow proof while preserving dynamic-list regressions.
- M176-J closed the CMS reference picker mobile proof gap: a self-referencing collection now drives the real record editor -> Pick record modal at 390px, with bounding-box/no-overflow assertions and dialog-only screenshot proof while preserving CMS shell and custom dynamic-list regressions.
- M176-K closed the browser-visible CMS record slug redirect acknowledgement proof gap: the real record editor now has focused Playwright coverage for slug save -> `301 redirect created` status message -> persisted redirect -> public old-route 301 response.
- M176-L closed the first-time published page rollback artifact gap in the atomic publish pipeline: rollback now deletes newly created published canvas records when no prior published snapshot existed, with unit/local persistence coverage and a real `/api/builder/publish/atomic` -> public 404 Playwright proof.
- M176-M wired dynamic page CMS collection derivation into the atomic publish route: `deriveDynamicCollections` now resolves builder-authored dynamic list/item page metas, prefers custom `dynamicList.cmsCollectionId` over the built-in renderer target, publishes the derived CMS collection with the page, and has real route -> public dynamic-list Playwright proof.
- M176-N added product proof for user-created CMS collection dynamic item pages: the Content Manager can create a custom collection detail page, the editor renders the selected record through dataset bindings, `deriveDynamicCollections` publishes the referenced collection with the page, and the public `/ko/{pageSlug}/{recordSlug}` route resolves the custom record.
- M176-O added linked dynamic item page status back into the Content Manager collection detail, so admins can see, reopen, and visit custom collection item pages after creation while preserving the custom dynamic-list linked-page regression.
- M176-P added product proof for custom CMS dynamic item slug redirects: editing a user-created collection record slug now creates both the collection-base redirect and the linked custom item-page redirect, while built-in source redirects exclude custom CMS item page bases.
- M176-Q added product proof for arbitrary custom CMS dynamic item SEO: custom collection item pages now source title, description, canonical, social titles, and Article JSON-LD from the matched published custom record, while built-in columns dynamic item SEO remains green.
- M176-R added a guarded dynamic-template block publish coordinator: code-owned templates still publish without a site CMS draft collection, while site-owned matching CMS collections are published through `publishAtomic` before the block snapshot and rollback/failure prevents the template published snapshot write.
- M176-S added redirect delete persistence confirmation: redirect deletion now re-reads the persisted site document after the guarded write and returns failure instead of success if the deleted id survived storage, with unit and real HTTP API proof.
- M176-T added wildcard redirect overlap diagnostics: active broad/narrow `/*` source conflicts now return stable `wildcard-overlap` diagnostics through POST/PATCH, inactive drafts remain allowed until activation, and the redirect manager UI shows localized conflict copy with API and browser proof.
- M176-U added Content Manager route coverage review for custom CMS dynamic item pages: linked item page rows now show published record route counts, draft records held back, missing slug values, and sample public record links with unit/build/browser proof.
- M176-V added repair deep links for custom CMS dynamic item route blockers: linked item page rows now take admins directly to the first draft or missing-slug record in the CMS editor, with unit/build/browser proof.
- M176-W added the first linked custom CMS dynamic item batch lifecycle mutation: slug-ready held-back draft/pending/approved/rejected records can be published from the route coverage card through the guarded CMS bulk route, while archived and missing-slug records stay excluded, with unit/build/browser proof.
- M176-X added guarded missing-slug batch repair for linked custom CMS dynamic item pages: route-card-selected blank slug records can generate unique slugs through the CMS bulk route, then publish atomically and render as public record routes with unit/build/browser proof.
- M176-Y added guarded duplicate slug conflict repair for linked custom CMS dynamic item pages: route coverage now excludes duplicate records from route-ready counts, exposes conflict repair from the same card, and proves repaired duplicate record routes after atomic publish.
- M176-Z added first custom slug source-field UX for linked custom CMS dynamic item pages: route-card generate/repair actions can choose a text/rich-text source field, the guarded bulk route validates `sourceFieldKey`, and browser proof shows a Code-sourced generated slug publishing to a public custom item route.
- M176-AA added custom slug pattern composition for linked custom CMS dynamic item pages: route-card generate/repair actions accept `{{fieldKey}}` patterns such as `{{code}}-{{title}}`, the guarded bulk route validates `slugPattern`, and browser proof shows the composed route publishing to a public custom item page.
- M176-AB added route-card slug pattern presets and live preview: field-token buttons compose patterns without duplicate tokens, preview resolves against the first affected custom CMS record, and browser proof shows preset-built `{{code}}-{{title}}` routes publishing publicly.
- M176-AC added the first explicit duplicate-slug conflict rule for linked custom CMS dynamic item pages: route-card repair can choose `Append record ID`, the guarded bulk route validates `slugConflictRule`, and browser proof shows the repaired record-id-suffixed route publishing publicly.
- M176-AD added route-card archive-held-back lifecycle handling for linked custom CMS dynamic item pages: held-back draft/pending records can be archived through the guarded bulk route, archived records are counted separately, and browser proof shows the archived custom item URL returning 404 after atomic publish.
- M176-AE added route-card restore-archived lifecycle handling for linked custom CMS dynamic item pages: archived custom item records can be restored to draft through the guarded bulk route, then published and atomically rendered again as public custom record routes.
- M176-AF added route-card delete-archived lifecycle handling for linked custom CMS dynamic item pages: archived custom item records can be permanently deleted through the guarded bulk route, coverage drops the archived count, and browser proof shows the deleted archived slug remains 404 after atomic publish while restored records can still republish.
- M176-AG extracted the linked custom CMS dynamic item route-card action surface out of the summary row, preserving delete/archive/restore/publish browser behavior while moving `CmsDynamicItemLinkedPageRow.tsx` out of the 200-250 pure LOC warning band.
- M176-AH added the first CMS bulk lifecycle audit trail: delete/generate/repair/status mutations now write typed `cms.records.bulk_lifecycle` events, `/api/builder/site/audit` can filter them by collection/record, and the custom dynamic item lifecycle Playwright proves those events after route-card mutations.
- M176-AI connected CMS lifecycle audit events to the Ops logs surface: `/admin-builder/ops?tab=logs&type=audit&q={collectionId}` opens directly to readable CMS lifecycle summaries, structured collection/action/status/record detail chips, detail-aware search/export, and mobile-safe row layout with browser/visual proof.
- M176-AJ promotes those CMS lifecycle audit events into the Ops overview dashboard: snapshot/export payloads now include total requested/changed records, action breakdowns, top collections, recent lifecycle rows, and collection-specific log deep-links with desktop/mobile browser proof.
- M176-AK adds non-destructive linked custom item route-card lifecycle policy presets for prepare-public-routes, quarantine-held-back, and recover-archived, with browser proof that one Prepare click repairs missing/duplicate slugs, publishes held-back records, atomically publishes the item page, and renders the public draft route.
- M176-AL adds linked custom item route-card trash/restore-from-delete: delete archived moves records into a recoverable `trashedRecords` bucket, Restore deleted returns the snapshot as archived, and browser proof covers the same route-card surface plus the existing public 404 lifecycle regression.
- M176-AM preserves CMS `trashedRecords` across ordinary editable CMS mutations by sharing trash normalization with `normalizeCmsCollections()` and returning trash on collection detail reads; unit proof drives trash -> record update -> trash/detail read.
- M176-AN turns Prepare public routes into an authored route-card policy: admins can set slug source, `{{field}}` pattern, and duplicate conflict rule once on the lifecycle toolbar; browser proof applies Code + `{{code}}-{{title}}` + Append record ID, publishes, and renders the generated/repaired custom item routes.
- M176-AO persists those authored Prepare policy options: the route-card saves source/pattern/conflict-rule through a guarded policy API, collection detail reload hydrates the same values, and browser proof continues through Prepare, atomic publish, and public custom item routes.
- M176-AP makes saved Prepare policy state visible: route cards now show who last saved the policy and when, immediately after save and after collection detail reload, while preserving the same Prepare/publish/public-route browser proof.
- M176-AQ keeps saved Prepare policy management visible after a linked custom item route is already clean: route cards with no active lifecycle preset still render `Lifecycle policy`, hydrated source/pattern/conflict-rule controls, saved metadata, and a working save action with desktop/mobile browser proof.
- M176-AR adds a named route-card policy foothold: saved Prepare policies now store and reload a `Policy name` alongside source/pattern/conflict-rule, with guarded API, unit, browser, and desktop/mobile visual proof.
- M176-AS adds reusable named route-card policy selection: clean linked custom item route cards can apply another linked item page's saved `Policy name` + source/pattern/conflict-rule template and persist it as the current page policy through the guarded policy API, with unit, browser, and desktop/mobile visual proof.
- M176-AT adds collection-level named route policy rollout: one linked custom item page's saved policy can be applied to every clean linked item page without an existing saved policy through the same guarded policy API, with unit, browser, and desktop/mobile visual proof.
- M176-AU adds collection-level saved route policy library management: linked item page policies are listed with source page, source/pattern/conflict summary, exact-template usage count, and a guarded remove action with unit, browser, and desktop/mobile visual proof.
- M176-AV adds scheduled `Prepare public routes` automation for linked custom CMS dynamic item pages: route-card policy options can be scheduled from the editor, persisted as due jobs, run through a cron-authorized route, and applied to missing/duplicate/held-back records with unit, browser, and public-route proof.
- M176-AW adds selected repeater canvas HUD loading/no-match skeleton reserve: the extracted `RepeaterTemplateCanvasHud` shows three skeleton cards, hides field chips, disables template actions while records resolve, and preserves existing selected-record/empty dataset behavior with unit, build, and browser proof.
- M176-AX adds exact bound-child selection from selected repeater HUD field chips: each field summary chip now selects its matching template child node, preserving inherited parent-record binding context with unit, build, and browser proof.
- M176-AY adds the selected repeater template child return path: the child context badge is now an actionable parent-repeater selector, proving HUD chip drill-down and badge return in one browser round trip.
- M176-AZ adds selected repeater template child sibling navigation: the selected child badge now includes a sibling field rail with active bound-child markers, proving parent HUD chip drill-down, direct sibling child switching, inherited binding context, and parent HUD return in one browser round trip.

## M157 Benchmark And Scoring

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F01 | Benchmark | Official Wix source manifest | `WIX-FULL-PRODUCT-GAP.md` lists official Wix/Wix Studio source URLs and date checked | 🟢 |
| F02 | Benchmark | Two-layer completion model | Goal requires both W-layer and F-layer gates | 🟢 |
| F03 | Benchmark | Current product gap inventory | Gap map covers CMS, apps, commerce, bookings, AI, collaboration, dev, multilingual, enterprise/ops | 🟢 |
| F04 | Benchmark | Priority milestone map | `WIX-PARITY-PLAN.md` includes M157-M176 with dependencies | 🟢 |
| F05 | Benchmark | Implementation manual extension | `WIX-PARITY-IMPLEMENT.md` defines M157-M176 | 🟢 |
| F06 | Benchmark | Master prompt gate update | `WIX-PARITY-PROMPT.md` and `CODEX-GOAL-WIX-FULL-BUILDER.md` prohibit completion on W01-W225 alone | 🟢 |

## M158 CMS Foundations

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F07 | CMS | Collection schema model | Collections can define id, name, fields, permissions, timestamps, and indexes | 🟢 |
| F08 | CMS | Content manager UI | Admin can create, edit, duplicate, delete, search, and sort collection rows | 🟢 M176-I adds 390px browser proof that the collection detail surface itself stays within the mobile viewport while exposing record cards, edit controls, moderation, permissions, and CSV controls; M176-J adds the same focused mobile proof for the record editor's reference picker modal. |
| F09 | CMS | Typed field coverage | Text, rich text, number, boolean, date, media, reference, tags, URL, email fields render and persist | 🟢 M176-J adds browser proof that reference fields open the real record picker dialog from a self-referencing collection at 390px, preserve the selected record state, and expose selectable reference rows without horizontal clipping. |
| F10 | CMS | Field validation/defaults | Required, unique, default values, min/max, regex, and help text are enforced | 🟢 |
| F11 | CMS | CSV import/export | Collection rows import/export with validation summary and rollback on failure | 🟢 |
| F12 | CMS | Collection permissions | Read/write/admin permissions are enforced for public, member, staff, and admin actors | 🟢 |
| F13 | CMS | Content revisions | Row history, restore, and author/time metadata are available | 🟢 |
| F14 | CMS | Media field integration | CMS media fields reuse the asset library and store alt text/focal metadata | 🟢 |
| F15 | CMS | Search/filter/sort | Content manager supports typed filters, saved views, and stable pagination | 🟢 M176-I adds mobile browser proof that the detail filter field/operator/value controls and status/moderation filter rows wrap within 390px instead of widening the page; M176-J proves the reference picker search box and filtered record rows stay within the same mobile viewport. |
| F16 | CMS | Guarded CMS APIs | Internal APIs validate schema, permissions, and typed payloads | 🟢 |

## M159 Dynamic Content

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F17 | Dynamic | Dataset config | Editor can attach a dataset to a collection with mode, filters, sort, and item limit | 🟢 |
| F18 | Dynamic | Element field binding | Text, image, link, button, gallery, and repeater elements bind to dataset fields | 🟢 Visual repeater authoring now has 3-up multi-record comparison preview (`RepeaterMultiRecordPreview.tsx`) with active-card switching + keyboard nav. Gallery/image/button/text/heading/container repeater bindings continue to flow through `applyBuilderDatasetBindingToNode`, and the repeater HUD now inserts bound text/image/button/gallery template children, offers a first-bound-child duplicate/reuse action, lets field summary chips jump directly to the exact bound template child, lets selected child badges return to the parent repeater, and lets selected child sibling field chips switch directly between bound template children with draft-save and published child-template gallery coverage. 2026-07-03 Codex reverified the prior inspector/stale-mapping blocker: dataset binding units, multi-record preview, child-badge, and transient-store tests passed 50/50, and Chromium `dataset-binding.playwright.ts` passed save/reload binding metadata, stale-field publish repair, and selected-record inspector/canvas preview flows against `http://127.0.0.1:3191`. |
| F19 | Dynamic | Repeater component | Repeater renders collection rows with template editing and empty/loading states | 🟢 Multi-record preview now appears in the repeater inspector with active-card switching, empty-state messaging, an explicit loading skeleton, direct canvas HUD insertion for bound text/image/button/gallery children, a reusable template-child duplication path, selected canvas HUD loading/no-match skeleton reserve with disabled template actions while records resolve, field-chip exact child selection from the selected canvas HUD, an actionable selected-child badge that returns to the parent repeater HUD, and a selected-child sibling field rail for direct bound-child switching. 2026-07-02 Claude loop iter19: repeater skeleton/HUD칩 라운드트립/자식그룹 복제 playwright 3종 + RepeaterMultiRecordPreview 등 unit 6종 green (실패원인이던 CMS 정크 레코드 정리 후 재실행). 검증 완료로 🟢 전환. |
| F20 | Dynamic | Dynamic list pages | Admin can create list pages backed by a collection and dataset filters | 🟢 Visitor-supplied filter/sort/pagination composition lands in `datasets-visitor-filters.ts` — `composeVisitorDatasetPatch` whitelists fields, clamps to 120 chars + 6 filters + 3 sorts, refuses unsafe field identifiers, never widens editor-authored limit. `parseVisitorPaginationParams` + `sliceVisitorRecords` give the renderer a normalized `{page, perPage, offset}`. Dataset preview now prefers published `site.cmsCollections` records when available, so the inspector mirrors live CMS records instead of static source fixtures. Pages panel quick-create now has browser proof for columns/service/attorney dynamic list creation, draft binding, and publish smoke coverage. M176-B adds red-first proof that authored list filters are applied to the published record set without being copied into visitor sort/search/pagination URLs, and reserves expanded repeater height for visitor controls. M176-C extends page creation to user-created CMS collections via `dynamicListCmsCollectionId`, preserving `cmsCollectionId` in the dataset binding and proving public custom records render through repeater child bindings after publish. M176-D resolves visitor field metadata from the actual user collection schema, so custom safe fields such as `difficulty` can drive public sort/filter whitelists and controls. M176-E moves the published dynamic-list runtime/query-summary/layout calculation into focused modules while preserving the same public behavior under regression proof. M176-F adds a Content Manager collection-detail action that creates a custom collection dynamic list page from the visible CMS UI and opens the generated editor page. M176-G lists those linked custom dynamic list pages back on the collection detail with editor reopen and public route links. M176-H adds a 390px CMS admin no-overflow regression and stacks the Content Manager root grid on mobile while keeping the create/reopen/publish custom-list flow green. M176-I extends the mobile proof into the selected collection detail, including record filter/action rows and record grid summaries. M176-J preserves that custom-list regression while adding reference picker modal mobile proof on the same CMS editor route. M176-M proves the atomic publish route can derive the custom CMS collection id from a dynamic list page and publish the draft record with the page without caller-supplied `cmsCollectionIds`. 2026-07-02 Claude loop iter19: dynamic-list-pages 전체(quick-create/publish/필터보존/정렬) + custom-collection + authored-filter-sort + atomic-publish + visitor-filters unit green. 검증 완료로 🟢 전환. |
| F21 | Dynamic | Dynamic item routing | Item pages resolve by slug/id and render the correct record | 🟢 Multi-record preview helps editors confirm slug→record mapping before publish; visitor filter/sort layer shares the same whitelist as dynamic item bindings. Service/lawyer source-backed dynamic item pages now track guarded source record edits after publish, with browser proof that source public routes and published dynamic item canvases reload to the same updated record values. Pages panel quick-create now has browser proof for columns/service/lawyer dynamic-item creation, draft binding, and publish smoke coverage. M176-N extends item routing to user-created CMS collections from the Content Manager action through editor render, atomic publish derivation, and public record-slug route resolution. M176-O lists those linked custom dynamic item pages back on the collection detail with editor reopen and public record-route links. M176-P proves the old custom item URL redirects after the underlying record slug changes. M176-U adds Content Manager route coverage review for linked custom item pages, showing published route count, draft records held back, missing slug values, and sample public record links. M176-V adds first-record repair deep links from those blockers back into the CMS editor for draft and missing-slug records. M176-W adds a guarded batch publish action for slug-ready held-back item records and proves the resulting public record route after atomic publish. M176-X adds guarded missing-slug batch generation from the same route card and proves the generated-slug record route after atomic publish. M176-Y adds guarded duplicate slug conflict repair from that card and proves repaired duplicate record routes after atomic publish. M176-Z adds first custom slug source-field UX for generate/repair actions and proves a Code-sourced generated route after atomic publish. M176-AA adds route-card `{{fieldKey}}` slug pattern composition and proves a `{{code}}-{{title}}` generated route after atomic publish. M176-AB adds field-token presets plus live preview for those patterns and proves preset-built composed routes after atomic publish. M176-AC adds a route-card duplicate-conflict rule select and proves `record-id-suffix` repaired routes after atomic publish. M176-AD adds route-card archive-held-back lifecycle action, archived count separation, and public 404 proof for archived custom item routes after atomic publish. M176-AE adds route-card restore of archived custom item records back to draft, then proves republish through guarded bulk publish plus atomic public route render. M176-AF adds route-card delete of archived custom item records and proves the deleted slug remains 404 after atomic publish. M176-AG extracts the route-card action surface into a focused component while preserving the full delete/archive/restore/publish browser flow. M176-AH adds typed CMS bulk lifecycle audit events and collection/record audit API filters, proved by the same route-card lifecycle browser flow. M176-AI adds Ops logs drilldown/search/export for those lifecycle events with readable summaries, structured details, direct query links, and mobile-safe browser/visual proof. M176-AJ adds an Ops overview CMS lifecycle dashboard summary with action totals, top collections, recent events, JSON export inclusion, and collection log deep-links with desktop/mobile browser proof. M176-AK adds non-destructive route-card lifecycle policy presets for prepare-public-routes, quarantine-held-back, and recover-archived, with browser proof that one Prepare click repairs missing/duplicate slugs, publishes held-back records, atomically publishes the item page, and renders the public draft route. M176-AL adds route-card trash/restore-from-delete: delete archived now moves records into a recoverable trash bucket, Restore deleted returns the snapshot as archived, and browser proof covers the same linked custom item route-card surface plus the existing public 404 lifecycle regression. M176-AM preserves `trashedRecords` across ordinary editable CMS mutations by carrying the trash bucket through `normalizeCmsCollections()` and collection detail reads, with unit proof for trash -> record update -> trash/detail read. M176-AN lets admins author the Prepare policy with slug source, `{{field}}` pattern, and duplicate conflict rule on the route-card toolbar; browser proof renders the resulting generated and repaired custom item routes. M176-AO persists those authored Prepare policy options with a guarded route policy API, collection detail reload hydration, and the same Prepare -> atomic publish -> public route proof. M176-AP shows saved policy metadata in the route card after save and reload, preserving the same Prepare/public-route proof. M176-AQ keeps that saved policy manageable after routes are clean by rendering the policy controls and save action even when there are no active lifecycle presets. M176-AR adds a stored/reloaded policy name to that route-card policy surface. M176-AS lets a clean linked item route-card apply another linked page's named saved policy template and persist it as the current page policy. M176-AT adds collection-level rollout from one named saved policy to all clean linked item pages without saved policies. M176-AU adds collection-level saved policy library inspection, usage counts, and guarded per-page policy removal. M176-AV adds scheduled Prepare public routes jobs with cron execution proof for missing/duplicate/held-back records. 2026-07-03 Codex reverified F21 against http://127.0.0.1:3191: dynamic item unit suite passed 14/14, focused columns/service quick-create rerun passed 2/2, full Chromium dynamic item browser surface passed 12/12, ESLint on the touched Playwright file passed, and `npm run typecheck` passed. |
| F22 | Dynamic | URL slug fields | Slug field generation, conflict handling, redirect creation, redirect conflict warnings, and CMS record-slug redirects work | 🟢 CMS record-slug redirect lifecycle ships in `dynamic-record-redirect-lifecycle.ts`. `computeRecordSlugRedirectInput(s)` handles locale-prefixed 301 rules for built-in collection bases and matching builder-authored dynamic item page bases, while `applyRecordSlugRedirect` deactivates preexisting `from === new-path` rules to avoid chain rejection. Redirect resolution now shares a single exact-first / longest-prefix-wildcard matcher between the site helper and edge middleware, and the redirect manager UI now advertises trailing `/*` wildcard authoring with browser proof that authored wildcard rules preserve nested suffixes and a shareable search/filter surface for large redirect tables. M176-T adds active wildcard-overlap diagnostics: broad/narrow `/*` source conflicts produce a stable `wildcard-overlap` API payload, inactive drafts can be saved, activation is blocked when coverage would overlap, and the UI shows localized conflict copy. Atomic CMS+page publish coordination ships in `dynamic-template-publish-coordinator.ts` (`publishDynamicTemplate` dedupes referenced collection ids and delegates to `publishAtomic`). M176-K adds browser-visible record editor proof for a user CMS record slug change: the save shows `301 redirect created`, persists the redirect, and the old public record route returns a 301 to the new route. M176-P extends that proof to custom CMS dynamic item pages, including UI helper copy, persisted collection-base and item-page redirects, and a public old item URL 301 response. M176-S closes the redirect delete success-masking gap by re-reading persisted state after delete and failing if the removed id remains. |
| F23 | Dynamic | Per-item SEO | Dynamic pages can generate title, description, canonical, OG image, and schema from fields | 🟢 Published CMS dynamic item pages now override page-template SEO with per-record fields. Built-in columns/services/lawyers keep the curated `findPublishedCmsCollectionRecordSeo()` / `findBuilderCollectionRecordSeo()` and `buildBuilderRecordJsonLd()` paths for title, description, canonical, OG/Twitter title/description/image, noIndex, and schema.org payloads. M176-Q extends the same page metadata/JSON-LD path to arbitrary custom CMS item pages via `dynamicItem.cmsCollectionId`, matching the published record by slug field and deriving title/description/image/date/author/noIndex/keywords from custom fields while canonicalizing to `/ko/{pageSlug}/{recordSlug}`. Unit coverage now includes `published-dynamic-item-seo.test.ts`; Playwright coverage includes both `dynamic-item-pages-seo.playwright.ts` for built-in records and `custom-dynamic-item-pages-seo.playwright.ts` for two custom record slugs with distinct title/description/canonical/Article JSON-LD. |
| F24 | Dynamic | Visitor filters/search | Public pages can expose safe search, filters, and sort controls for dataset content | 🟢 First slice on `/[locale]/columns`: public search input updates `?q=` URL via router.replace, ColumnsGrid filters posts client-side against title/summary/category/author/tag, shows result count, and exposes a clear-search affordance. Category chips already honor URL params. Playwright `columns-visitor-search.playwright.ts` proves search/clear round-trip. CMS-backed service/lawyer dynamic lists now also honor visitor `filter[...]`, `q=`, and `sort` query state in the runtime repeater before pagination; published CMS previews now expose sanitized `fieldValues`, so service `filter[description]` can match subtitle/intro-backed CMS records without title-field leakage. M176-B proves sort/search controls only serialize visitor state: authored filters continue filtering records but no longer appear as removable visitor filter chips or `filter[...]` query params. M176-C proves the same visitor controls can filter/search/sort user-created CMS collection records through custom runtime `fieldValues`. M176-D proves arbitrary user collection fields of safe types now populate public sort controls and query whitelists, with `difficulty` sorting verified end to end. M176-E keeps the same visitor query contract after extracting runtime state and active-summary href generation from `public-page.tsx`. M176-F proves the Content Manager-created custom list page keeps the same public search/filter/sort controls after publish. M176-G preserves that public search/filter/sort contract while adding the CMS linked-page reopen surface. M176-H reruns the custom collection dynamic-list E2E after the CMS mobile grid fix, preserving the public search/filter/sort contract. M176-I preserves the same E2E after deeper CMS detail filter/action mobile wrapping. M176-J reruns it after reference picker proof, keeping the public visitor query contract intact. 2026-07-02 Claude loop iter18: columns-visitor-search.playwright(노트 직접 명명) pass + datasets-visitor-filters unit green. 검증 완료로 🟢 전환. |
| F25 | Dynamic | Pagination/load more | Dataset pagination and load-more behavior preserve query state | 🟢 Public columns load-more now stores the expanded slice in `?page=`, survives reload/back navigation, clears `page` when search changes, and has visual coverage for the expanded state. Published CMS-backed service and attorney list pages prove query-preserving `Previous`/`Next` pagination links, and M176-B adds browser/visual proof that the query controls remain below expanded repeater content and clickable at desktop/mobile widths. M176-C adds browser/visual proof for user-created CMS dynamic list pagination preserving `filter[...]`, `sort`, `perPage`, and search state at desktop/mobile widths. M176-D keeps custom field sort state such as `sort=difficulty:asc` active across the same pagination/search controls without horizontal overflow. M176-E preserves query state and repeater height behavior after moving pagination slicing/layout calculation into focused published-runtime helpers. M176-F keeps the same pagination/query behavior through the new Content Manager page-creation path. M176-G keeps the same pagination/query behavior after reopening the generated page from the linked CMS row. M176-H reruns the same custom dynamic-list browser flow after the CMS mobile fit fix, preserving query pagination behavior. M176-I reruns the same flow after deeper CMS detail mobile wrapping, preserving pagination/query behavior. M176-J reruns the same flow after reference picker proof, preserving pagination/query behavior. 2026-07-02 Claude loop iter18: columns-load-more.playwright pass + published-dynamic-list-runtime unit green. 검증 완료로 🟢 전환. |
| F26 | Dynamic | Preview/publish pipeline | Draft CMS changes preview correctly and publish atomically with page changes | 🟢 Atomic publish transaction with rollback ships in M167-WX3: `beginPublishTransaction` snapshots site doc + per-page published records, `publishAtomic` orchestrator runs page+CMS publishes sequentially and rolls back on any failure, POST `/api/builder/publish/atomic` exposes the workflow. The publish dialog now loads active scheduled jobs and supports canceling them from the same surface, the cron route has browser proof that a due job is actually published and cleared, the atomic publish HTTP wrapper has route-level auth/validation/success coverage, the browser flow proves a real page + CMS record can commit together and show up on the public route, rollback revalidates restored public page paths, M176-L deletes newly created first-time published canvas artifacts when a later atomic step fails, proving the public route remains 404 after rollback, M176-M adds `deriveDynamicCollections` so builder-authored dynamic pages can publish their referenced custom CMS collection atomically without duplicating collection ids in the caller, M176-N proves the same derivation path for custom CMS dynamic item pages, and M176-R guards the separate dynamic-template block publish endpoint so a matching site-owned CMS collection must atomically publish before the template published snapshot is written, while code-owned source templates continue to publish without synthetic CMS records. |

## M160 Visitor Input To CMS

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F27 | Input | Form-to-collection mapping | Form submissions can write to a selected collection with mapped fields | 🟢 |
| F28 | Input | Input field binding | Text, email, phone, checkbox, radio, select, date, upload, and consent fields bind to CMS fields | 🟢 |
| F29 | Input | Submit validation | Client and server validation return clear field-level errors | 🟢 |
| F30 | Input | Moderation queue | Visitor-created rows can enter pending/approved/rejected states | 🟢 |
| F31 | Input | Spam/rate controls | Honeypot, rate limit, and duplicate submission guards are enforced | 🟢 |
| F32 | Input | Visitor upload fields | Public uploads use asset validation, size limits, scan hooks, and CMS media references | 🟢 |

## M161 App Market Architecture

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F33 | Apps | App manifest schema | Apps declare metadata, permissions, widgets, settings panels, routes, migrations, and translations | 🟢 |
| F34 | Apps | App discovery/catalog | Admin can browse/search/filter local app catalog entries | 🟢 |
| F35 | Apps | Lifecycle controls | Apps install, enable, disable, upgrade, and uninstall without orphaned state | 🟢 |
| F36 | Apps | App settings UI | Installed apps expose settings panels with validation and save/restore behavior | 🟢 |
| F37 | Apps | Widget registration | Apps can register editor widgets/components into the add panel | 🟢 |
| F38 | Apps | Public runtime loader | Published pages load app widgets with scoped data and no global collisions | 🟢 |
| F39 | Apps | App migrations | Versioned app data migrations run and report status | 🟢 |
| F40 | Apps | App permissions/scopes | Apps request and enforce scoped access to CMS, media, checkout, bookings, and members data | 🟢 |
| F41 | Apps | App version model | Installed app version, available update, compatibility, and rollback are tracked | 🟢 |
| F42 | Apps | Uninstall cleanup | App data removal is explicit, reversible when possible, and audited | 🟢 |

## M162 Native App Packs

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F43 | Native apps | Blog data model/admin | Blog posts, authors, categories, tags, drafts, and scheduling exist | 🟢 |
| F44 | Native apps | Blog public widgets | Blog list, post, category, author, recent posts, and search widgets publish correctly | 🟢 |
| F45 | Native apps | Events app | Events admin, RSVP/ticket basics, event pages, and calendar/list widgets exist | 🟢 |
| F46 | Native apps | Members area | Member profile, login gating, account pages, and role-aware navigation exist | 🟢 |
| F47 | Native apps | FAQ app | FAQ categories, public widgets, schema output, and search/filter are app-backed | 🟢 |
| F48 | Native apps | Chat app | Chat inbox/settings widget and public launcher are app-backed | 🟢 |
| F49 | Native apps | Portfolio app | Portfolio projects, galleries, categories, and project detail pages exist | 🟢 |
| F50 | Native apps | Site search app | Indexable content model, search results page, and widget configuration exist | 🟢 |
| F51 | Native apps | App translation hooks | First-party apps participate in the multilingual manager | 🟢 |
| F52 | Native apps | Unified app dashboard | Installed native apps appear in a single manage/update/settings dashboard | 🟢 |

## M163 Stores And eCommerce

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F53 | Stores | Product schema | Products support title, description, media, price, inventory, SKU, SEO, variants, and status | 🟢 |
| F54 | Stores | Product manager | Admin can create, duplicate, bulk edit, import/export, archive, and search products; the localized ko/zh-hant shell no longer leaks English chrome | 🟢 |
| F55 | Stores | Variants/options | Product options, variant prices, inventory, images, and availability render correctly | 🟢 |
| F56 | Stores | Categories/collections | Product categories drive navigation, galleries, and dynamic URLs | 🟢 |
| F57 | Stores | Product gallery widgets | Store galleries support filters, sort, pagination, quick view, and responsive layout | 🟢 |
| F58 | Stores | Product detail page | PDP supports gallery, variants, quantity, availability, related products, and SEO | 🟢 |
| F59 | Stores | Cart | Cart add/update/remove, coupon entry, totals, persisted state, and mini-cart work | 🟢 |
| F60 | Stores | Checkout adapter | Checkout flow supports address, shipping/tax/payment adapter, and order confirmation | 🟢 |
| F61 | Stores | Order creation | Orders persist line items, customer, payment state, fulfillment state, totals, and audit data | 🟢 |
| F62 | Stores | Order admin | Admin can view, search, filter, update fulfillment/payment state, and export orders | 🟢 |
| F63 | Stores | Discounts/coupons | Coupon and automatic discount rules apply safely to cart and checkout | 🟢 |
| F64 | Stores | Tax rules | Tax calculation rules are configurable and visible in checkout/order admin | 🟢 |
| F65 | Stores | Shipping/delivery | Shipping zones, rates, pickup/local delivery, and free shipping rules are supported | 🟢 |
| F66 | Stores | Abandoned cart/notifications | Cart recovery and order notification hooks exist | 🟢 |

## M164 Payments And Business Operations

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F67 | Payments | Provider abstraction | Payment providers expose test-mode intents, captures, failures, and status mapping | 🟢 |
| F68 | Payments | Webhooks | Provider webhook verification, idempotency, and event replay are implemented | 🟢 |
| F69 | Payments | Refunds | Refunds and partial refunds update payment, order, and audit states | 🟢 Internal/manual refunds, Stripe refund execution for non-stub provider payments, provider-failure no-mutation guard, booking cancellation Stripe refunds, refund locks, audit, filters, and CSV visibility are verified |
| F70 | Ops | Invoices/receipts | Orders/bookings can generate receipt/invoice data and email/export it | 🟢 2026-07-03 Codex: Orders and bookings can issue invoice/receipt records, preserve payment/currency snapshots, enforce paid-only receipt guards, email-stub issued documents, list/search/export them centrally, download HTML/PDF, create/revoke tokenized share links, render no-store public share/pay views, track viewed/downloaded access, and expose document actions in admin and member portal flows. Automatic issuance policy covers order/booking create and paid transitions without duplicate backfill; the shared `INV/RCT-YYYY-000001` numbering ledger keeps auditable reservations with process/file-lock local concurrency; lifecycle supports void/supersede; bulk issue/void/CSV and billing templates are covered. Unpaid order/booking invoice payment links support create/renew/revoke, manual payment reconciliation, stale-link invalidation, Stripe Checkout hosted session creation with SCA return/cancel URLs, signed hosted webhook settlement, idempotent receipt automation, webhook ledger/list/replay, and sanitized public invalid/expired/revoked responses. Verification: billing document model/bulk/copy/booking-documents/routes/settings/templates/webhooks/Stripe webhook Vitest 78/78, Chromium `billing-documents.playwright.ts` policy + booking invoice payment/manual-payment flows 2/2, Chromium `billing-templates.playwright.ts` 2/2, and Chromium `members-area.playwright.ts` member document portal flow 1/1 on `http://127.0.0.1:3191`. Distributed Blob CAS and live provider/legal-template QA remain production hardening beyond the local F70 invoice/receipt contract. |
| F71 | Payments | Manual payments | Offline/manual payment methods are represented consistently | ✅ Order, booking, and central billing invoice views now have admin-only manual payment records, succeeded/pending/failed/canceled ledger statuses, partial-paid state, balance due tracking, successful-payment overpayment guard, idempotency keys, configurable order/booking offline payment instructions for public pay links and rendered invoices/PDFs, generic status/webhook locks only after successful manual payment entries, invoice balance refresh, stale order/booking pay-link revocation on successful balance movement, persisted stale-link reason/balance metadata, central billing renewal-needed status, per-document payment-link created/renewed/revoked activity history, idempotent customer payment-received outbox events for successful manual and hosted invoice settlements, payment-received workflow toggles for manual/hosted/receipt-overlap behavior, outbox payment summaries, booking hosted-settlement mismatch protection, CSV/search or analytics visibility, full-payment receipt automation handoff, and localized billing-documents admin shell/body copy for ko and zh-hant; live email delivery/provider QA and richer branded email body previews remain 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 실 이메일 발송/프로바이더 QA 필요. Claude loop iter20 기록. |
| F72 | Payments | Multi-currency | Currency formatting, conversion placeholder, and checkout restrictions are explicit | 🟢 2026-07-03 Codex: Currency formatting, preview conversion helpers, and checkout restrictions are explicit. Checkout shows a single-currency policy, rejects raw mixed-currency carts before product normalization, validates unsupported/disabled currencies through the guarded currency settings enabled subset, and keeps settlement in the cart currency. Admin currency settings persist base currency, enabled checkout currency matrix, disabled/manual-preview conversion mode, and manual preview-rate metadata with localized ko/zh-hant/en copy and stable API errors. Billing document rows, manual-payment forms, checkout, and public invoice pay links surface document/checkout currency plus no-conversion disclosure. Verification: multi-currency/currency-shared/checkout-shared/currency-settings-copy/currency-settings route/checkout route Vitest 34/34 with 3 skipped fixture cases, Chromium `commerce-products.playwright.ts` ko+zh-hant currency settings and checkout mixed-currency flows 3/3, and Chromium `billing-documents.playwright.ts` invoice currency/manual-payment/pay-link disclosure flow 1/1 on `http://127.0.0.1:3191`. Live provider currency QA, automatic FX-rate providers, and converted settlement/order workflows remain future payment depth beyond the F72 explicit-placeholder/guardrail contract. |
| F73 | Payments | Security audit | Checkout/payment paths pass permission, CSRF, validation, and logging checks | 🟢 2026-07-03 Codex: Checkout and payment paths now have explicit permission, CSRF/mutation-guard, validation, rate-limit, token, signature, logging, replay, and admin visibility coverage. Public invoice share/pay links return no-store/noindex responses and hide customer/payment details on invalid, expired, or revoked tokens. Payment-intents diagnostics are builder-admin mutation guarded, return localized validation/invalid-intent errors, and avoid leaking exception details. Production billing share/payment token generation requires `BILLING_DOCUMENT_SHARE_SECRET` or `NEXTAUTH_SECRET` instead of admin-password/fixed local fallback. Billing Stripe and commerce payment webhooks require signed payloads, reject invalid/stale signatures, mask stored payloads, persist durable event ledgers, deduplicate provider retries, block amount/currency mismatches and paid-after-failed downgrades, and expose guarded list/replay APIs. Central Billing Activity shows matched hosted webhook history/replay and unmatched failed/ignored exception review; Commerce payment-webhooks and payment analytics expose localized ko/zh-hant admin review/replay/reconciliation surfaces. Commerce order DELETE uses the shared mutation guard path. Verification: payment-intents/checkout/order-detail/delete/commerce-webhooks/billing-webhooks/payment-link route and engine Vitest 62/62, Chromium `commerce-products.playwright.ts` payment-webhooks ko/zh-hant shell + signed event replay, Chromium `payment-analytics.playwright.ts` webhook reconciliation, and Chromium `billing-documents.playwright.ts` hosted webhook Activity/exception flow 5/5 on `http://127.0.0.1:3191`. Broader production SOC logging and provider-live security drills remain operational hardening beyond the F73 checkout/payment security contract. |
| F74 | Ops | Payment analytics | Revenue, conversion, refund, and failed payment summaries are available | 🟢 Cross-source order/booking payment analytics now has a shared aggregation helper plus `/admin-builder/commerce/payments` cards for net collected, attempts, conversion, failed payments, refunds, by-currency gross/refunded/net/outstanding totals, order-vs-booking source quality, a 7-day trend chart, a source-funnel panel, a live webhook reconciliation panel with captured fee metadata, an order payment-provider mix panel, a visible alert/snapshot banner, exportable JSON/CSV report downloads, and a compact central Billing analytics strip for collected/balance/refunded/review-needed document scope |

## M165 Bookings Pro

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F75 | Bookings | Resources/rooms | Services can require resources/rooms with availability constraints | 🟢 Resource/room records now exist with admin management, services can require one or more resources, resources can carry one-off blocked times, weekly recurring hours, and timezone-aware public filtering, new bookings snapshot required resources, availability blocks cross-staff overlaps and resource blocked times for required rooms, same-process resource/date locks reduce double-book races, and resource capacity rules allow overlapping bookings only up to the configured room limit. Browser proof confirms the shared consultation room editor updates blocked counts, buffers, recurring weekly hours, and capacity behavior; inactive-resource warnings surface in the service card/editor; newly created services stay visible on the first admin page instead of hiding behind accumulated pagination; and the public availability API hides overlapping/adjacent slots when a room has blocked time, resource-specific buffers, its own weekly calendar, or exhausted capacity. 2026-07-03 Codex verified F75 with booking availability/types/pricing/services/admin-create Vitest 72/72, Chromium `bookings-resources.playwright.ts` 5/5 against `http://127.0.0.1:3191`, `npm run typecheck`, and targeted ESLint. |
| F76 | Bookings | Packages/memberships | Service packages, session credits, memberships, and redemption rules exist | 🟢 2026-07-03 Codex: Booking session packages, customer credit ledgers, member-visible package state, and redemption rules are implemented. Admins can create/edit/deactivate service-scoped packages, grant/update/revoke email-matched customer credits with expiry/status/remaining counts, and review redemption counts in the localized packages UI. Public paid booking payment-intent preparation returns package coverage instead of creating a Stripe intent when a usable credit exists; the booking confirmation path atomically consumes one credit with same-process credit locks, snapshots `packageId`/`packageCreditId`/`packageCreditsUsed`, marks the booking paid, blocks stale credit races, and restores credits on cancellation or booking-save failure. Admin-created bookings and waitlist promotion share the same redemption/restoration path; member account booking detail shows package status, remaining credits, usage history, and repeat-booking package payment confirmation. Booking analytics reports package-credit attribution, and the separate commerce subscription plan/customer-subscription API covers recurring membership primitives with create/list/transition/delete flows. Verification: booking package/types/copy/analytics/payment-intent/public booking/admin package+credit routes/admin-create/waitlist/promote/cancel route plus commerce subscription Vitest 154/154, and Chromium `bookings-admin-shell.playwright.ts` + `commerce-subscriptions.playwright.ts` + `members-area.playwright.ts` 3/3 on `http://127.0.0.1:3191`. Live automatic package purchase, provider proration/refunds, and shared/team credit settlement remain future business depth beyond the F76 local package-credit redemption contract. |
| F77 | Bookings | Deposits/varied pricing | Deposits, pay-later, staff/resource-specific pricing, and discounts are supported | 🟢 Services can store optional fixed deposits below full price, public payment-intent creation charges only due-now deposits while snapshotting total/balance metadata, booking creation verifies Stripe amount/currency before accepting real intents, successful deposit webhooks mark bookings partially paid with online paid totals, billing/manual-payment balance math subtracts online deposits, public/admin UI surfaces due-now, total, deposit, and balance amounts, pay-later paid services can be booked from the public widget without creating a payment intent while persisting unpaid/full-balance state, customer cancellation refunds for deposit-paid bookings are capped to captured online deposits, selected-staff and required-resource price overrides flow through service storage, admin editing, public cards, payment-intent metadata, public booking enforcement, admin booking creation, and waitlist promotion, and service-scoped discounts now persist through admin editing/cards, apply in the public widget, reduce payment-intent due-now totals, and persist booking discount code/amount snapshots. 2026-07-03 Codex verified F77 with booking pricing/discount/types/payment-intent/book/admin-create/waitlist Vitest 103/103, Chromium pay-later/deposit-refund/staff-pricing/resource-pricing/staff-pricing-editor/discounts browser pack 6/6 against `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint, and escape-hatch scan; provider-safe QA covers amount/currency/metadata enforcement and Stripe-safe/stubbed payment paths, not a live production provider charge. |
| F78 | Bookings | Staff calendar depth | Staff calendar supports blocked time, overrides, recurring availability, and conflict checks | ✅ Staff availability already supports weekly availability, one-off blocked dates, holiday calendars, imported external busy blocks, date-specific override rows with open/closed preview messaging, and URL-backed month/view/staff state in the admin calendar, while required resources now have one-off blocked times plus recurring weekly hours that close overlapping booking slots; the admin calendar modal now also supports direct reschedule edits for booking start time and staff, the month grid supports drag/drop booking moves between days, and calendar-sync now surfaces persisted last-run QA summaries plus provider OAuth scope/account metadata, pull reconciliation counts, a provider event reconciliation feed with failure entries, and a visible booking conflict visualization panel |
| F79 | Bookings | Client portal | Clients can see upcoming/past bookings and account details | 🟢 Member account links to the bookings portal, authenticated members can see upcoming/past bookings matched by normalized current/prior emails, booking rows link into safe member booking detail pages, and the portal exposes account/profile details, policy/refund chips, customer-local plus office-local time summaries, explicit office timezone, copyable manage links, safe `Add to calendar` downloads, booking-linked invoice/receipt open/pay/download/copy/email actions, billing portal entry points, direct member-authenticated cancel/reschedule actions, and prefilled repeat-booking for the same service/staff. The booking list preserves shareable `q`/status/payment/service/staff/timezone/date range/sort state across reload/back, exports the current filtered slice as CSV, and shows active-filter no-results reset UI while hiding split upcoming/past sections. Member-facing DTOs omit manage tokens, payment intents, manual payment ledgers, and raw billing documents. 2026-07-03 Codex verified F79 with customer-portal/member booking/auth/calendar/package Vitest 29/29, Chromium `members-area.playwright.ts` 1/1 against `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint, and a clean touched-file escape scan; the Playwright file also dropped a stale unused helper and narrowed created IDs instead of non-null assertions. |
| F80 | Bookings | Cancel/reschedule policy | Policy windows, fees, and self-service reschedule/cancel flows exist | 🟢 Service cancellation policy IDs now drive refund decisions and customer self-service cancel/reschedule windows; signed manage links and member booking detail pages show policy/refund status, disable blocked actions, and enforce the same policy before refund, credit restore, email, webhook, or booking-save side effects. Admin-authored cancellation policies include reusable full/partial refund windows and fee rules, refund math covers full, partial, fee-deducted, no-refund, collect-later, and deposit-capped online-paid cases, and member/signed-link cancellation routes share the same refund path while billing-document/manual-payment state blocks refunded or cancelled bookings. 2026-07-03 Codex verified F80 with refund/manage-route/cancellation-policy/billing-document/member-bookings Vitest 30/30, Chromium `bookings-m26-customer-manage.playwright.ts` + `bookings-policies.playwright.ts` + `bookings-deposit-refund.playwright.ts` + `members-area.playwright.ts` 5/5 against `http://127.0.0.1:3191`, targeted ESLint, `npm run typecheck`, clean touched-file escape scan, failed-artifact check, and `git diff --check`; provider coverage is Stripe-safe/stubbed/local webhook proof, not a live production charge. |
| F81 | Bookings | Waitlist | Full sessions can accept waitlist entries and promote clients | 🟢 Service capacity/remaining seats now keep group-booking slots open until capacity is full, suppress exhausted slots, expose public waitlist entry when no slots remain, dedupe active same-date/service/staff/email entries, and let admins mark contacted/active/closed or promote active entries into confirmed bookings with slot rechecks, same-process slot locks, package-credit/deposit/resource price snapshots, confirmation email, billing automation, webhook/audit event context, and localized stable-code errors. 2026-07-03 Codex verified F81 with booking availability/copy/waitlist route Vitest 51/51, Chromium `bookings-m27-waitlist.playwright.ts` public join + admin promote flow 1/1 against `http://127.0.0.1:3191`, `npm run typecheck`, and targeted ESLint; the browser fixture now uses a public-safe slug instead of the intentionally hidden `g-editor-*` sandbox namespace. |
| F82 | Bookings | Reminders | Email/SMS-style reminder hooks and admin templates exist | ✅ Email/SMS reminder windows now share service-specific schedule settings with admin 24h/1h/off controls, the booking email template editor now supports live preview plus clickable placeholder insertion, and the service editor exposes reminder-draft state with a chip summary; provider delivery QA and deeper per-channel policy remain 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 실 발송 프로바이더 QA 필요. Claude loop iter20 기록. |
| F83 | Bookings | Timezone/localization | Booking slots render correctly across site/admin/client timezones and locales | 🟢 Availability now converts staff local date/time through validated IANA timezones instead of a Seoul/Taipei offset shortcut, slot availability checks use the staff local date across UTC boundaries, public booking/manage/member/email surfaces format customer-facing times with explicit saved customer timezone, the member booking detail page now also shows customer-local and office-local time side by side using the assigned staff availability timezone, the admin dashboard/calendar booking modals now show the same office-local context, the admin bookings dashboard/calendar chrome and body copy are localized for ko and zh-hant, and the shared bookings admin shell now localizes its eyebrow, aria-label, nav labels, route titles, route subtitles, and body copy across the dashboard/services/policies/packages/resources/staff/calendar/calendar-sync/email-template/staff-availability pages for ko and zh-hant, while the bookings policies and email-template editors now localize their visible editor chrome, summary text, preview/save controls, and preview sample values for ko and zh-hant, the public booking widget flow now localizes its step labels, waitlist chrome, payment chrome, and confirmation controls for ko and zh-hant, the bookings packages editor now localizes its visible editor chrome, package/credit summary chips, and create/edit/grant/revoke modal controls for ko and zh-hant, the bookings resources editor now localizes its visible resource toolbar, card summaries, weekly schedule builder, blocked-time editor, and save/cancel modal controls for ko and zh-hant, the bookings services editor now localizes its visible services toolbar, card summaries, payment/deposit labels, reminder schedule, resource warnings, and create/edit modal controls for ko and zh-hant, and the booking availability editor now localizes its weekday labels, time-range aria labels, blocked-date controls, date-override remove control, and availability preview messages for ko and zh-hant, the public booking manage page is localized for ko and zh-hant, the member account/login/premium route titles are localized for ko and zh-hant, the consultation admin shell now localizes its hero summary, time-window label, fallback state, section chrome, funnel/conversion labels, knowledge form labels/buttons/placeholders, performance/safety/category/risk/locale table labels, and recent submissions/chat/feedback headings for ko and zh-hant, the public Events pages localize their eyebrow/back-link/RSVP shell copy, the public service detail page localizes its back link, key points label, attorney heading, related-columns heading, read-more text, contact card, empty state, review note, and breadcrumb label for ko and zh-hant, the public lawyer profile page localizes its profile label, contact CTA, and related-section headings for ko and zh-hant, the public column detail page localizes its back link, consultation card, attorney heading, and related-topics heading for ko and zh-hant, the public portfolio detail route localizes its back link, eyebrow, gallery label, and side labels for ko and zh-hant, the portfolio admin shell localizes its header, stats label, form labels/buttons, filter labels, notices, row status text, and gallery fallback alt text for ko and zh-hant, the app market admin shell localizes its frame title/description, search toolbar, installed-app summary, KPI labels, action buttons, uninstall/state notices, and no-results copy for ko and zh-hant, the marketing admin shell localizes its workspace title/description, sidebar labels, nav label, route titles, and campaigns/subscribers body copy for ko and zh-hant, the asset library modal localizes its dialog title, toolbar labels, folder-tree labels, empty states, and item actions for ko and zh-hant, the builder workspace dashboard localizes its top-shell title/description, sidebar rail labels, workspace/site pills, inspector labels, static route policy text, starter gallery labels, route validation copy, and route metadata title/description for ko and zh-hant, the builder CMS route localizes its metadata title/description, back link, sidebar labels, top-shell title, status labels, and shared frame labels for ko and zh-hant, the builder service-source route localizes its metadata title/description, back link, sidebar labels, top-shell title, lifecycle heading, and shared frame labels for ko and zh-hant, the builder lawyer-source route localizes its metadata title/description, back link, sidebar labels, top-shell title, lifecycle heading, locale label, and shared frame labels for ko and zh-hant, the shared attorney authority card now localizes its eyebrow on public service/lawyer/column routes, the public columns list page localizes its shell label for ko and zh-hant, the shared intent landing page now localizes its header label, section labels, and attorney CTA copy for ko and zh-hant, the public portfolio list page localizes its eyebrow and categories aria-label for ko and zh-hant, the public search page localizes its FAQ tab label for ko and zh-hant, the public homepage hero quick menu now localizes its FAQ quick link for ko and zh-hant, the public FAQ explorer now localizes its category-rail label for ko and zh-hant, the public homepage FAQ section label now localizes for ko and zh-hant, the shared public header logo now localizes its home label in both the app shell and published site shell for ko and zh-hant, the shared public footer now localizes its social icon aria-labels for Blog, YouTube, and Website in both shells for ko and zh-hant, the shared public primary nav now localizes its landmark label in both shells for ko and zh-hant, the published site header now localizes its main-nav aria-label and navigation rename fallback labels for ko and zh-hant, the published site mobile drawer now localizes its dialog label, close button, and drawer nav landmark in both shells for ko and zh-hant, the app-shell mobile drawer now localizes its nav landmark for ko and zh-hant, and the public year-end event popup now localizes its badge for ko and zh-hant, the live-chat inbox admin route localizes its title/description, conversation count label, refresh button, empty state, anonymous visitor label, closed-status badge, close-conversation button, reply placeholder, send button, no-selection state, author fallback labels, and date formatting for ko and zh-hant, the shared global header/footer editor localizes its route title, admin back link, drawer buttons, slot labels, save-state copy, and fallback sentence for ko and zh-hant, the ops dashboard localizes its route metadata, visible page heading/summary, and tab labels/aria-label for ko and zh-hant, the backups admin route localizes its metadata title/description, visible heading/body, action buttons, table headers, empty state, and restore message copy for ko and zh-hant, the webhooks admin route localizes its metadata title/description, visible heading/body, create controls, table headers, action labels, and delivery-detail labels/messages for ko and zh-hant, and the forms admin route localizes its metadata title/description, dashboard header/filter/empty-state copy, submissions list headers, and builder shell title/body for ko and zh-hant, and the dev functions shell and SDK docs now localize their visible shell heading, docs labels, and function action chrome for ko and zh-hant, and the shared admin nav rail, breadcrumb, page back bars, and recent-nav chrome now localize their labels for ko and zh-hant; the sandbox navigation rename flow now localizes its untitled fallback label for ko and zh-hant, the builder page switcher now localizes its home badge, published/draft tooltip, and untitled fallback labels for ko and zh-hant, Zoom meeting links now inherit the staff office timezone on create/reschedule, and long timezone labels have safer mobile wrapping, and the localized builder workspace page/starter/dynamic-route/scene shells now localize their page-mode pills, page list status chips, canvas-focus copy, mode policy labels, publish-readiness labels, page diagnostics disclosure, dataset seam labels, publish warning copy, starter-template shell/action labels, dynamic-route seam labels, route summary labels, scene shell labels, readonly home/about/contact preview shells, and SEO dashboard shell, while the SEO panel now localizes its untitled page fallback in social preview for ko and zh-hant, search admin shell, error monitoring shell, FAQ admin shell, member admin shell, experiments admin shell, and lightbox admin shell for ko and zh-hant; customer manage-link API errors now return localized stable-code payloads for ko/zh-hant/en across invalid links, missing bookings, rate limits, policy blocks, invalid updates, slot conflicts, unavailable slots, and staff-unavailable failures, with focused route/helper tests plus live HTTP proof for the zh-hant invalid-link surface |
| F84 | Bookings | Booking analytics | Utilization, no-show, revenue, staff, and service analytics are available | 🟢 Dashboard analytics now has a Today/Needs Action operations queue for pending, unpaid, waitlist, no-show, and document-needed work, plus exportable JSON/CSV reports that bundle the current filters, visible rows, trend series, source attribution, source funnel, payment-channel attribution, and utilization/heatmap metrics. Alert banners surface no-show/pending/waitlist/trend signals, and the dashboard/API/report now split Stripe, manual payment, package-credit, pay-later, and free-booking attribution with booking counts, paid counts, and revenue context. 2026-07-03 Codex verified F84 with booking analytics/report/API Vitest 21/21, focused Chromium dashboard export/panel flow on `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint, and `npm run build`. 2026-07-03 Codex+GLM 5.2 max follow-up: booking analytics API now scopes bundle input and payment-channel attribution to requested `serviceId`/`staffId`, preventing filtered reports from leaking other services' manual/Stripe/payment-channel totals. Red route test failed on unfiltered `svc-other`, then green with analytics route Vitest 5/5, bookings analytics Vitest 19/19, typecheck, full lint, changed-file ESLint, and diff-check. 2026-07-03 Codex+GLM 5.2 max follow-up-2: admin dashboard analytics panels and JSON report summary now scope overview/source/source-funnel/payment attribution/utilization/customer profiles to the visible filtered booking set, while action queue rows remain action-scoped; red report test failed on hidden booking leakage, then green with dashboard-report+analytics Vitest 16/16, focused Chromium service-filter export/panel flow against `http://127.0.0.1:3001`, screenshot `test-results/booking-dashboard-filter-scope.png`, typecheck, lint, full Vitest 783/783 files, builder-route guard audit, no-excuse scan, and diff-check. |

## M166-M167 AI Builder And Assistants

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F85 | AI | Prompt-to-site intake | AI builder captures business type, goals, pages, brand, tone, and constraints | 🟢 Expanded intake captures industry/business type, company, slogan, audience, goals, desired pages, brand keywords, constraints, visual direction, optional uploaded hero image asset, tone, and color preference with mobile-safe admin UI. Recent prompt/draft history saves and restores the last 6 local generated drafts, while server-side intake versions append generated and cached drafts with spec snapshots into the selected-site ledger using the shared Blob/file backend pattern. The admin rail lists server versions, diffs previous→latest, and restores an older server version into the current draft; generation remains successful with `versionWarning: server_version_record_failed` if ledger recording fails. 2026-07-03 Codex verification: AI generator route/intake-store/version-route/page-spec/orchestrator Vitest 33/33 with 1 skipped fixture case, Chromium `ai-generator-server-versions.playwright.ts` plus focused expanded-intake and uploaded-hero-image AI generator flows 3/3 on `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint with existing `next/no-img-element` warning only, clean escape-hatch scan, and `git diff --check`. |
| F86 | AI | Generated sitemap/pages | AI can generate a coherent page tree and initial content plan | ✅ 2026-07-03 Codex: AI draft returns deterministic sitemap, content plan, visual/brand brief, and apply review before write. Sitemap scope can select non-home generated pages, reorder the selected page tree, show page-tree diff states for new/existing/nested/created pages, and show Navigation diff states for hidden-until-publish vs public-after-publish entries. Apply creates selected non-conflicting draft pages only, can append actually-created pages to Navigation while public runtime hides unpublished pageId-backed nav items, then exposes per-created-page publish controls with preflight, warning acknowledgement, immediate publish, scheduled publish, cancel schedule, transaction status, and route/navigation visibility state. Verification covers API sitemap creation/duplicate skip/rollback tests plus `tests/builder-editor/ai-generator.playwright.ts` mobile flow for tree diff, reorder, selection removal, Navigation diff, draft creation, hidden public nav before publish, schedule/cancel, publish, public route 200, and navigation diff becoming `public_after_publish`. |
| F87 | AI | Layout generation | AI can generate page sections from the design pool with editable nodes | 🟢 2026-07-03 Codex: AI apply now writes schema-valid editable canvas documents from generated drafts through both inline single-page and selected sitemap-scope creation, with invalid/reserved/duplicate slug guards, draft-only creation, canvas-write rollback, optional created-page Navigation insertion, no prompt dialog, UI draft discard, and initial SEO metadata seed. Generated layouts use container-based visible hero/proof/brand visual/content card/CTA primitives, real hero media image frames, selected uploaded hero asset overrides, visual brief/prompt/treatment previews, mobile overrides, no-overlap mobile CTA placement, mobile-stacked proof cards, sitemap starter page cards, and design-pool template metadata/classes for services/faq/insights/offices. AI generated root sections can be saved into Saved Sections and reinserted into the builder while preserving metadata/classes/mobile overrides. Verification: canvas-import/apply/orchestrator/page-spec/designer-score/responsive-rules/section-library Vitest 64/64 with 2 skipped fixture cases, plus Chromium `ai-generator.playwright.ts` focused expanded layout apply, selected sitemap draft page, and saved-section reuse flows 3/3 on `http://127.0.0.1:3191`. Page-tree diff/reorder and publish controls are covered by F86; responsive preview/undo by F88; style suggestions by F89; image generation/edit imports by F92/F93; assistant depth by F90-F94. Curated pixel-perfect design-pool ranking and deeper generator controls remain future enhancement depth, not F87 blockers. |
| F88 | AI | Responsive AI | AI can suggest and apply breakpoint/layout fixes with preview and undo | ✅ 2026-07-03 Codex: deterministic heuristic engine (`scanResponsiveSuggestions`) flags overflow text, oversized fonts, side-by-side container stacking, and now generic node viewport overflow for image/container/gallery/text nodes that start left of or run past the mobile/tablet safe area. POST `/api/builder/ai-generator/responsive` returns suggestions; the topbar mounts `ResponsiveAiPanel` for tablet/mobile with analyze, per-row/all preview, cancel preview, apply one/all, and undo-last controls. Preview uses transient responsive override mutation sessions; apply commits one undoable history step; undo restores the previous responsive layout and row state. Viewport changes clear stale suggestions/previews, reanalysis reads the restored baseline document instead of transient preview state, and the 390px editor topbar keeps the original navigation/preview controls reachable without clipping the F88 entry. Korean/zh-hant/en copy is tested. Verification includes targeted responsive-rule/copy/route Vitest, `tests/builder-editor/responsive-ai-overflow.playwright.ts` against the live dev API, `npm run typecheck`, `npm run lint`, no-excuse scan, Playwright regression QA (`home-hero-title` 308px → preview/apply 160px → undo 308px; reanalysis request widths stayed 308px; preview → tablet reset 675px), visual screenshots at 1280/768/390 in `/tmp/tseng-f88-qa/`, and release/W audits. |
| F89 | AI | Style/theme suggestions | AI can propose palette, typography, spacing, and component style changes | ✅ Deterministic theme suggestions now cover palette, fonts, radii, shadow/effects, and typography scale through six vibes (modern/warm/professional/playful/luxury/minimal). `/api/builder/ai-generator/theme` supports `suggest`, `analyze`, `derive-accent`, and settings-gated `apply`, with `apply` persisting through `readSiteDocument`/`writeSiteDocument` plus existing `mergeTheme`. Site Settings > Presets renders an AI theme suggestion panel that previews swatches/style tokens, applies the suggestion to the modal theme state, and persists through the existing Save flow. Verification: 21 unit tests for vibe/style-token/apply behavior, Chromium API Playwright for suggest/analyze/apply persistence, Chromium UI Playwright for suggest -> apply -> save -> persisted settings, `npm run typecheck`, `npm run lint` exit 0 with existing warnings only, and screenshot proof `/tmp/tseng-law-ai-theme-panel.png`. Future LLM-driven color tuning remains an enhancement, not a gate blocker. |
| F90 | AI | Section generator | AI can insert a requested section with copy, assets, and structure | ✅ `/api/builder/ai-generator/section` accepts prompt + optional sectionKind, calls gpt-4o-mini with JSON response_format for spec, server runs deterministic `buildSectionNodes` to emit hero/features/testimonials/cta/faq node trees with mobile overrides. `AiSectionGeneratorPanel` shows preview + apply-to-canvas. Falls back to stub when no API key. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 실 LLM API 키 스모크 필요(스텁 폴백은 구현됨). Claude loop iter20 기록. |
| F91 | AI | Text assistant | AI can rewrite, expand, shorten, translate, and tone-adjust selected text | ✅ 2026-07-03 Codex: Inline text editor exposes an AI button with rewrite/expand/shorten/translate/tone choices, custom prompt guidance, source/result preview, and panel-level result undo/redo backed by `/api/builder/ai-generator/text` (guardMutation, gpt-4o-mini, schema-validated input, translate requires `targetLocale`, tone requires `tone`). Route/unit coverage proves prompt building plus happy/error validation paths. Playwright now drives the live editor with a mocked rewrite endpoint, applies AI text to the selected canvas node, commits it to the draft API, uses the stage toolbar undo to restore the original text in both canvas and draft, uses redo to reapply the AI text in both canvas and draft, and verifies translate forwards `targetLocale`. Deeper inline selection scoping, streaming responses, prompt versioning, and shared collaborator prompt history remain future assistant depth rather than core F91 blockers. |
| F92 | AI | Image creator connector | Image generation/import flow can create assets with prompt metadata | 🟢 `/api/builder/ai-generator/image` calls Image 2.0 (`gpt-image-2`), validates returned bytes via shared image-upload validation, persists the result into the builder asset store via `uploadBuilderImageAsset`, and audits the upload. Generated hero image nodes retain visual direction, Image 2.0-ready generation prompt metadata, alt text, focal point, and deterministic filter treatment. The AI generator wizard intake step exposes prompt/size/quality/format options and can reuse uploaded hero assets. |
| F93 | AI | Image edit/replace | AI-assisted image replacement respects selected element, alt text, and focal point | 🟢 ImageEditDialog AI tab calls `/api/builder/ai-generator/image/edit` (Image 2.0 v1/images/edits, gpt-image-2) with preset mask areas, freeform brush mask drawing, Add/Erase modes, brush size/feather/edge controls, undo-last-stroke, original-vs-result review, generated edit selection history, before/after transaction review, and apply that swaps only `content.src` while preserving alt text, focal point, crop aspect, and filter state on the selected image node. |
| F94 | AI | Code assistant | Developer-facing assistant can explain/build code slots with reviewable diffs | 🟢 `/api/builder/ai-generator/code` runs explain/fix/optimize/comment via gpt-4o-mini with JSON shape, server computes unified diff. `CodeAssistantPanel` shows explanation + collapsed fixedCode/diff + apply button for the serverless functions editor. 2026-07-02 Claude loop iter19: code-assistant unit green + dev-functions.playwright 어시스턴트 구간 pass(실패건은 F106 샌드박스 타임아웃 가드로 무관). route 전용 unit 부재는 노트에 기재. 검증 완료로 🟢 전환. |

## M168-M169 Collaboration And Workflow

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F95 | Collaboration | Presence/cursors | Multiple editors show active users, selected nodes, and cursor/location state | ✅ In-memory `presence-store.ts` with TTL + heartbeat, `/api/builder/collab/presence` GET/POST routes behind `guardBuilderRead`/`guardMutation`, and `PresenceIndicator` client component (avatar stack with initials, selected-node tooltip). Per-process only; Redis/Upstash backend remains for multi-replica deploys. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 멀티리플리카 Redis/Upstash 백엔드=프로덕션 인프라. Claude loop iter20 기록. |
| F96 | Collaboration | Conflict handling | Concurrent edits resolve deterministically with user-visible conflict feedback | 🟢 Page canvas writes now use the shared per-(site,page) `withCanvasMutex(siteId, pageId, task)` path, preserving FIFO same-page writes, parallel different-page writes, lock release on throw, and empty-queue GC. Draft saves require the current revision for enveloped records and return localized stable `draft_expected_revision_required` / `draft_conflict` payloads with current revision metadata instead of overwriting stale browser state. The editor save path consumes those conflicts, blocks the stale save, and shows a visible reload/backup banner. 2026-07-03 Codex verification: mutex + page-id draft route + legacy page-key lifecycle Vitest 34/34, Chromium `collab-conflict.playwright.ts` real stale-draft conflict flow 1/1 on `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint, clean escape-hatch scan, and `git diff --check`. Multi-replica distributed locks/CRDT merge remain production hardening beyond the local deterministic conflict contract. |
| F97 | Collaboration | Canvas comments | Comments can be attached to pages/elements/regions | 🟢 File-backed `comments-store.ts` with per-(site,page) write mutex, GET/POST + PATCH (resolve/reopen) + DELETE routes, `CommentsPanel` React component with attach-to-node toggle and resolve filter. Unit + Playwright coverage. 2026-07-02 Claude loop iter16: 격리(4641+BUILDER_SITE_ROOT 양측) 브라우저 증빙 green 재확인 — collab-comments smoke(create/list/resolve/reopen/delete) 1/1. 검증 완료로 🟢 전환. |
| F98 | Collaboration | Resolve/assign comments | Comments support assign, reply, resolve, reopen, and filter states | 🟢 2026-07-03 Codex/GLM 5.2 read-only audit selected this bounded local gap. `BuilderCollabComment` now persists `parentId`, `assignee`, and `resolvedBy`; `createComment` validates same-page parents; `listComments` filters by `assignee`; `assignComment` sets/clears assignees; resolve/reopen uses thread helpers so root resolution covers replies while reply resolution stays single-row. API POST accepts `parentId`/`assignee`, GET filters by `assignee`, and PATCH `action:"assign"` sets or clears assignment. Unit/threading tests 21/21 and collab-comments Playwright API proof 2/2 passed. Follow-up route contract repair: stale comments route tests now assert `listComments` options, resolver username, `CommentParentNotFoundError`, assignee filter, dangling reply 404, and assign action; collab suite 73/73 and full Vitest 4,394 passed / 14 skipped. |
| F99 | Collaboration | Roles/permissions | Owner/admin/designer/editor/client roles gate editor/admin actions | 🟢 Per-user role store on top of basic-auth: `user-role-store.ts` with module write-queue, owner-safety guards, last-seen timestamp; pure role→permission matrix (`role-permissions.ts`) defines owner/admin/designer/editor/client capabilities; `userHasPermission(username, perm)` adapter falls back to owner when no record exists. New `manage-roles`/`delete-site`/`view-cms`/`manage-translations`/`manage-secrets` permissions added. UsersAdmin client UI + `/api/builder/security/users` routes for CRUD. 2026-07-02 Claude loop iter18: 증빙 재실행 green — user-role-store/role-permissions/resolve-permission unit + security/users 2 route tests = 역할스토어·매트릭스·어댑터·CRUD 전부 green. 검증 완료로 🟢 전환. |
| F100 | Collaboration | Client review mode | Clients can review/comment without full editor permissions | 🟢 Short-lived signed review tokens (HMAC-SHA256 via NEXTAUTH_SECRET) with file-backed session ledger, `/api/builder/review-sessions` CRUD, `/api/builder/review-sessions/[token]` resolver, public `/review/[token]` page that sets an httpOnly session cookie and renders an iframe preview + comment form. Token revocation + expiry verified by `verifyReviewToken`. 2026-07-02 Claude loop iter18: 증빙 재실행 green — review-tokens unit(HMAC 폐기/만료) + review-sessions 2 route tests green. 검증 완료로 🟢 전환. |
| F101 | Workflow | Branches/variants | Site changes can be isolated in branches/variants and compared | 🟢 File-backed branch store under `runtime-data/branches/${id}.json` with `BuilderBranch` snapshots, per-page change map, status (draft/merged/discarded). Routes: GET/POST `/api/builder/branches`, GET/PATCH/DELETE `/api/builder/branches/[id]`, POST `/api/builder/branches/[id]/merge`. Merge writes per-page draft canvases via existing `writePageCanvas`. 2026-07-02 Claude loop iter16: 격리(4641+BUILDER_SITE_ROOT 양측) 브라우저 증빙 green 재확인 — branches POST/GET/PATCH(discard)+검증 2/2. 검증 완료로 🟢 전환. |
| F102 | Workflow | Approval workflow | Publish can require approval for selected roles/sites | 🟢 File-backed approval store at `runtime-data/approvals/${id}.json` with pending/approved/rejected states. Routes: GET/POST `/api/builder/approvals`, GET/PATCH `/api/builder/approvals/[id]`. Branch merge consults `site.workflow.requireApproval` and refuses unless the latest approval is `approved`; both transitions emit `kind:approval` notifications. 2026-07-02 Claude loop iter16: 격리(4641+BUILDER_SITE_ROOT 양측) 브라우저 증빙 green 재확인 — approvals request/approve+missing-branch 거부 2/2. 검증 완료로 🟢 전환. |
| F103 | Workflow | Audit log | Security, publish, app, CMS, commerce, and role changes are logged | 🟢 Existing `audit/record` + new ops Security panel surface CSRF denials, publish.blocked/failure, asset.delete events; deeper coverage across CMS/commerce/role changes remains. 2026-07-02 Claude loop iter26: GLM5.2 3분할 구현·Claude 검수 — security.user_*(users 라우트 2), cms.record_*(records 라우트 2), commerce.settings_updated(currency/notifications) 감사 배선 + 스키마/타입/aggregator 확장, 관련 44/44 green. payments/webhooks는 설정 PATCH 라우트 부재로 대상 아님(보고 기록). 🟢 전환. |
| F104 | Workflow | Notifications | Comment, approval, order, booking, app, and publish notifications have a shared model | 🟢 Unified `BuilderNotification` shape with kind (comment/approval/order/booking/app-install/publish), audience filter, file-backed inbox at `runtime-data/notifications/inbox.json`, GET/POST/PATCH/DELETE routes (POST is internal-only via same-origin/`x-internal-source` secret), bulk mark-all-read PUT, `NotificationInbox` bell component for the admin shell. 2026-07-02 Claude loop iter18: notifications unit 2 + route 2 + notifications.playwright 전부 green. 검증 완료로 🟢 전환. |

## M170 Developer Platform

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F105 | Dev | Custom code slots | Page/site head/body/custom element code slots exist with validation and preview warnings | 🟢 `validateCustomCode` strips insecure `<script src="http://...">` and `<iframe>` tags, warns on `eval()`, caps at 8000 chars. Site/per-page custom code stored on BuilderSiteDocument/BuilderPageMeta with new PATCH routes; render injection in PublishedSitePageView. `CustomCodePanel` admin UI with live warning chips. 2026-07-02 Claude loop iter18: 증빙 재실행 green — validateCustomCode unit + site/page custom-code 2 route tests green. 검증 완료로 🟢 전환. |
| F106 | Dev | Serverless functions | Backend function route model, environment, and local execution harness exist | ✅ `functions-model.ts` stores typed function records (slug/code/runtime/enabled), GET/POST/PATCH/DELETE + invoke routes, and a bounded worker-thread/vm test harness with 1000ms timeout/log capture. Canvas codeBlock slots can run inline JS/TS function-body snippets or bind to saved Functions by slug/id through `/api/builder/dev/code-slots/run`. The worker/vm harness now creates `ctx`/`console` logging helpers inside the vm context so `constructor.constructor` on injected log functions cannot reach the worker `process`. Production-grade isolation (Vercel Sandbox/isolated-vm) remains. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 프로덕션급 샌드박스 격리(Vercel Sandbox/isolated-vm) 필요. Claude loop iter20 기록. |
| F107 | Dev | API/SDK surface | Internal SDK docs/types cover pages, CMS, media, apps, bookings, commerce, and publish | 🟢 `getSdkDocSections()` exposes structured documentation (title/paragraphs/types/example) for Pages/CMS/Media/Apps/Bookings/Commerce/Publish/Functions APIs; `/admin-builder/dev/sdk` renders the sections with anchor nav and `/api/builder/dev/sdk` returns the same JSON-friendly source for future LLM tooling. 2026-07-02 Claude loop iter19: sdk-docs unit(getSdkDocSections) + dev-functions.playwright SDK 구간 pass. 검증 완료로 🟢 전환. |
| F108 | Dev | Data APIs | CMS and app data APIs support typed query/mutation with permissions | 🟢 Already covered by M158/M160 CMS routes; SDK-style facade pending. 2026-07-02 Claude loop iter22: GLM5.2 구현·Claude 검수 — data-sdk.ts 타입드 파사드(권한게이트 edit-pages 미러, DataSdkPermissionError)+SDK 문서 섹션+테스트, dev/__tests__ 41/41 green. 🟢 전환. |
| F109 | Dev | App extension hooks | Apps can hook editor, public runtime, checkout, bookings, and CMS lifecycle events | ✅ In-memory registry + file-backed metadata at `runtime-data/apps/hook-registrations.json`. Six event kinds (editor.page-save, public.page-render, commerce.order-created, bookings.reservation-created, cms.record-created, publish.completed). Priority-desc live dispatch with per-handler error isolation logged to F110 dev logs. GET/POST `/api/builder/apps/hooks` for diagnostics + secret-backed stored code registration, guarded POST `/api/builder/apps/hooks/invoke` for manual lifecycle dispatch, and `hook-runtime.ts` executes stored code through the hardened worker/vm harness with app-log references. Publish, commerce order, booking reservation, editor draft save, public page render, and editable CMS record create lifecycle patches now call the combined live+stored dispatcher; Playwright drives editor/public/CMS hook logs through real HTTP surfaces. Stored hook runs now write a file-backed delivery ledger with status/error/event/attempt metadata, guarded GET `/api/builder/apps/hooks/deliveries` exposes failed delivery visibility, guarded POST `/api/builder/apps/hooks/deliveries/[deliveryId]/retry` replays the original event, and `/api/cron/app-hooks-retry` drains failed stored hook deliveries with attempt backoff + max-attempt cutoff. Durable distributed logs and true isolation remain. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 내구성 분산 로그+진짜 격리=프로덕션 인프라. Claude loop iter20 기록. |
| F110 | Dev | Logs/console | Admin can inspect function/app/webhook logs and errors | ✅ File-backed local ring buffer (`logs-store.ts`) per source (function/webhook/app) with GET `/api/builder/dev/logs?source=&since=&reference=` exact-reference filtering, function and canvas code-slot invocation auto-logs to store, saved-function slot logs use `canvas-code-block:*:function:<slug>` references, codeBlock Content inspector shows recent stored logs for the selected slot, `DevLogsPanel` table view with auto-refresh, and Functions admin level/query filtering plus JSON export. The async read path mirrors logs to private Vercel Blob when `BUILDER_DEV_LOGS_BACKEND=blob` or production Blob env is configured, the same read route can export durable logs server-side as JSON/JSONL with source/all, level, query, since, limit, and reference filters, guarded DELETE `/api/builder/dev/logs?source=` can purge one source or all sources through `guardMutation`, and guarded DELETE with `before=<ISO timestamp>` prunes entries older than a cutoff while preserving newer history. Full multi-replica delivery guarantees, automated long-term retention policy, production observability depth, and true isolation remain. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 멀티리플리카 전달 보장/프로덕션 관측성 필요. Claude loop iter20 기록. |
| F111 | Dev | Local dev/deploy validation | Custom code/app functions pass typecheck, lint, smoke, and publish validation | 🟢 `/api/builder/publish/validate` GET endpoint runs the existing publish gate (`runAllChecks`) read-only and returns the suite (empty-content, broken-links, image-alt, SEO, stale-dataset, code-slot saved-function, prerender, form-target, responsive-overflow, h1-count). Publish UI can pre-flight before commit and shows a Developer card for code-slot/function binding blockers. Saved builder functions compile through a strict async function-body validator on create/update, so syntax errors are rejected before save; publish validation also blocks bound saved functions that use blocked APIs such as `require` and `process`. `validate:dev-platform` now gives local/CI a named gate for typecheck, lint, custom-code/serverless unit validation, code-slot inspector smoke, and publish-validation browser proof, with `validate:dev-platform:unit` and `validate:dev-platform:browser` wired into the Builder Quality workflow. 2026-07-03 Codex verified F111 with `BASE_URL=http://127.0.0.1:3191 npm run validate:dev-platform` (typecheck, lint with existing warnings, 40 focused Vitest tests, Chromium `publish-validate.playwright.ts` + `code-block-code-slot.playwright.ts` 4/4), plus `npm run build` exit 0 with existing warnings only. |
| F112 | Dev | Secrets manager | Site secrets can be stored, scoped, rotated, and referenced by server code | 🟢 AES-256-GCM encrypted secret store at `runtime-data/dev/secrets.json` (Blob in prod), KEK via `BUILDER_SECRET_KEK`/`NEXTAUTH_SECRET`, site- vs function-scope with allowedFunctions, one-time plaintext reveal on create/rotate. `/api/builder/dev/secrets` routes with `manage-secrets` permission. `ctx.getSecret(key)` injected into serverless function sandbox closure-bound to caller slug. `SecretsAdmin` UI with reveal modal + clipboard copy. 2026-07-02 Claude loop iter16: 격리(4641+BUILDER_SITE_ROOT 양측) 브라우저 증빙 green 재확인 — secrets shell ko/zh 2/2 + secrets-store unit 10/10(암호화·스코프·회전). 검증 완료로 🟢 전환. |

## M171-M174 Cross-Cutting Milestones

M171-M174 are represented across the F-layer checkpoints above plus existing W-layer SEO/publish/admin gates. Do not mark those milestones complete unless their dependent F-items are green and a dedicated milestone memo links the relevant implementation/test evidence.

**2026-05-21 first-slice progress** — six parallel sub-agents drafted credible MVP slices for the remaining 🔴 milestones in one wave:

- **M171 Marketing/CRM** 🟡: file-backed contacts (`contact-model.ts` / `contact-store.ts`) with merge-by-email + tag union, automation engine with contact-created/tag-added/form-submitted triggers and add-tag/send-email-stub/HMAC-signed-webhook actions, Slack/generic/Mailchimp audience dispatcher, guarded outbox read API, `/admin-builder/crm` four-tab UI (contacts/automations/integrations/outbox), public form submissions auto-merge into CRM, subscriber/contact linkage for public and admin subscribers, marketing campaign delivery through stub/Resend/Mailchimp Transactional providers, CRM Mailchimp Marketing API list member/tag sync, marketing open/click/unsubscribe analytics webhook fan-out, campaign stats breakdown/funnel/recent-activity analytics dashboard, consent evidence, seven-day double opt-in token windows, expired verification rejection, guarded deliverability readiness/test-send QA, and Playwright/API coverage for contact-created email-stub outbox visibility plus direct HTTP subscriber linkage, campaign dispatch, fake-Mailchimp CRM contact sync verification, fake-webhook marketing analytics event verification, desktop/mobile campaign editor analytics UI verification, public subscribe/verify consent policy verification, and desktop/mobile deliverability QA panel verification. Live production provider DNS/authentication and inbox smoke with real credentials remains.
- **M173 Enterprise workspace** 🟡: single-account workspace (`account.json` under `runtime-data/workspace/`) with sites/members/shared-assets/analytics tabs, owner-safety guard against demoting the last owner, file+Blob-capable shared asset library, cross-site CMS rollup, payment analytics rollup across orders+bookings, M173-A library-level shared asset upload hardening, M173-B shared asset usage/delete protection, M173-C account-level CMS collection lifecycle, M173-D shared asset Blob parity, M173-E granular workspace route permissions, M173-F richer operations analytics, M173-G first-slice workspace-site editor routing, M173-H selected-site chrome API routing, M173-I selected-site site-level SEO API routing, M173-J selected-site page SEO assistant routing, M173-K selected-site main page SEO route routing, M173-L selected-site site settings route routing, M173-M selected-site lightbox API routing, M173-N selected-site scheduled-publish API routing, M173-O selected-site page revisions API routing, and M173-P selected-site collab API routing: workspace CMS POST/PATCH/DELETE validates registered site selection, delegates to the existing per-site CMS engine, returns localized stable-code errors, shared asset storage resolves file/Blob at call time with private `builder-workspace/assets/*` Blob pathnames, read routes require settings/manage-users/edit-pages/view-cms as appropriate, mutations pass settings/manage-users/edit-pages into `guardMutation`, analytics reports exact shared asset count/bytes/latest upload, site/member totals, unique CMS collection count, CMS record totals/latest update, per-site CMS counters, workspace Sites rows deep-link to `/{locale}/admin-builder?siteId=...`, the main editor plus core page/draft/publish/publish-check routes resolve selected siteId from query/body/referrer while preserving legacy default clients, navigation/header/footer draft APIs now do the same for selected editor referrers, SEO overview/checklist/settings/preview/bulk APIs resolve the selected siteId from query or editor referrer while overview/bulk draft-canvas reads stay on the selected site, page SEO assistant GET/PATCH resolves the selected siteId with its draft-canvas reads, main page SEO GET/PATCH resolves the selected siteId after splitting its oversized route into schema/response/mutation modules, site settings GET/PUT resolves the selected siteId after splitting its oversized route into schema/settings/appearance/response modules, lightbox list/create/detail/draft APIs resolve the selected siteId for GET/POST/PATCH/DELETE/draft GET/draft PUT, scheduled-publish GET/POST/DELETE APIs resolve the selected siteId from query/body or editor referrer while legacy `siteId: "default"` mutation bodies inherit the selected editor site, page revisions GET/POST plus rollback POST resolve selected siteId from query/body or editor referrer, and collab comments/presence/cursors/review-marker APIs resolve selected site scope for list/create/update/delete surfaces; revisions GET confirms selected-site page membership before reading global revision documents, draft snapshot POST uses selected-site page canvas record state, rollback creates reversible backup revisions from the selected site's current draft before restoring the requested revision, and collab legacy `siteId: "default"` mutation bodies inherit the selected editor site. Live HTTP/browser proof covers CMS lifecycle, file-backed asset POST -> list -> binary GET -> DELETE -> 404 cleanup, denied/allowed secondary-credential permission paths, desktop/mobile analytics operations rendering without horizontal overflow, a smoke workspace site link opening a ready selected-site editor whose pages/draft/publish-check APIs read the same referrer-selected site, a second smoke site whose navigation/header/footer PUT+GET round-tripped from `/ko/admin-builder?siteId=...` even when mutation bodies carried legacy `siteId: "default"`, `m173-seo-smoke-mqmtklo3` whose SEO settings/checklist markers round-tripped through `/ko/admin-builder?siteId=...`, appeared in preview/overview, updated one selected-site page through bulk SEO, `m173-page-seo-assistant-mqmu0hcv` whose selected-site page focus keyword round-tripped through page SEO assistant while a no-referrer default-site read returned 404, `m173-page-seo-mqmukbx7` whose selected-site page SEO title round-tripped through `/api/builder/site/pages/[pageId]/seo` while a no-referrer default-site read returned 404, `m173-settings-mqmv3t3n-p6ilr` whose selected-site firm name and `darkMode.defaultMode` round-tripped through `/api/builder/site/settings` while a no-referrer default-site read did not leak the selected marker, `m173-lightbox-mqmvkiar-0xhew` whose selected-site lightbox create/list/PATCH/draft PUT+GET/DELETE flow completed while a no-referrer default-site list did not leak the selected slug, `m173-schedule-mqmw0had-u9zxn` whose selected-site page scheduled publish POST/GET/DELETE flow completed while a no-referrer default-site GET did not leak the scheduled job, `m173-revisions-mqmwxa2v-rqda3b` whose selected-site page revision snapshot/list/default-site 404/draft mutation/rollback restoration flow completed while a no-referrer default-site GET returned `builder_page_not_found`, and `m173-collab-1781994471620` whose selected-site comment, presence session, cursor, and review marker round-tripped through `/ko/admin-builder?siteId=...` while no-referrer default-site reads did not leak them. Remaining M173 gap: production deployment smoke.
- **M174 Ops/observability** 🟡: health snapshot collector aggregating audit/error/backup/cache signals, opaque cache key listing with clear-one/all, backup stub register, unified log aggregator (audit/dev/security/error), process perf snapshot, sliding-window security summary, six-tab `/admin-builder/ops` dashboard, M174-A deploy identity health surface, M174-B backup restore integrity verification, M174-C backup restore drill reporting, M174-D cache inventory/purge reporting, and M174-E long-term alert report surface. `OpsHealthSnapshot.deploys` reports source/environment/url/git ref/git SHA from Vercel env with `.next/BUILD_ID` local-build fallback, ops backup records store SHA-256 checksums and reject tampered restores, the `복원 드릴` card runs a non-destructive backup/mutate/restore/checksum/cleanup drill with a persisted latest report, cache ops classify stale/large keys and persist stale purge reports, and the Overview tab now persists `runtime-data/ops/alert-report.json` with open/ok evaluations for security denials, log errors, heap pressure, RSS growth, missing backups, and empty cache state. Proof covers unit projection/tamper/drill/cache/alert-report tests, local production-shaped health/backup/drill/cache/alert API smokes, localized Playwright dashboard assertions, and desktop/mobile screenshot checks without horizontal overflow. Remaining M174 gap: production builder smoke with real credentials.
- **M175 UX/navigation** 🟡: unified admin rail and breadcrumb shell are active across admin sub-surfaces, M175-A records recent localized admin surfaces during normal route changes, M175-B normalizes editor-root quick-jump recent entries saved from other locales back to the current locale label/section/href, M175-C restores mobile editor-root quick-jump access as a compact 34px top-bar chip, and M175-D folds stored app/settings/dashboard deep links into localized parent admin surfaces. The recent trail filters the current section on deeper paths, dedupes by canonical href, and lets users jump back from Ops to Apps/Bookings/settings-style surfaces; the quick-jump modal suppresses stale `/zh-hant`, `/en`, and deep `/apps/installations/.../settings` or `/bookings/dashboard?...` recent links on `/ko/admin-builder`; mobile rail drawer padding prevents the close button from overlapping the editor back link; the mobile quick-jump chip opens the Korean modal at 390px without horizontal overflow. Proof covers unit helper tests, `next start` Playwright for apps → ops → recent-app return, stored deep-link recent normalization, cross-locale quick-jump recent click-through, mobile quick-jump open/search-input visibility, mobile control separation, plus desktop/mobile screenshots with no horizontal overflow. Remaining M175 gap: production builder smoke with real credentials.

All three pass typecheck/lint and ship with unit + Playwright coverage. Storage paths now compute via functions instead of import-time constants so test cwd swaps actually isolate state.

## M172 Multilingual

| ID | Area | Checkpoint | Done when | Status |
| --- | --- | --- | --- | --- |
| F113 | Multilingual | Translation manager dashboard | Admin sees language status across pages, CMS rows, app content, SEO, and media | 🟢 `dashboard-model.ts` computes per-page × per-locale status (untranslated/draft/published/outdated) and returns `coverageSummaries` for CMS column strings, media alt text, and app content. The admin dashboard renders overview cards, localized coverage cards, per-locale CMS/media/app drill-down rows with missing/outdated review links, filter/share controls, live refresh, last-sync status, and the page table; browser proof seeds CMS/media/app entries, verifies coverage cards, follows direct review-query links, then rewrites the backing site document and refreshes ko/zh-hant dashboards from the live API without reload. 2026-07-03 Codex reverified F113 with translation dashboard/query/coverage/app-source/media/dashboard-route Vitest 31/31, Chromium `translations-dashboard.playwright.ts` + `translations-dashboard-drilldown.playwright.ts` 12/12 against `http://127.0.0.1:3191`, targeted ESLint, and `npm run typecheck`; warning override and publish-time policy depth remain tracked outside this dashboard acceptance item. |
| F114 | Multilingual | Manual translations | Editors can edit per-language text/content without overwriting source language | 🟢 `edit-store.ts` applies node-level text patches to target locale draft canvas with content path resolver, preserves rich-text `plainText`/doc formatting when translating marked text into a blank target draft, exposes POST `/api/builder/translations/edit`, and the `TranslationEditor` side-by-side component supports per-locale text, SEO, image overrides, line-segment review/control, source-line apply/remap/append/split/delete/reorder/merge/group actions, rich-text structure review, and per-line Bold/Link/Bullet/list-depth signal chips without overwriting the source locale. HTTP and browser proof covers manual text + SEO override persistence, localized image override reload/public-route rendering, equal-line rich-text mark preservation, adjacent mixed inline mark remapping, adjacent link/underline words, punctuation/terminal/paired/double-quote/square-bracket/curly-brace cases, split/compact separator runs, Unicode/compact dash, nested compact slash list cases, compact colon/semicolon/pipe, guarded ampersand/plus/cross, multi-paragraph blocks, multi-text-node paragraph blocks with link/underline marks, empty paragraphs, hardBreak plus following paragraph blocks, marked bullet items, and ordered-list attrs plus nested bullet-list persistence. 2026-07-03 Codex fixed the public-route media regression by preserving sparse `srcByLocale`/`altByLocale` bags in `imageCanvasNodeSchema`, added a red-first `normalizeCanvasDocument` locale-media regression, and verified F114 with manual translation/editor/rich-text Vitest 92/92, Chromium manual editor/rich-text pack 24/24 against `http://127.0.0.1:3191`, `npm run typecheck`, targeted ESLint, clean touched-file escape scan, `git diff --check`, and `npm run build` with existing warnings only. |
| F115 | Multilingual | Auto-translation adapter | Translation adapter can propose changes with review/apply/rollback behavior | ✅ `auto-translate.ts` extracts translatable nodes with bounded concurrency, the guarded batch route validates multi-entry payloads with Zod, calls the provider router once per batch, and the router uses provider-native OpenAI JSON-array requests or DeepL multi-text form requests before falling back to single calls for providers without batch support. UI Auto-translate snapshots the manual draft before applying proposals with a localized Revert auto-translate action before save, and Translation Manager batch actions now consume a guarded SSE stream that emits provider progress before final batch results, shows an aria-live progress panel while disabling batch buttons during the in-flight request/save phase, and surfaces provider telemetry: provider, native/fallback mode, succeeded/requested count, failed count, provider step details (`provider-selected`, `cache-checked`, `provider-request`, `provider-result`, `provider-response`, plus fallback/mock terminal steps), provider response duration, item-level provider-result progress, ordered SSE event sequence numbers with stale/out-of-order frame guarding, and OpenAI upstream usage token/cost telemetry (`promptTokens`, `completionTokens`, `totalTokens`, and configured-rate `estimatedCostUsd`). The guarded provider-readiness route and Translation Manager panel now report OpenAI/DeepL secret presence, router selection, production mock fallback risk, and admin-triggered provider smoke status without exposing secret values. Browser proof covers proposal fill/rollback, the Translation Manager batch button persisting a mock-provider result, a delayed save request that keeps the Korean progress panel plus completed provider telemetry visible, a live stream progress event that shows `성공 0/1` before final results arrive, provider step telemetry showing `단계 provider-request · 캐시 0 · 요청 1 · 성공 0 · 실패 0 · 응답 42ms`, the real HTTP stream route emitting ordered progress/result sequence frames, the Translation Manager preserving newer telemetry when stale sequence frames arrive, item-level `provider-result` telemetry showing `단계 provider-result · 성공 1/1 · 응답 84ms` before final stream results, OpenAI provider-response telemetry showing `토큰 30 · $0.000013`, and the provider readiness panel showing OpenAI configured/DeepL missing plus `openai 점검 통과 · 42ms` after a mocked smoke call; provider unit proof verifies OpenAI performs one upstream request for two items, DeepL sends one request with multiple `text` fields, router progress emits before native batch resolution, step telemetry is ordered around cache/request/provider-result/response phases, provider-response duration is measured, stream progress/result events are sequence-ordered, OpenAI stream requests include `stream_options.include_usage`, upstream usage survives the SSE/client parser boundary, and the provider-readiness API redacts secrets. Remaining gap: live production smoke with real OpenAI/DeepL credentials and real multi-user production reviewer smoke/reporting depth. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 실 OpenAI/DeepL 크리덴셜 + 실 멀티유저 프로덕션 스모크. Claude loop iter20 기록. |
| F116 | Multilingual | Per-language menu/slug | Menus and slugs are configurable by language with redirects/canonical handling | 🟢 `slugByLocale` field projection via `resolveLocaleSlug`, `findPageMetaForLocaleSlug`, `isLocaleSlugConflict`. `LocaleSlugEditor` embedded in the Translation Editor. PATCH route + page-resolution integration now persists localized slug overrides and browser proof covers the localized public route. 2026-07-02 Claude loop iter15: 격리(BUILDER_SITE_ROOT 사본+4641 prod빌드)에서 unit 증빙(locale-slug/page-resolution 등 28 tests)+브라우저 증빙(translations-editor.playwright 2/2) green 재확인 — 검증 완료로 🟢 전환. 주의: 이 spec은 테스트 프로세스가 persistence를 직접 읽으므로 격리 실행 시 BUILDER_SITE_ROOT를 playwright 프로세스에도 줘야 함(스플릿브레인 방지). |
| F117 | Multilingual | Per-language SEO | SEO title, description, OG, structured data, and sitemap output vary by language | 🟢 New `BuilderSeoMetadata.localizedOverrides` field stored on source page; `seo-projection.ts` returns effective per-locale `{title, description, ogImage}` (override → source fallback); SEO inputs in TranslationEditor persist via `setPageLocaleSeoOverride`; browser proof now verifies the localized public route metadata after publish. 2026-07-02 Claude loop iter15: seo-model unit + translations-editor SEO override 브라우저 증빙 green 재확인(위와 동일 격리 환경) — 검증 완료로 🟢 전환. |
| F118 | Multilingual | Per-language media/assets | Media replacement and alt text can vary by language | 🟢 `srcByLocale` + `altByLocale` projection via `resolveLocaleImageContent`, `projectImageNodeForLocale` (render-time), `applyImageLocaleOverride`, `listImageNodesForLocaleEditor`. `LocaleMediaEditor` lets editors upload/pick per-locale image. Browser proof now covers save/reload and the published public route, so the image projection path is no longer helper-only. |
| F119 | Multilingual | Translation status warnings | Publish warns on missing/stale translations and broken language routes | ✅ `buildTranslationPublishWarnings` detects untranslated/outdated/broken-link issues from the site document, GET `/api/builder/translations/publish-warnings`, and `TranslationPublishWarningsPanel` polls every 30s, renders severity-coded rows, and gives each warning a localized review action that opens the Translation Manager with `sourceLocale`, page category, page-id search, target locale, and missing/outdated/all status preserved. The publish-gate runner now maps current source-page translation warnings into the publish modal preflight: untranslated/outdated issues require warning acknowledgement, broken language-route warnings become blockers, and each translation issue row has a localized `검토`/`Review` action link back into the exact Translation Manager review query. The publish modal now also renders a localized warning override review panel with pending/acknowledged state, warning count, category chips, and direct/scheduled publish disabled until acknowledgement, plus a site-wide translation review panel that summarizes current-page vs other-page warnings, links to the full Translation Manager review, and requires separate acknowledgement of other-page translation warnings before direct or scheduled publish can proceed. Acknowledged direct and scheduled publish requests now persist the site-wide translation review snapshot as `publish.translation_site_review` audit events, including action, source locale, synced timestamp, warning/error counts, current-page/other-page counts, review link, and scheduled job metadata when applicable. Organization-level translation release policy now has guarded GET/PUT settings, file/blob-backed persistence, a `block-other-page-warnings` mode that augments publish checks with a translation blocker, a localized modal policy panel, and direct/scheduled publish server enforcement that returns a stable 409 payload before writes. Role-scoped translation release approvals now extend that policy with `approvalRequiredForRoles`, stable warning fingerprints, guarded request/approve/reject APIs, latest-context approval lookup, publish-check required/pending blockers, a localized modal request panel, and direct/scheduled 409 enforcement before writes. Site settings now includes a guarded approval-role settings panel that saves required roles, lists pending approval requests with status filtering, and lets a settings reviewer approve/reject requests from the same modal without leaving the builder. Browser proof verifies ko/zh-hant warning actions, a source-only ko page showing the localized `번역` preflight warning bucket, publish-modal row action handoff, pending review, page-warning acknowledgement, still-disabled publish/schedule actions while site-wide other-page review is pending, site-wide acknowledgement, enabled publish/schedule actions, site-wide total/other-page review context afterward, a scheduled-publish audit event containing the acknowledged site-wide review metadata, the organization policy blocker keeping publish/schedule disabled even after site-wide acknowledgement, an owner role approval request moving the modal from required to pending while publish/schedule stay disabled, and the settings panel saving Owner/Admin role requirements while approving the pending request from the reviewer queue. Remaining gap: real multi-user production reviewer smoke/reporting depth. 2026-07-02 사용자 승인 조건부 waive(배포 후 실환경 검증 조건) — 로컬 검증 불가: 실 멀티유저 프로덕션 리뷰어 스모크. Claude loop iter20 기록. |
| F120 | Multilingual | App translation compatibility | First-party/native apps expose translatable strings and content to the manager | 🟢 Existing app translation sync (M162) shipped via `translations/sync.ts`; browser proof now covers app settings plus native FAQ content review/apply, the translation manager shell/dashboard/editor chrome is localized for ko and zh-hant, and the manager can translate first-party app strings and content end to end. |

2026-05-31 M165-CS update: builder workspace page/starter shell localization now localizes the page-mode pills, page list status chips, canvas-focus copy, mode policy labels, publish-readiness labels, page diagnostics disclosure, dataset seam labels, publish warning copy, and starter-template shell/action labels for ko and zh-hant, so the localized builder workspace no longer exposes those English shells. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-DH update: booking services editor localization now localizes the visible services toolbar, card summaries, payment/deposit labels, reminder schedule, resource warnings, and create/edit modal controls for ko and zh-hant, so the services editor no longer exposes English body copy on localized bookings admin pages. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-DO update: member booking detail package-credit status localization now maps package-credit statuses to ko/zh-hant/en display labels before rendering the detail page, so the member booking detail route no longer exposes the raw internal package-credit status string on localized pages. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-DQ update: currency settings admin localization now localizes the route metadata title, visible shell title/subtitle, policy banner, stats, controls, matrix, status labels, and manual preview-rate flow for ko and zh-hant, so the currency settings route no longer exposes English admin copy. F72 remains yellow for actual conversion/provider QA.
2026-05-31 M165-DR update: payment analytics admin localization now localizes the route title, hero summary, KPI labels, alerts, source quality/funnel panels, live reconciliation, provider fee reconciliation, trend chart, provider mix, and provider fee panels for ko and zh-hant, so the payment analytics route no longer exposes English analytics shell copy. F74 remains yellow for deeper trend/provider-fee/provider QA work.
2026-05-31 M165-DW update: CRM admin localization now localizes the route metadata title, visible shell title/summary, tablist aria-label, and tab labels for ko and zh-hant, so the CRM route no longer exposes English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-DX update: AI generator shell localization now localizes the route metadata title, visible heading/description/badge, and dashboard launcher aria-label/title for ko and zh-hant, so the AI generator route and launcher no longer expose English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-DY update: Redirect manager shell localization now localizes the route metadata title, visible heading/summary, create/search chrome, empty states, row labels, wildcard/help text, and action buttons for ko and zh-hant, so the redirect manager route no longer exposes English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EA update: Builder columns admin shell localization now localizes the route metadata title/description and the visible list shell title, description, quick-navigation labels, stats, sidebar, search/status controls, empty state, and new-post button for ko and zh-hant, so the columns admin route no longer exposes English list-shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EB update: Builder columns edit surface localization now localizes the route metadata title, not-found state, quick-navigation/breadcrumb labels, editor topbar actions, save-state text, editor placeholder, publish alerts, summary chrome, frontmatter panels, locale-linker panel, and editor flow assertions for ko and zh-hant, so the columns edit route no longer exposes English editor chrome. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EC update: Builder users admin shell localization now localizes the route metadata title/description and the visible shell title/eyebrow, description, add-user form, table headers, permission matrix, role labels, and action copy for ko and zh-hant, so the users/roles admin route no longer exposes English admin shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-ED update: Builder secrets admin shell localization now localizes the route metadata title/description and the visible shell title/description, refresh control, list/table chrome, create form, per-secret actions, and reveal modal for ko and zh-hant, so the secrets admin route no longer exposes English admin shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EE update: Builder forms-flow shell localization now localizes the route metadata title/description and the visible heading/body/empty state/card labels for ko and zh-hant, so the forms-flow route no longer exposes English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EF update: Builder workspace admin shell localization now localizes the route metadata title/description, workspace eyebrow, section rail labels, overview stats, account metadata, and the sites/members/shared-assets/analytics panel copy for ko and zh-hant, so the workspace admin route no longer exposes English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EG update: Commerce notifications admin shell localization now localizes the route metadata title/description, visible shell title/subtitle, nav links, stats/settings/templates headings, control labels, payment-rules copy, outbox/recovery headings, and refresh/save/notice text for ko and zh-hant, so the commerce notifications admin route no longer exposes English shell copy. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EH update: Commerce payment webhook list label localization now localizes the payment-webhooks list aria-label via shared ko/zh-hant/en copy and keeps the existing shell coverage green, so the payment-webhooks admin surface no longer exposes that English accessible label. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EI update: Commerce product manager control localization now localizes the variant remove button, SEO labels, inventory/backorder checkboxes, product-row action buttons, export/import controls, and variant fallback label for ko and zh-hant, so the product manager admin surface no longer exposes raw English controls. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EM update: SEO dialog localization now localizes the basics, social, advanced, hreflang, and assistant tabs for ko and zh-hant, with a server-rendered unit test covering the localized dialog copy. The browser publish-history suite still times out on page-creation setup, so the browser proof remains pending.
2026-05-31 M165-EO update: Publish modal localization now centralizes the modal shell, diff summary, preflight labels, schedule state, toasts, and error strings in a shared ko/zh-hant/en copy helper, and the modal/preflight unit test covers the localized copy. The browser publish-history suite is still blocked by Chromium launch permission errors in this environment, so the browser proof remains pending.
2026-05-31 M165-ER update: Builder/dev code assistant localization now centralizes the assistant shell, action labels, diff/review labels, hunk labels, selected-hunk summaries, and fallback states in a shared ko/zh-hant/en copy helper, and the helper unit test covers the localized strings. The focused dev-functions browser run is still blocked by Chromium launch permission errors in this environment, so the browser proof remains pending.
2026-05-31 M165-EX update: Public FAQ list widget localization now localizes the all/category/search/loading/no-results/empty-state labels for ko and zh-hant, so the public FAQ list widget no longer exposes raw English chrome. F83 remains yellow for broader site/app locale copy.
2026-05-31 M165-EY update: Public site search localization now localizes the fallback placeholder/button text and builder inspector labels for ko and zh-hant, so the site-search component no longer leaks raw English fallback/inspector copy. F83 remains yellow for broader site/app locale copy.
2026-07-03 M165-FB update: Customer booking manage-link API error localization now returns localized stable-code payloads for ko/zh-hant/en across invalid/expired links, missing bookings, rate limits, policy blocks, invalid updates, slot locks, unavailable slots, and staff-unavailable failures; focused route/helper tests plus live HTTP proof on the zh-hant invalid-link surface passed. F83 is now green for timezone/localization.
- `M165-EZ`: Public member-login localization now localizes the visible eyebrow, fallback text, and builder inspector labels for ko and zh-hant.
- `M165-FA`: Public member-account-summary localization now localizes the eyebrow, role/loading/guest/error copy, link labels, and builder inspector labels for ko and zh-hant.
- `M166-GG`: Builder container and gallery inspector localization now localizes the shared container and gallery inspector labels, layout options, card variants, and published gallery filter labels for ko and zh-hant.
- `M166-GH`: Image edit shell localization now localizes the shared image inspector and edit-dialog shell labels, buttons, tabs, and preview controls for ko and zh-hant.
- `M166-GI`: Blog post card inspector localization now localizes the post selector, card-style selector, display toggles, and variant labels for ko and zh-hant.
- `M166-GJ`: Blog feed inspector and runtime localization now localizes the inspector labels, layout/sort options, loading/error/empty-state strings, read-more text, and category labels for ko and zh-hant.
- `M166-GK`: Blog categories inspector and archive localization now localizes the inspector labels, the public `All` category label, the archive shell, and the archive empty-state text for ko and zh-hant.
- `M166-GL`: Blog recent posts runtime localization now localizes the loading and empty-state strings for ko and zh-hant.
- `M166-GM`: Featured posts inspector and runtime localization now localizes the inspector labels, layout options, empty-state string, category prefix, and featured marker for ko and zh-hant.
- `M166-GN`: Link picker localization now localizes the shared builder link picker shell plus the button/image/container/text inspector callsites that render it for ko and zh-hant.
- `M166-GO`: Brand kit panel localization now localizes the brand asset count, open-assets button, linked/pick states, and radius-scale label for ko and zh-hant.
- `M166-GP`: Canvas shell chrome localization now localizes the workspace header badge, global canvas back/add/layers/save-state labels, and lightbox top-bar labels for ko and zh-hant.
- `M166-GQ`: Lightbox settings panel localization now localizes the lightbox settings heading, size mode, dimensions, and close behavior labels for ko and zh-hant.
- `M166-GR`: Repeater template chrome localization now localizes the repeater template badge, warning, HUD labels, actions, and fallback text for ko and zh-hant.
- `M166-GS`: Shortcut chrome localization now localizes the editor prefs popover, keybindings modal, shortcut help modal, and canvas node badge for ko and zh-hant.
- `M166-GT`: Inspector controls localization now localizes the mixed-value badge, viewport override badge, swatch row, slider row, toggle row, advanced disclosure, and inspector section buttons for ko and zh-hant.
- `M166-GU`: Inspector controls badge/toggle refinement now localizes the color picker, adjust-value slider label, theme-binding badge states, detached/custom color titles, and bound-to helper string for ko and zh-hant.
- `M166-GV`: Sandbox editor rail localization now localizes the rail buttons, design drawer headings, designer audit labels, columns drawer labels, and history chrome for ko and zh-hant.
- `M166-GW`: Site settings presets localization now localizes the component design presets, design token bundle, radius/shadow presets, My Themes section, pending preset dialog, and saved-theme actions for ko and zh-hant.
- `M166-GX`: Site settings typography preview localization now localizes the typography panel headings, preview row labels, and preview sample text for ko and zh-hant.
- `M166-GY`: Site settings color token localization now localizes the advanced/dark-mode/typography palette color labels and validation messages for ko and zh-hant.
- `M166-GZ`: Event and portfolio widget runtime localization now localizes event-list, event-calendar, and portfolio-list loading/empty/filter/capacity/action labels for ko and zh-hant.
- `M166-HA`: Event RSVP widget runtime localization now localizes RSVP ticket, capacity, empty-state, form-label, error, and submit-state copy for ko and zh-hant.
- `M166-HB`: Notification bar widget localization now localizes the runtime dismiss aria-label plus inspector field labels, tone options, position options, and dismiss-toggle copy for ko and zh-hant.
- `M166-HC`: Service feature card inspector localization now localizes the service-card field labels and variant options for ko and zh-hant.
- `M166-HD`: Floating chat inspector localization now localizes provider labels, link/label/color fields, placement options, and show-label toggle copy for ko and zh-hant.
- `M166-HE`: Social widgets localization now localizes social-bar, share-button, and social-embed provider/layout/style labels, runtime placeholders, and key accessible labels for ko and zh-hant.
- `M166-HF`: Navigation/decorative widget localization now localizes breadcrumbs, timeline, and pattern inspector labels/options plus breadcrumbs aria-label and timeline empty-state copy for ko and zh-hant.
- `M166-HG`: Media widgets localization now localizes audio, video, and Lottie inspector labels/options plus runtime fallback/helper/accessibility copy for ko and zh-hant.
- `M166-HH`: Visual widget inspector localization now localizes icon, divider, shape, frame, and sticker inspector labels/options plus theme color-token labels for ko and zh-hant.
- `M166-HI`: Data widgets localization now localizes bar chart, line chart, pie chart, and counter inspector labels/toggles/placeholders plus chart aria fallbacks and line-chart empty-state copy for ko and zh-hant.
- `M166-HJ`: Advanced form runtime localization now localizes form step controls, captcha/validation fallback messages, payment widget runtime/inspector chrome, and signature widget runtime/inspector chrome for ko and zh-hant.
- `M166-HK`: Location widgets localization now localizes address-block, business-hours, multi-location-map, and map runtime, inspector, empty-state, preset, and accessibility chrome for ko and zh-hant.
- `M166-HL`: Interactive widgets localization now localizes countdown, back-to-top, progress, and rating inspector labels/options plus countdown segment labels and progress/rating accessible labels for ko and zh-hant.
- `M166-HM`: Conversion widgets localization now localizes contact-form, CTA-banner, and custom-embed runtime fallback states, visitor-facing labels, inspector controls, and embedded-content accessibility chrome for ko and zh-hant.
- `M166-HN`: Marketing data widgets localization now localizes pricing-table, comparison-table, team-member-card, and testimonial-carousel empty states, inspector labels/options, card variant labels, and testimonial navigation accessibility labels for ko and zh-hant.
- `M166-HO`: Member account widgets localization now localizes member-login, member-account-summary, member-profile-form, and member-bookings-list default titles/actions, inspector labels, auth-tab accessibility labels, account role labels, and booking status badges for ko and zh-hant.
- `M166-HP`: Form field defaults localization now localizes first-party form input, textarea, select, radio, checkbox, and submit runtime legacy defaults plus inspector placeholders/options for ko and zh-hant.
- `M166-HQ`: Utility advanced widgets localization now localizes code-block, video-embed, anchor-menu, and parallax-background runtime defaults, inspector labels/options, code empty states, anchor accessibility labels, and parallax default title/subtitle for ko and zh-hant.
- `M166-HR`: FAQ list inspector localization now localizes FAQ source/category/limit/toggle/item controls, placeholders, add/remove actions, and category option labels for ko and zh-hant.
- `M166-HS`: Booking widget inspector localization now localizes booking-widget runtime legacy defaults, preview completion text, locale selector labels, inspector sections, field labels, placeholders, and booking-form toggles for ko and zh-hant.
- `M166-HT`: Product gallery runtime localization now localizes product-gallery loading/error/empty states, category aria/filter labels, sort options, availability labels, pagination, quick-view chrome, and builder-preview mock products/categories for ko and zh-hant.
- `M166-HU`: Domain card widget localization now localizes column-card, column-list, and attorney-card empty placeholders, inspector labels, locale options, card variant options, category/limit controls, and attorney specialty/photo fields for ko and zh-hant.
- `M166-HV`: Layout navigation widget localization now localizes section, spacer, and menu-bar inspector labels, edit placeholders, menu aria labels, mobile open/close labels, empty state, localized default menu items/hrefs, and menu option labels for ko and zh-hant.
- `M166-HW`: Heading inspector option localization now localizes heading typography option labels, letter-spacing label, and alignment labels/options by reusing the shared text-controls copy for ko and zh-hant.
- `M166-HX`: Gallery runtime aria localization now localizes gallery slider navigation, dot navigation, thumbnail selection, and lightbox accessible labels for ko and zh-hant.
- `M166-HY`: Blog post card runtime localization now localizes blog-card featured badges, select/loading/unavailable states, fallback mock card content, category fallback, reading-time text, read-more label, and card aria label for ko and zh-hant.
- `M166-HZ`: Button inspector localization now localizes button label/link/style/tag/class labels, link helper text, HTML tag options, class hints, and button variant option labels for ko and zh-hant.
- `M166-IA`: Blog author runtime localization now localizes blog-author loading/empty states, fallback author names, and builder mock author/post content for ko and zh-hant.
- `M166-IB`: Blog feed runtime fallback localization now localizes blog-feed load-error copy, builder mock author names, and mock post title/excerpt/tag data for ko and zh-hant.
- `M166-IC`: CTA widget legacy default localization now localizes notification-bar and service-feature-card default message/title/description/CTA label/href values for ko and zh-hant.
- `M166-ID`: Pricing table legacy plan localization now localizes untouched pricing-table default plan names, prices, features, CTA labels, and CTA hrefs for ko and zh-hant while preserving custom plans.
- `M166-IE`: Comparison table legacy default localization now localizes untouched comparison-table default columns and feature rows for ko and zh-hant while preserving custom tables.
- `M166-IF`: Sticker legacy default localization now localizes untouched sticker default labels for ko/zh-hant/en while preserving custom sticker labels.
- `M166-IG`: Gallery fallback metadata localization now localizes empty-gallery preview fallback captions, alt text, and filter tags for ko/zh-hant/en while preserving custom gallery images.
- `M166-IH`: Breadcrumbs legacy default localization now localizes untouched breadcrumbs home/item labels and default item hrefs for ko/zh-hant/en while preserving custom breadcrumbs.
- `M166-II`: Floating chat legacy label localization now localizes untouched floating-chat default labels for ko/zh-hant/en while preserving custom labels and URL safety behavior.
- `M166-IJ`: Form root legacy message localization now localizes untouched form success messages and auto-reply placeholders for ko/zh-hant/en while preserving custom success messages.
- `M166-IK`: Event widget mock/RSVP default localization now localizes event-list/calendar/RSVP preview mock content and untouched RSVP title/success defaults for ko/zh-hant/en.
- `M166-IL`: Timeline legacy default localization now localizes untouched timeline default items for ko/zh-hant/en while preserving custom timeline items.
- `M166-IM`: Share buttons legacy title localization now localizes untouched share-buttons default titles for ko/zh-hant/en while preserving custom share titles.
- `M166-IN`: Data widget legacy defaults localization now localizes untouched bar/line/pie chart titles/data labels and counter titles/suffixes for ko/zh-hant/en while preserving custom data.
- `M166-IO`: Interactive widget legacy label localization now localizes untouched countdown, back-to-top, progress, and rating labels/expired text for ko/zh-hant/en while preserving custom labels.
- `M166-IP`: Location widget legacy defaults localization now localizes untouched address, business-hours, and multi-location defaults for ko/zh-hant/en while preserving custom content.
- `M166-IQ`: Site search legacy placeholder localization now localizes untouched search placeholders and submit labels for ko/zh-hant/en while preserving empty fallback behavior and custom search text.
- `M166-IR`: Portfolio list mock project localization now localizes builder preview mock project content for ko/zh-hant/en.
- `M166-IS`: Blog recent posts mock localization now localizes builder preview mock post titles, excerpts, and author names for ko/zh-hant/en.
- `M166-IT`: Featured posts mock localization now localizes builder preview mock featured-post titles and excerpts for ko/zh-hant/en.
- `M166-IU`: Team member card legacy default localization now localizes untouched default names, roles, and bios for ko/zh-hant/en while preserving custom team member content.
- `M166-IV`: Testimonial carousel legacy default localization now localizes untouched default reviewer names, roles, and quotes for ko/zh-hant/en while preserving custom testimonial items.
- `M166-IW`: Form payment legacy default localization now localizes untouched payment labels and descriptions for ko/zh-hant/en while preserving custom payment content.
- `M166-IX`: Form signature legacy default localization now localizes untouched signature labels and help text for ko/zh-hant/en while preserving custom signature content.
- `M166-IY`: Member login inspector legacy default localization now localizes untouched titles, subtitles, auth labels, and account-path placeholders for ko/zh-hant/en while preserving custom member login content.
- `M166-IZ`: Member account inspector legacy default localization now localizes untouched summary/profile/bookings text and account/login placeholders for ko/zh-hant/en while preserving custom member account content.
- `M166-JA`: Form field inspector/runtime legacy default localization now localizes untouched field labels, select placeholders/options, submit loading labels, and date/file labels for ko/zh-hant/en while preserving custom field content.
- `M166-JB`: CTA banner locale-aware link placeholder now localizes button-link placeholders for ko/zh-hant/en so non-Korean inspectors no longer show `/ko/contact`.
- `M166-JC`: Image hotspot placeholder locale guard now covers zh-hant/en image hotspot placeholder copy and rendered inspector markup so non-Korean inspectors cannot regress to `/ko/contact`.
- `M166-JD`: Text bullet list fallback localization now uses locale-specific fallback list items for ko/zh-hant/en when the text inspector creates a bullet list from empty text.
- `M166-JE`: Video embed runtime fallback localization now localizes empty URL, invalid URL, and iframe title copy for ko/zh-hant/en.
- `M166-JF`: Map inspector address placeholder localization now uses ko/zh-hant/en map address examples instead of a hard-coded Traditional Chinese placeholder.
- `M166-JG`: Composite fallback diagnostic localization now localizes missing-composite and insights-unavailable canvas fallback copy for ko/zh-hant/en.
- `M166-JH`: Button legacy default label localization now localizes untouched `버튼` labels for ko/zh-hant/en runtime and inspector surfaces while preserving custom labels.
- `M166-JI`: Heading legacy default text localization now localizes untouched `헤딩을 입력하세요` heading text/rich text for ko/zh-hant/en runtime and inspector surfaces.
- `M166-JJ`: Text legacy default text localization now localizes untouched `텍스트를 입력하세요` text/rich text for ko/zh-hant/en runtime, input placeholder, marquee/text-path, and inspector surfaces.
- `M166-JK`: Booking widget active locale fallback now leaves new widget locale unset and falls back to the active builder locale until a widget locale is explicitly selected.
- `M166-JL`: CTA widget default content legacy constants now seed notification-bar and service-feature-card defaults from shared legacy constants instead of duplicated component literals.
- `M166-JM`: Utility widget default content legacy constants now seed anchor-menu labels and parallax background text from shared legacy constants instead of duplicated component literals.
- `M166-JN`: Menu bar default items legacy constant now seeds menu-bar default labels and `/ko` hrefs from the shared layout/navigation default items.
- `M166-JO`: Contact form legacy submit default localization now owns the legacy `Submit` label in conversion copy and localizes untouched submit labels in runtime and inspector surfaces.
- `M166-JP`: Site search schema legacy default localization now treats older `검색...` and `Search` schema defaults as localizable legacy search copy and aligns schema defaults with canonical Korean values.
- `M166-JQ`: Lottie empty hint spacing localization now uses explicit locale copy spacing metadata instead of translated-string prefix checks.
- `M166-JR`: Image runtime modal copy localization now moves lightbox/popup close labels, popup dialog labels, and popup fallback text into locale-aware image runtime copy.
- `M166-JS`: Image placeholder and compare copy localization now moves placeholder aria/text, SVG icon labels, before/after alt/range labels, and edit-overlay text into locale-aware image runtime copy.
- `M166-JT`: Image inspector placeholder copy localization now moves source, hover, SVG color, and GIF search placeholders into locale-aware image inspector copy.
- `M166-JU`: Video inspector placeholder copy localization now moves video URL/poster placeholders into locale-aware media widget inspector copy.
- `M166-JV`: Conversion inspector copy cleanup now localizes contact-form action URL labels and centralizes custom-embed HTML placeholders in conversion widget copy.
- `M166-JW`: Form root inspector placeholder copy localization now moves form name, target email, webhook, redirect, and steps JSON placeholders into form control copy.
- `M166-JX`: Blog post card manual slug placeholder copy now moves the manual postId placeholder into blog post card inspector copy.
- `M166-JY`: Contact form action placeholder constant now uses the shared contact-form legacy action default for inspector placeholder rendering.
- `M166-JZ`: Lottie URL placeholder copy now moves the Lottie embed URL placeholder into media widget inspector copy.
- `M166-KA`: Container anchor placeholder copy now moves the anchor target placeholder into container/gallery inspector copy.
- `M166-KB`: Blog categories color placeholder copy now moves the final implementation inspector placeholder into blog categories copy.
- `M166-KC`: Audio legacy default title localization now maps untouched `Audio track` titles to locale-specific audio fallback titles in runtime and inspector surfaces.
- `M166-KD`: Container hover fallback localization now moves the hover-box missing-description fallback into container/gallery runtime copy.
- `M166-KE`: Component catalog display name localization now renders Add-panel built-in component names from locale-aware catalog copy for ko/zh-hant/en while preserving kind and legacy display-name search.
- `M166-KF`: Add panel catalog chrome localization now moves catalog headings, search/result copy, widget-pack headings, category labels, quick-add actions, drag titles, and empty states into locale-aware ko/zh-hant/en helpers.
- `M166-KG`: Text widget preset card copy localization now localizes every text widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KH`: Media widget preset card copy localization now localizes every media widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KI`: Gallery widget preset card copy localization now localizes every gallery widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KJ`: Layout widget preset card copy localization now localizes every layout widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KK`: Interactive widget preset card copy localization now localizes every interactive widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KL`: Navigation widget preset card copy localization now localizes every navigation widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KM`: Social widget preset card copy localization now localizes every social widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KN`: Location widget preset card copy localization now localizes every location widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KO`: Decorative widget preset card copy localization now localizes every decorative widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KP`: Designer widget preset card copy localization now localizes every designer widget pack card label/description for ko/zh-hant/en while preserving legacy preset names in search keywords.
- `M166-KQ`: Built-in section panel chrome localization now localizes the section library eyebrow, market name, all-packs filter, category labels, empty state, and section-card add title/aria copy for ko/zh-hant/en.
- `M166-KR`: Saved sections panel chrome localization now localizes the saved-section library title, refresh/loading/empty/error states, category labels, card tooltip, usage text, and insert/rename/delete actions for ko/zh-hant/en.
- `M166-KS`: Page template preview metadata localization now localizes page-template page type, visual style, quality tier, and section-count metadata for ko/zh-hant/en.
- `M166-KT`: Built-in section template card copy localization now localizes built-in section template card names/descriptions/hints for ko/zh-hant/en while preserving legacy template data in search.
- `M166-KU`: Page template preview name/tag localization now renders Add-panel page-template card names and known tags from locale-aware category/subcategory/tag copy for ko/zh-hant/en while preserving raw names/tags in search.
- `M166-KV`: Template gallery modal localization now localizes full page-template gallery chrome, filters, category labels, card/preview names, descriptions, tags, CTA goals, and section summaries for ko/zh-hant/en while preserving raw template search and canonical selection names.
- `M166-KW`: Page template thumbnail overlay localization now localizes thumbnail footer names, style/page-type meta, and premium badges for ko/zh-hant/en, with locale included in thumbnail cache keys.
- `M166-KX`: Page switcher member access copy localization now localizes Pages drawer member-access menu labels, badges, redirect presets, dialog labels, page picker text, and save/cancel states for ko/zh-hant/en.
- `M166-KY`: Page switcher slug prompt copy localization now localizes the page creation slug dialog label/title, template/blank descriptions, navigation checkbox, choose-template-back action, and create states for ko/zh-hant/en.
- `M166-KZ`: Page switcher quick/empty card copy localization now localizes Pages drawer missing-page creation, columns quick links, dynamic-list quick-create, and empty-page state copy for ko/zh-hant/en.
- `M166-LA`: Page switcher rename/status copy localization now localizes Pages drawer loading, fetch/create/delete/rename/order/member-access fallback messages, delete confirmation, rename redirect hints, reorder aria text, and generated dynamic page titles for ko/zh-hant/en.
- `M166-LB`: Page switcher header/clipboard copy localization now localizes the Pages drawer title, add-page button, busy label, and clipboard element-count pill for ko/zh-hant/en.
- `M166-LC`: Page switcher missing member page title localization now centralizes missing-page title resolution and gives account/profile and account/bookings locale-specific titles instead of generic untitled fallbacks.
- `M166-LD`: Page switcher member starter eyebrow localization now localizes the generated missing member page starter eyebrow for ko/zh-hant/en instead of inserting English-only `MEMBER AREA`.
- `M166-LE`: Page switcher member starter hero copy source now moves generated missing member page heading/body/CTA copy into tested ko/zh-hant/en copy helpers.
- `M166-LF`: Page switcher member starter setup copy source now moves generated missing member page setup-card title/checklist copy into tested ko/zh-hant/en copy helpers.
- `M166-LG`: Page switcher member starter widget intro copy source now moves generated missing member login/account/profile/bookings widget subtitle/title defaults into tested ko/zh-hant/en copy helpers.
- `M166-LH`: Page switcher member starter widget label copy source now moves generated missing member login/signup, account navigation, profile form, and bookings empty/status labels into tested ko/zh-hant/en copy helpers.
- `M166-LI`: Page switcher reorder handle title localization now moves the page drag/keyboard reorder hover title into tested ko/zh-hant/en copy helpers.
- `M166-LJ`: AI section generator panel copy localization now moves section-generator errors, labels, placeholders, actions, preview node counts, kind labels, and fallback notices into tested ko/zh-hant/en copy helpers.
- `M166-LK`: Sandbox status bar copy localization now moves viewport, selection, save-state, density, theme, and shortcut labels into tested ko/zh-hant/en copy helpers.
- `M166-LL`: AI text assistant panel copy localization now moves text-assistant errors, action/tone/target-language labels, placeholders, history controls, preview headers, and apply/reset actions into tested ko/zh-hant/en copy helpers.
- `M166-LM`: Responsive AI panel copy localization now moves responsive assistant request errors, viewport/reason labels, intro text, action buttons, applied-state labels, and empty-state copy into tested ko/zh-hant/en copy helpers.
- `M166-LN`: Canvas stage toolbar copy localization now moves grid, grid-size, undo, redo, title, and accessibility labels into tested ko/zh-hant/en copy helpers.
- `M166-LO`: Canvas empty-state copy localization now moves empty-page guidance title/body into tested ko/zh-hant/en copy helpers.
- `M166-LP`: Layers panel copy localization now moves layer drawer chrome, node counts, empty guidance, drag/drop hints, kind fallback labels, and search input labels into tested ko/zh-hant/en copy helpers.
- `M166-LQ`: Canvas zoom dock copy localization now moves zoom out/in, fit-to-screen, and slider accessibility labels into tested ko/zh-hant/en copy helpers.
- `M166-LR`: Editor preferences dialog aria localization now renders the preferences popover aria label from the existing tested ko/zh-hant/en editor preferences copy.
- `M166-LS`: Element comments panel copy localization now moves inspector comment empty states, titles, form labels, action labels, resolved/default-author labels, and date-time locale hints into tested ko/zh-hant/en copy helpers.
- `M166-LT`: Context menu top action copy localization now moves multi-select/node titles plus text, image, alt, link edit, and link removal labels into tested ko/zh-hant/en copy helpers.
- `M166-LU`: Context menu clipboard arrange copy localization now moves copy/cut/paste/duplicate, style clipboard, bring/send order, and lock/unlock labels into tested ko/zh-hant/en copy helpers.
- `M166-LV`: Context menu alignment copy localization now moves align, distribute, and match-size labels/titles into tested ko/zh-hant/en copy helpers.
- `M166-LW`: Context menu viewport state copy localization now moves hide-by-device, pin, anchor, animation, and effect labels/titles into tested ko/zh-hant/en copy helpers.
- `M166-LX`: Context menu library style copy localization now moves move-to-page, save-as-section, library, component, style override, and reset-style labels/titles into tested ko/zh-hant/en copy helpers.
- `M166-LY`: Context menu group delete copy localization now moves group, ungroup, and delete labels/titles into tested ko/zh-hant/en copy helpers, leaving the context menu component without direct user-facing menu copy.
- `M166-LZ`: Inspector status Z-order copy localization now moves the single-selection SEO notice, hidden/locked hints, stacking labels, and send/bring/duplicate action copy into tested ko/zh-hant/en copy helpers.
- `M166-MA`: Inspector header multi-select copy localization now moves panel aria/title/toggle labels, multi-selection notices, common property labels, duplicate-selection controls, alignment/distribution labels, and match-size labels into tested ko/zh-hant/en copy helpers.
- `M166-MB`: Inspector tab copy localization now moves tab labels/titles and the active-section tab label into typed ko/zh-hant/en copy helpers.
- `M166-MC`: Inspector selected kind label localization now reuses the existing Layers drawer ko/zh-hant/en kind labels for selected-node kind display in the inspector header and active-section header.
- `M166-MD`: Inspector empty composite copy localization now moves empty-selection guidance and composite slot editor title/close/placeholder copy into tested ko/zh-hant/en inspector copy helpers.
- `M166-ME`: Inspector layout viewport copy localization now moves viewport selector labels, helper messages, override banners, reset labels, hidden-override notices, and show-on-device toggle aria/title copy into tested ko/zh-hant/en layout-tab copy helpers.
- `M166-MF`: Inspector layout field state copy localization now moves position/size/font-size field labels, field aria/title copy, flow-layout notice, rotation/state/pin controls, sticky origin labels, and anchor guidance into tested ko/zh-hant/en layout-tab copy helpers.
- `M166-MG`: Inspector data binding copy localization now moves dataset binding panel chrome, field-control labels, preview states, stale-field warnings, filter/sort summaries, and repeater child-template binding actions into tested ko/zh-hant/en copy helpers.
- `M166-MH`: Inspector repeater quick edit copy localization now moves service/FAQ repeater quick-edit section titles, notices, and form labels into tested ko/zh-hant/en copy helpers.
- `M166-MI`: Inspector office quick edit copy localization now moves office sync section labels, preset aria labels, field labels, synced-value aria labels, and generate-from-address action copy into tested ko/zh-hant/en copy helpers.
- `M166-MJ`: Inspector accessibility panel copy localization now moves a11y pass/summary/page-kind labels plus checker rule messages and suggestions into tested ko/zh-hant/en copy helpers.
- `M166-MK`: Inspector content fallback copy localization now moves the missing-content-inspector fallback into tested ko/zh-hant/en editor copy helpers.
- `M166-ML`: Inspector animations tab copy localization now moves animation section chrome, controls, hints, Lottie guidance, and preset option labels into tested ko/zh-hant/en editor copy helpers.
- `M166-MM`: Motion timeline editor copy localization now moves scroll-bound, duration, timeline removal, keyframe track, placeholder, marker title, opacity aria, and remove-keyframe labels into tested ko/zh-hant/en editor copy helpers.
- `M166-MN`: Inspector style source/border copy localization now moves style-source visualizer labels, origin chips/hints, button variant badges, background labels, and border controls into tested ko/zh-hant/en editor copy helpers.
- `M166-MO`: Inspector style shadow/hover copy localization now moves shadow controls, hover state controls, hover adjustment labels, and related number-stepper aria labels into the tested ko/zh-hant/en style-tab copy helper.
- `M166-MP`: Background editor copy localization now moves background modes, solid/gradient/image controls, asset loading states, image placement options, overlay labels, and stop aria labels into tested ko/zh-hant/en editor copy helpers.
- `M166-MQ`: Advanced color picker copy localization now moves color popover chrome, palette/recent labels, native input labels, EyeDropper states, contrast badges, theme-token labels, and color binding badges into tested ko/zh-hant/en editor copy helpers.
- `M166-MR`: Asset library chrome copy localization now moves folder/search/sort/upload/tag/drop-zone chrome into tested ko/zh-hant/en editor copy helpers and fixes the tag-create action to use localized copy.
- `M166-MS`: Asset library grid action copy localization now moves image-card use/delete/deleting actions into tested ko/zh-hant/en editor copy helpers.
- `M166-MT`: Asset library modal copy localization now moves dialog/header/empty/error/retry/upload copy and built-in folder labels into tested ko/zh-hant/en editor copy helpers.
- `M166-MU`: Brand kit asset label copy localization now moves light-logo, dark-logo, favicon, and OG-image labels into tested ko/zh-hant/en text control copy helpers.
- `M166-MV`: Brand kit color label copy localization now moves color token labels and swatch aria text into tested ko/zh-hant/en text control copy helpers.
- `M166-MW`: Brand kit Traditional Chinese copy polish now removes English Site Settings/Logo/Favicon terms from helper-owned zh-hant brand kit strings.
- `M166-MX`: Image edit AI dialog copy localization now moves prompt placeholders, preset prompts, generated-edit notices, review restore messages, preview fallback alt, and mask descriptions into tested ko/zh-hant/en image edit copy helpers.
- `M166-MY`: Image edit crop/filter preset copy localization now moves focal preset labels, focal preset aria text, and filter preset button labels into tested ko/zh-hant/en image edit copy helpers while replacing raw English filter-label branching with stable preset keys.
- `M166-MZ`: Image edit aspect ratio copy localization now gives aspect-ratio presets stable keys and renders ratio button labels from tested ko/zh-hant/en image edit copy helpers while preserving existing saved crop aspect labels.
- `M166-NA`: Legacy crop modal copy localization now moves crop modal title, description, aria, preview, no-image, action, close, and ratio button copy into tested ko/zh-hant/en image edit helpers while preserving canonical crop aspect state.
- `M166-NB`: Legacy filter panel copy localization now moves filter panel title, close action, slider labels, and preset buttons into tested ko/zh-hant/en image edit helpers while preserving filter mutation behavior.
- `M166-NC`: Preview modal copy localization now moves preview dialog chrome, device labels, actions, iframe title, unpublished fallback, and footer summary into tested ko/zh-hant/en copy helpers.
- `M166-ND`: Version history panel copy localization now moves modal shell, restore confirmation, current draft, revision list, source labels, and diff preview chrome into tested ko/zh-hant/en copy helpers while preserving restore and diff behavior.
- `M166-NE`: Document diff detail copy localization now moves shared diff summary, node kind, node summary, field/state labels, and change descriptions into tested ko/zh-hant/en copy helpers for version history and publish previews.
- `M166-NF`: Move-to-page modal copy localization now moves modal shell, empty state, move-failed fallback, untitled-page fallback, home badge, moving state, and close guidance into tested ko/zh-hant/en copy helpers.
- `M166-NG`: Save-section modal copy localization now moves modal shell, form labels/placeholders, validation errors, category labels, save-failed fallback, cancel/save/saving actions, and close accessibility into tested ko/zh-hant/en section copy helpers.
- `M166-NH`: Sandbox top-bar copy localization now moves top-bar site/page titles, save/status chips, member preview, viewport/reset labels, quick-jump controls/modal/search/empty states, and admin quick-jump section/item labels into tested ko/zh-hant/en copy helpers.
- `M166-NI`: Sandbox feedback overlay save-status localization now moves floating saving/saved/error labels into tested ko/zh-hant/en copy helpers while preserving activity chips and toast behavior.
- `M166-NJ`: Sandbox draft-conflict banner localization now moves the mixed Korean/English conflict guidance and reload action into tested ko/zh-hant/en page chrome copy helpers.
- `M166-NK`: SandboxPage toast/activity feedback localization now moves component-preset, paste, navigation, saved-section, footer-link, and selection-summary feedback into tested ko/zh-hant/en page chrome copy helpers.
- `M166-NL`: Inline text editor toolbar localization now moves TipTap placeholder, formatting tooltips/aria labels, link popover copy, visible link action, and AI assistant tooltip copy into tested ko/zh-hant/en copy helpers.
- `M166-NM`: Component library panel localization now moves Add drawer saved-component title, name placeholder, save/insert/delete actions, empty state, selection hint, aria labels, and date locale into tested ko/zh-hant/en copy helpers.
- `M166-NN`: Site settings preset feedback localization now moves component preset no-target notices into copy helpers and polishes ko/zh-hant preset applied/deleted feedback to avoid mixed English/Korean notices.
- `M166-NO`: SEO panel shell/status localization now moves parent modal title/actions, tab labels, validation summary, loading/save/cancel states, fetch/save/assistant errors, redirect warnings, preview fallbacks, and recommendation fallback text into tested ko/zh-hant/en copy helpers.
- `M166-NP`: SEO panel Basics-tab copy localization now moves slug/canonical helpers, redirect guidance, title/description placeholders, noindex/nofollow explanations, preview heading, and recommended-length counters into tested ko/zh-hant/en copy helpers.
- `M166-NQ`: SEO panel Social-tab copy localization now moves OG/Twitter field labels, fallback placeholders, preview heading, and no-image state into tested ko/zh-hant/en copy helpers.
- `M166-NR`: SEO panel Advanced-tab copy localization now moves additional meta-tag chrome, FAQ schema controls, JSON-LD guidance, Type/Label fields, add/delete/use actions, and structured-data block type labels into tested ko/zh-hant/en copy helpers.
- `M166-NS`: SEO panel Hreflang-tab copy localization now moves alternate-link descriptions, linked-locale guidance, missing-locale warnings, sibling index state, sitemap inclusion labels, and crawlability status into tested ko/zh-hant/en copy helpers.
- `M166-NT`: SEO panel Assistant-tab copy localization now moves focus-keyword chrome, empty/validation states, failure detection tokens, task severity/status labels, validation severity labels, and SEO field labels into tested ko/zh-hant/en copy helpers.
- `M166-NU`: SEO Assistant generated task copy localization now moves indexability, H1, image-alt, focus-keyword title/description/slug/body, and focus-keyword setup task labels/details into tested ko/zh-hant/en backend copy helpers.
- `M166-NV`: SEO overview checklist copy localization now moves business-name, keywords, page SEO fields, indexable-pages, H1, image-alt, and publish-blocker checklist labels/details into tested ko/zh-hant/en backend copy helpers.
- `M166-NW`: SEO validation issue copy localization now moves title/description, slug, canonical, image URL, focus-keyword, additional-meta, robots, structured-data, and custom JSON-LD issue messages/fix hints into tested ko/zh-hant/en backend copy helpers.
- `M166-NX`: SEO default pattern copy localization now moves unconfigured default description templates into ko/zh-hant/en fallback patterns so default metadata and preview rows no longer leak Korean descriptions on non-Korean builder SEO output.
- `M166-NY`: SEO dashboard copy helper localization now moves dashboard shell, checklist, defaults, page-table, tools, status, and custom robots copy into tested ko/zh-hant/en helpers while using locale-specific empty SEO defaults before settings load.
- `M166-NZ`: SEO API route error copy localization now moves page SEO and SEO assistant 404, invalid JSON, and unexpected-failure errors into tested ko/zh-hant/en backend copy helpers so UI fallbacks no longer display raw English or internal exception messages.
- `M166-OA`: Live-chat inbox copy helper localization now moves the admin route metadata/header and inbox/thread chrome into a tested ko/zh-hant/en helper while preserving SSE, refresh, close, send, and date-format behavior.
- `M166-OB`: Public live-chat widget copy localization now moves widget defaults, field labels/placeholders, launcher/close/start/send actions, validation errors, and API error fallbacks into tested ko/zh-hant/en helpers wired through locale-aware live-chat settings.
- `M166-OC`: Live-chat admin sender label localization now shares author fallback labels across inbox copy and the admin send API so optimistic and persisted admin messages use locale-specific labels instead of raw `admin`.
- `M166-OD`: Public live-chat API error localization now returns locale-aware `error` text with stable `errorCode` values from start/send/stream routes while widget requests pass locale through query/body/stream params.
- `M166-OE`: Live-chat SSE error event localization now emits locale-aware stream failure text with stable `errorCode` values from the shared stream builder, so public and admin streams no longer expose raw exception messages in `event: error` payloads.
- `M166-OF`: Live-chat admin API error localization now returns locale-aware builder inbox 400/404 payloads with stable `errorCode` values from detail, send, and stream routes while inbox fetches pass locale through reset, refresh, close, send, and stream calls.
- `M166-OG`: Booking analytics API error localization now returns locale-aware `unknown_locale`, `invalid_from_timestamp`, and `invalid_to_timestamp` payloads from the admin analytics route instead of raw English timestamp/locale errors.
- `M166-OH`: Booking notification template API error localization now returns locale-aware stable-code payloads for invalid filters, invalid create/update payloads, duplicates, and not-found detail mutations instead of raw template store strings.
- `M166-OI`: Booking email template API error localization now returns locale-aware stable-code payloads for unknown email template types and invalid update payloads while the admin template editor passes locale through save requests.
- `M166-OJ`: Booking calendar-sync connect API error localization now returns locale-aware stable-code payloads for missing staff ids, OAuth state failures, and provider auth URL failures while the admin calendar-sync UI passes locale through Google/Outlook connect requests.
- `M166-OK`: Booking calendar-sync utility API error localization now returns locale-aware stable-code payloads for missing sync connections and invalid recurrence RRULE requests while the admin calendar-sync UI passes locale through sync-now requests.
- `M166-OL`: Booking services API error localization now returns locale-aware stable-code payloads for invalid service create/update payloads and service not-found mutations while the services admin UI passes locale through save/deactivate requests.
- `M166-OM`: Booking staff API error localization now returns locale-aware stable-code payloads for invalid staff create/update payloads and staff not-found mutations while the staff admin UI passes locale through save/deactivate requests.
- `M166-ON`: Booking resources API error localization now returns locale-aware stable-code payloads for invalid resource create/update payloads and resource not-found mutations while the resources admin UI passes locale through save/deactivate requests.
- `M166-OO`: Booking packages API error localization now returns locale-aware stable-code payloads for invalid package create/update payloads and package not-found mutations while the packages admin UI passes locale through save/deactivate requests.
- `M166-OP`: Booking package credits API error localization now returns locale-aware stable-code payloads for invalid customer-credit create/update payloads, missing credits, and missing package references while the packages admin UI passes locale through credit save/revoke requests.
- `M166-OQ`: Booking cancellation policies API error localization now returns locale-aware stable-code payloads for invalid policy create/update payloads and policy not-found mutations while the policies admin UI passes locale through save/toggle requests.
- `M166-OR`: Booking staff availability API error localization now returns locale-aware stable-code payloads for invalid availability updates while the availability admin UI passes locale through save requests.
- `M166-OS`: Booking admin mutation API error localization now returns locale-aware stable-code payloads for admin booking create/update validation, missing records, service/staff lookup failures, slot conflicts, unavailable slots, and create failures while dashboard/calendar booking mutation calls pass locale through requests.
- `M166-OT`: Booking waitlist detail API error localization now returns locale-aware stable-code payloads for missing waitlist entries, invalid waitlist updates, and already-promoted entries while the dashboard waitlist update call passes locale through requests.
- `M166-OU`: Booking waitlist promotion API error localization now returns locale-aware stable-code payloads for missing or closed waitlist entries, invalid promotion payloads, service/staff availability and assignment failures, missing promotion slots, slot lock conflicts, and unavailable slots while the dashboard waitlist promote call passes locale through requests.
- `M166-OV`: Booking manual payment API error localization now returns locale-aware stable-code payloads for validation failures, invalid JSON, missing bookings, amount/balance/payment-state failures, and fallback failures while the dashboard manual payment call passes locale through requests.
- `M166-OW`: Booking documents API error localization now returns locale-aware stable-code payloads for invalid document payloads, missing bookings, invalid document types, unpaid receipt requests, stale/missing documents, and fallback document-action failures while the dashboard document issue/email call passes locale through requests.
- `M166-OX`: Booking calendar OAuth callback API error localization now returns locale-aware stable-code payloads for provider denial, missing callback params, invalid or expired OAuth state, token exchange failures, and token encryption failures without exposing provider, env, or encryption details.
- `M166-OY`: Billing documents list API error localization now returns locale-aware stable-code payloads for invalid list filters and list-load failures while the billing documents admin refresh call already passes locale through requests.
- `M166-OZ`: Billing document webhooks API error localization now returns locale-aware stable-code payloads for invalid webhook filters, webhook list-load failures, missing replay events, and replay failures while the billing documents admin refresh/replay calls pass locale through requests.
- `M166-PA`: Billing document settings API error localization now returns locale-aware stable-code payloads for settings load failures, malformed JSON saves, and settings save failures while the billing documents admin automation-policy save call passes locale through requests.
- `M166-PB`: Billing document templates API error localization now returns locale-aware stable-code payloads for invalid template payloads, missing templates, and template list/create/load/update/delete failures.
- `M166-PC`: Billing documents manual payment API error localization now returns locale-aware stable-code payloads for invalid sources/payloads/JSON, missing documents/orders/bookings, unavailable or over-balance manual payments, order/booking locked/paid states, and fallback manual-payment failures while the billing documents admin manual-payment call passes locale through requests.
- `M166-PD`: Billing documents payment/share link API error localization now returns locale-aware stable-code payloads for invalid sources, invalid link payloads, unavailable/missing documents, and payment/share link create/revoke failures while the billing documents admin link actions pass locale through requests.
- `M166-PE`: Billing documents lifecycle API error localization now returns locale-aware stable-code payloads for invalid sources, invalid lifecycle payloads, unavailable lifecycle actions, and lifecycle failures while central void/supersede actions pass locale through requests and store locale-specific default reasons.
- `M166-PF`: Billing document download API error localization now returns locale-aware stable-code payloads for invalid sources and missing documents while central download links pass locale through the generated href.
- `M166-PG`: Billing documents bulk API error localization now returns locale-aware stable-code payloads for invalid bulk payloads, empty parsed targets, and bulk operation failures while preserving issue-invoice, void, and CSV export success shapes.
- `M166-PH`: Commerce payment webhook admin API error localization now returns locale-aware stable-code payloads for invalid list filters, list-load failures, missing replay events, and replay failures while the admin webhook manager passes locale through refresh/replay requests and localizes known replay reasons.
- `M166-PI`: Commerce notifications API error localization now returns locale-aware stable-code payloads for invalid notification filters, load failures, malformed saves, and save failures while the notification manager passes locale through refresh/save requests and displays localized refresh failures.
- `M166-PJ`: Commerce currency settings API error localization now returns locale-aware stable-code payloads for currency settings load/save failures while the currency settings admin save request passes locale through requests.
- `M166-PK`: Commerce tax rules API error localization now returns locale-aware stable-code payloads for invalid filters/payloads, malformed saves, load failures, and save failures while the tax rules admin save request passes locale through requests.
- `M166-PL`: Commerce shipping rules API error localization now returns locale-aware stable-code payloads for invalid filters/payloads, malformed saves, load failures, and save failures while the shipping rules admin save request passes locale through requests.
- `M166-PM`: Commerce categories API error localization now returns locale-aware stable-code payloads for invalid category filters and category load failures without leaking product/category storage exception messages.
- `M166-PN`: Commerce orders list/detail API error localization now returns locale-aware stable-code payloads for invalid filters/update payloads, list/detail load failures, missing orders, malformed updates, and update failures while the order manager update request passes locale through requests.
- `M166-PO`: Commerce order manual-payment API error localization now returns locale-aware stable-code payloads for invalid payloads, malformed JSON, missing orders, amount/balance/state failures, and fallback failures while the order manager manual-payment request passes locale through requests.
- `M166-PP`: Commerce order refund API error localization now returns locale-aware stable-code payloads for invalid payloads, malformed JSON, missing orders, invalid/over-balance/non-refundable refunds, provider refund failures, and fallback failures while the order manager refund request passes locale through requests.
- `M166-PQ`: Commerce order documents API error localization now returns locale-aware stable-code payloads for invalid payloads, malformed JSON, missing orders, invalid document types, unpaid receipt requests, and document issue failures while the order manager document request passes locale through requests.
- `M166-PR`: Commerce products API error localization now returns locale-aware stable-code payloads for invalid product filters/payloads, malformed JSON, SKU conflicts, missing products, product load/save/bulk/action failures, and product manager mutation/import requests pass locale through requests.
- `M166-PS`: Commerce payment-intents API error localization now returns locale-aware stable-code payloads for invalid intent payloads, malformed JSON, invalid captured intents, and provider intent failures while preserving create/capture success response shapes.
- `M166-PT`: Commerce checkout API error localization now returns locale-aware stable-code payloads for rate limits, invalid checkout payloads, checkout validation failures, malformed JSON, and checkout failures while the public checkout submit request passes locale through requests.
- `M166-PU`: Commerce cart-recovery API error localization now returns locale-aware stable-code payloads for rate limits, invalid recovery payloads, malformed JSON, empty carts, and recovery failures while the public checkout recovery request passes locale through requests.
- `M166-PV`: Commerce subscriptions API error localization now returns locale-aware stable-code payloads for invalid subscription filters/payloads, malformed JSON, list/load/create/update/delete failures, missing plans/subscriptions, archived plans, required customer email, invalid IDs, ID-kind mismatches, blocked transitions, and plan delete conflicts.
- `M166-PW`: Commerce payment webhook receiver API error localization now returns locale-aware stable-code payloads for unsupported providers, missing webhook configuration, invalid signatures, malformed JSON, unsupported events, and processing failures while preserving verified webhook success handling.
- `M166-PX`: Member API error localization now returns locale-aware stable-code payloads for login/signup/profile/bookings validation, malformed JSON, invalid credentials, missing members, duplicate emails, blocked email changes, authentication requirements, session creation failures, profile save failures, booking load failures, and auth/signup fallback failures while member UI requests pass locale through API calls.
- `M166-PY`: Builder members admin API error localization now returns locale-aware stable-code payloads for member list/detail load failures, create/update validation, malformed JSON, duplicate emails, missing members, update failures, and delete failures while the members admin UI passes locale through create/update/delete requests.
- `M166-PZ`: Builder notifications API and inbox localization now returns locale-aware stable-code payloads for internal-only posts, malformed JSON, invalid kinds, missing subjects, list/create/update/delete failures, and missing notifications while the admin notification inbox passes locale through list/read-all/read requests and localizes its bell, dialog, status, empty, and kind labels.
- `M166-QA`: Builder users security API error localization now returns locale-aware stable-code payloads for user-role list/create/update/delete validation, malformed JSON, missing role-management permission, missing users, and fallback failures while the Users admin passes locale through create/update/delete requests.
- `M166-QB`: Builder review sessions API error localization now returns locale-aware stable-code payloads for review-session list/create validation, malformed JSON, list/create failures, invalid review tokens, and token verification failures while preserving review session and audience success response shapes.
- `M166-QC`: Builder FAQ API error localization now returns locale-aware stable-code payloads for FAQ list/create/detail/update/delete validation, malformed JSON, missing items, and fallback failures while the FAQ admin passes locale through mutations and localizes status badges.
- `M166-QD`: Builder workspace account/team/sites API error localization now returns locale-aware stable-code payloads for account load/update, site list/create, member list/create/update/delete validation, malformed JSON, missing members, owner-protection conflicts, and fallback failures while the workspace admin passes locale through site/member mutations.
- `M166-QE`: Builder workspace shared assets, analytics, and CMS rollup API error localization now returns locale-aware stable-code payloads for asset list/upload/load/delete validation and failures, workspace analytics failures, and CMS collection rollup failures while the workspace shared-assets admin passes locale through upload/delete mutations.
- `M166-QF`: Builder blog admin/posts API error localization now returns locale-aware stable-code payloads for blog admin model validation/load failures and blog post query/load failures while preserving public post success shapes and the `scope=all` builder-read guard.
- `M166-QG`: Builder collaboration API error localization now returns locale-aware stable-code payloads for comments, cursors, presence, and review marker validation/not-found/load/create/update/delete failures while preserving collaboration success response shapes.
- `M166-QH`: Builder services API error localization now returns locale-aware stable-code payloads for service area load, validation, malformed JSON, conflict, not-found, update, and reset failures while preserving source override and slug redirect success shapes.
- `M166-QI`: Builder translations manager API error localization now returns locale-aware stable-code payloads for sync, validation, missing-entry, and save failures while preserving translation sync and PATCH save success response shapes.
- `M166-QJ`: Builder translations edit API error localization now returns locale-aware stable-code payloads for malformed edit JSON, invalid edit requests, empty edit saves, and edit persistence failures while preserving node, SEO, and image override success response shapes.
- `M166-QK`: Builder translations provider/batch API error localization now returns locale-aware stable-code payloads for provider validation, unconfigured provider, provider failure, and batch failure paths while Translation Manager passes route locale through translate, batch, save, and sync requests.
- `M166-QL`: Builder translations dashboard/publish-warning API error localization now returns locale-aware stable-code payloads for dashboard and publish-warning load failures while the publish-warning panel uses route locale for copy and polling requests.
- `M166-QM`: Builder apps installation/settings API error localization now returns locale-aware stable-code payloads for app install, lifecycle, uninstall, settings, migration, rollback, restore, and CMS scope failures while App Market notices use localized action/settings fallbacks instead of raw request strings.
- `M166-QN`: Builder apps catalog/hooks API and App Market lifecycle chrome localization now returns locale-aware stable-code payloads for catalog and hook list/register failures, invalid hook payloads, app IDs, hook IDs, and malformed JSON while App Market cards render localized required/select, empty, version, rollback, permission, audit, and migration chrome.
- `M166-QO`: Builder CRM contacts/automations/integrations API and admin UI localization now returns locale-aware stable-code payloads for CRM list/create/detail/update/delete failures while localized CRM admin panels pass locale through non-Korean mutations and render localized contacts, automations, integrations, table, empty-state, validation, and fallback chrome.
- `M166-QP`: Builder CRM segments and send-queue API error localization now returns locale-aware stable-code payloads for invalid segment JSON/payloads, segment list/create/preview failures, and send queue stats failures while preserving segment list/count/preview and queue stats success shapes.
- `M166-QQ`: Builder marketing campaign API error localization now returns locale-aware stable-code payloads for campaign list/create/detail/update/stats/send failures while CampaignsAdmin and CampaignEditor pass locale through create, save, and send requests.
- `M166-QR`: Builder marketing subscribers/import/templates API and admin UI localization now returns locale-aware stable-code payloads for subscriber list/create/import and template list/create/detail/render/update failures while SubscribersAdmin, TemplatesAdmin, TemplateEditor, and CampaignEditor pass locale through marketing requests and template admin chrome renders localized labels.
- `M166-QS`: Public marketing and CRM tracking API error localization now returns locale-aware stable-code payloads for public subscribe/verify/unsubscribe/track/cron failures plus CRM click-tracking misconfiguration and invalid-token failures, while opt-in/unsubscribe links carry locale context and unsubscribe confirmation HTML renders in the subscriber's preferred locale.
- `M166-QT`: Builder site navigation, lightbox, and saved-section API error localization now returns locale-aware stable-code payloads for malformed navigation/lightbox/section requests, missing payloads, slug conflicts, not-found states, and save failures while preserving navigation, lightbox, draft, and saved-section success shapes.
- `M166-QU`: Builder site global header/footer and brand-kit API error localization now returns locale-aware stable-code payloads for missing global draft canvases, malformed draft/brand-kit JSON, invalid brand asset paths, validation failures, and brand-kit load/save fallback failures while preserving draft and brand-kit success shapes.
- `M166-QV`: Builder site SEO overview, preview, checklist, settings, and bulk APIs now return locale-aware stable-code payloads for validation failures, malformed JSON, load failures, save failures, preview generation failures, and bulk update failures while preserving SEO success response shapes.
- `M166-QW`: Builder site settings API now returns locale-aware stable-code payloads for validation failures, malformed JSON, settings load failures, and settings save failures while preserving localized settings/theme/mobile success response shapes.
- `M166-QX`: Builder site custom-code APIs now return locale-aware stable-code payloads for validation failures, malformed JSON, oversized snippets, missing pages, custom-code load failures, and site/page custom-code save failures while preserving success response and warning shapes.
- `M166-QY`: Builder site redirect APIs now return locale-aware stable-code payloads for malformed redirect JSON, validation failures, helper validation failures, missing redirect rules, redirect list/load failures, save failures, delete failures, and non-local public fallback access while preserving redirect manager and middleware fallback success shapes.
- `M166-QZ`: Builder site publish-check, scheduled-publish, revision, and rollback APIs now return locale-aware stable-code payloads for missing page ids, missing draft canvases, invalid schedule times, schedule load/save/cancel failures, missing revisions, missing snapshot drafts, revision create/load failures, and rollback failures while preserving publish lifecycle success shapes.
- `M166-RA`: Builder site page order, seed, and linked-locale APIs now return locale-aware stable-code payloads for malformed order/seed JSON, invalid order payloads, unknown or duplicate page order ids, order save failures, invalid seed bodies, seed failures, missing pages, and linked-page load failures while preserving page ordering, seeding, and linked-page success shapes.
- `M166-RB`: Builder site page draft and publish APIs now return locale-aware stable-code payloads for missing drafts, draft load/save failures, expected-revision requirements, draft conflicts, locale mismatches, malformed draft JSON, and internal publish failures while preserving publish blocked/stale codes and success response shapes.
- `M166-RC`: Builder site pages create/update/delete and move-from APIs now return locale-aware stable-code payloads for page list/create/update/delete failures, invalid slugs, duplicate slugs, unsupported dynamic page collections, localized slug failures, home-page delete blocks, move validation, draft load/not-found states, and move write failures while preserving page create/update/delete/move success shapes.
- `M166-RD`: Builder site page SEO and SEO assistant APIs now return locale-aware stable-code payloads for malformed JSON, validation failures, missing pages, page SEO fallback failures, and SEO assistant fallback failures while preserving page SEO read/write and assistant task success shapes.
- `M166-RE`: Builder site audit API now returns locale-aware stable-code payloads for audit log load failures while preserving recent audit event success shapes and default limit fallback behavior.
- `M166-RF`: Builder home snapshot, publish-check, publish, revision, and rollback APIs now return locale-aware stable-code payloads for malformed JSON, invalid snapshot kinds, locale/page mismatches, missing drafts/revisions, conflicts, validation blockers, and fallback failures while preserving home snapshot and publish lifecycle success shapes.
- `M166-RG`: Advanced picker design-contract coverage now recognizes localized ColorPicker copy helpers for EyeDropper, theme palette, and recent-color labels, restoring the full unit suite after the color picker localization split.
- `M166-RH`: Builder page dataset GET/PUT/preview/seed APIs now return locale-aware stable-code payloads for malformed JSON, invalid dataset bodies, missing builder sites/pages, invalid/unapproved targets, collection/mode/limit validation failures, CMS collection misses, conflicts, and preview/load/save/seed fallback failures while preserving dataset overview, preview, and seed success shapes.
- `M166-RI`: Builder nested page publish-check and revision APIs now return locale-aware stable-code payloads for missing sites/pages, page/locale mismatches, missing drafts, invalid revision kinds, missing revisions, malformed rollback JSON, rollback revision requirements, conflicts, and publish-check/revision/rollback fallback failures while preserving publish-check, revision history, and rollback success shapes.
- `M166-RJ`: Builder nested page overview, draft-save, and publish APIs now return locale-aware stable-code payloads for missing sites/pages, overview load failures, malformed draft JSON, invalid draft bodies, page/locale mismatches, draft save conflicts/failures, missing publish drafts, publish validation blockers, publish conflicts, and publish fallback failures while preserving overview, draft save, and publish success shapes.
- `M166-RK`: Builder columns list/create/detail/update/delete/publish APIs now return locale-aware stable-code payloads for load failures, malformed JSON, validation failures, missing drafts, duplicate slugs, legacy-delete blocks, publish conflicts, and publish fallback failures while preserving column create/edit/delete/publish success shapes.
- `M166-RL`: Builder events list/create/detail/update/delete/RSVP APIs now return locale-aware stable-code payloads for query/body validation, malformed JSON, list/create/load/update/delete failures, not-found states, closed/unpublished/full RSVP states, and RSVP fallback failures while preserving event admin and RSVP success shapes.
- `M166-RM`: Builder portfolio list/create/detail/update/delete APIs now return locale-aware stable-code payloads for query/body validation, malformed JSON, list/create/load/update/delete failures, not-found states, and validation fallback failures while preserving portfolio admin success shapes.
- `M166-RN`: Public site search API now returns locale-aware stable-code payloads for rate limits, search-index load/build failures, and query execution failures while preserving empty-query and hit-list success shapes, and the live search widget displays localized API failure messages.
- `M166-RO`: Builder webhooks list/create/update/delete/deliveries/retry APIs now return locale-aware stable-code payloads for malformed JSON, validation failures, missing webhooks/deliveries, list/create/update/delete/delivery-load/retry failures while preserving masked subscription, one-time secret, delivery list, and retry result success shapes.
- `M166-RP`: Builder and public experiments APIs now return locale-aware stable-code payloads for malformed JSON, validation failures, rate limits, missing experiment ids, missing experiments, duplicate variants, unknown variants, list/create/load/update/results/assign/event failures while preserving admin experiment, results, assignment, and conversion success shapes.
- `M166-RQ`: Builder domains list/create/detail/delete/verify APIs now return locale-aware stable-code payloads for malformed JSON, validation failures, missing domains, list/create/load/delete/verify failures, DNS-pending states, and Vercel detach/attach failures while preserving domain registration, already-registered, detail, delete, verification, and active status success shapes.
- `M166-RR`: Public booking services/staff/availability APIs now return locale-aware stable-code payloads for rate limits, missing service/date filters, and service/staff/slot load failures while preserving service list, staff list, and slot success shapes, and the booking flow widget displays localized API failures.
- `M166-RS`: Public booking payment-intent API now returns locale-aware stable-code payloads for rate limits, malformed/invalid payloads, unavailable/free services, missing prices, Stripe provider/client config failures, provider errors, missing client secrets, provider connectivity failures, and generic payment preparation failures while preserving package-credit, dev-stub, and Stripe client-secret success shapes.
- `M166-RT`: Public booking create API now returns locale-aware stable-code payloads for malformed/invalid booking payloads, honeypot rejects, missing service/staff, payment required/not-allowed/mismatch/unsettled/unverified states, slot lock/unavailable conflicts, package-credit redemption misses, and create fallback failures while preserving booking creation success shapes.
- `M166-RU`: Canvas flow-section and flow-container child moves now use direct DOM translate previews during pointer drag and defer store transient/commit work until drop, reducing per-frame document churn while preserving drop-time reparent/reorder behavior.
- `M166-RV`: Canvas node resize interactions now use direct DOM left/top/width/height previews during pointer drag and defer store rect updates until pointerup, keeping resize handles/readouts responsive while preserving final commit and responsive flow resize behavior.
- `M166-RW`: Canvas node selection now memoizes node render boundaries, removes the broad selected-node prop from stage rendering, scopes selection subscriptions to per-node primitive selectors, and restores home canvas rendering after the selector helper regression while preserving nested selection, context-menu, accordion, office, and hero-search preview behavior.
- `M166-RX`: Builder page candidates now share the PageSwitcher internal-page visibility filter at the sandbox site-state layer, preventing generated test/sandbox pages from leaking into page switcher, link picker, SEO, publish, and move-picker page lists while keeping real home/user/CMS pages visible.
- `M166-RY`: Canvas move start now reuses an unchanged active selection instead of writing the same selected-node set back into the store before every drag, reducing drag-start selection churn while preserving multi-select, locked-node filtering, direct DOM move previews, and drop-time commits.
- `M166-RZ`: Builder editor site-header preview now switches to the hamburger header sooner inside the constrained canvas, prevents nav links from wrapping into overlapping rows, and neutralizes mobile right-rail action offsets so the search button stays inside the canvas while home page nodes remain rendered.
- `M166-SA`: Canvas direct move/resize previews now cache their target DOM elements for the active interaction, avoiding repeated `[data-node-id]` lookups on every drag frame while still invalidating disconnected or mismatched elements and clearing preview styles on cancel/drop.
- `M166-SB`: Canvas interactions now skip redundant hover-container, interaction-pointer, resize-preview, and overlap-picker state writes when pointer frames produce the same values, reducing React churn during direct DOM drag/resize previews while preserving preview readouts and cleanup.
- `M166-SC`: Canvas direct move/resize previews now skip redundant DOM style and marker writes when the active element already has the target translate, left/top/width/height, or preview dataset value, shaving per-frame work without changing commit or cleanup behavior.
- `M166-SD`: Canvas move start now resolves unlocked move ids and start rect records directly from `nodesById`, avoiding repeated full-node scans for selected-node locking and start rect/absolute rect setup while preserving missing-id and locked-node behavior.
- `M166-SE`: Canvas move start now precomputes visible container nodes and feeds that narrowed list into move-candidate creation, avoiding a container hit-test pass over every visible canvas node while preserving snap candidates, container reparent targets, and home rendering.
- `M166-SF`: Canvas node rendering now computes top-level flow section metrics once in the stage and reuses the visible-node array through recursive node renders, removing repeated `nodesById.values()` full-array creation from every CanvasNode while preserving responsive flow gaps and nested drop previews.
- `M166-SG`: Canvas node rendering now receives the route locale from the stage instead of parsing `window.location.pathname` inside every CanvasNode, reducing repeated per-node locale/copy setup while preserving localized repeater, hero-search, quick-panel, insight-preview, and code-assistant behavior.
- `M166-SH`: Canvas node rendering now computes hero-search quick-edit nodes, labels, layout, and destination options only for selected hero-search targets, avoiding repeated home hero-search model lookups for unrelated canvas nodes while preserving the quick-edit UI.
- `M166-SI`: Canvas service/FAQ accordion preview selectors now return stable default values for unrelated nodes and only subscribe to preview indices where accordion frames, roots, or top-level offsets need them, reducing cross-canvas rerenders while preserving selected accordion expansion.
- `M166-SJ`: Canvas move start now narrows snap candidate nodes to the current root, parent, or active-group scope before building move candidates, avoiding another full visible-node scan on large home pages while preserving snap scope and drop-target behavior.
- `M166-SK`: Builder editor site-header preview now switches to the hamburger header at the wider constrained-canvas breakpoint, preventing long public navigation labels from remaining visible and overlapping around the common 1037px editor canvas width while preserving home rendering.
- `M166-SL`: Canvas feedback geometry now reuses the move interaction's narrowed snap rects for distance labels instead of scanning every visible node again on move start, while resize feedback keeps its existing non-active-node candidate behavior.
- `M166-SM`: Canvas selected-node derivation now resolves selected ids through the store's `nodesById` map instead of filtering the full node array on selection changes, preserving selected-id order while reducing click/selection overhead on large pages.
- `M166-SN`: Canvas store selection setters now return the existing state for unchanged single-node, multi-node, and empty selection updates when preview state is also unchanged, avoiding redundant store publishes during repeated clicks while preserving accordion preview reveal/reset behavior.
- `M166-SO`: Canvas store viewport, draft-save-state, and selected-surface setters now skip unchanged values, avoiding redundant store publishes from repeated toolbar/inspector/status updates while preserving actual state transitions.
- `M166-SP`: Builder shell, topbar, and inspector selected/modal node lookups now reuse the store's `nodesById` map instead of scanning the full canvas document, reducing selection, modal, reset-control, and office quick-edit render work while preserving home rendering, page-menu cleanup, and drag previews.
- `M166-SQ`: Builder layers panel rendering now reuses the store's `nodesById` map, derives a selected-node `Set`, and computes default collapsed ids from the parent map instead of extra full-node passes, reducing layer-panel work on dense home pages while preserving top-level expansion and drag previews.
- `M166-SR`: Canvas style clipboard, accordion preview sizing, and inspector repeater quick-edit paths now reuse existing `nodesById` maps instead of repeated full-node searches or map rebuilds, reducing remaining editor chrome work around selection, copy/paste style, and service/FAQ quick editing.
- `M166-SS`: Canvas context-menu, drop-highlight, and overlap-picker layers now reuse the existing `nodesById` map and a selected-node `Set` instead of per-render array scans, reducing remaining pointer/menu chrome work while preserving home content, hamburger header behavior, context menus, and direct drag previews.
- `M166-ST`: Canvas stage flow-section drag preview now reuses the existing `nodesById` map instead of scanning `rootVisibleNodes` and rebuilding a root map during move renders, reducing another root-drag preview cost while preserving home content, page-menu cleanup, header hamburger behavior, and direct drag previews.
- `M166-SU`: Canvas store now maintains a reference-stable `selectedNodeIdSet`, and CanvasNode plus common selection commands use `Set.has` instead of repeated selected-id array membership checks, reducing selection publish and command overhead on dense home pages while preserving selection order, previews, and direct drag behavior.
- `M166-SV`: Canvas service/FAQ preview rendering now reuses a module-level empty index Set and only allocates revealed-index Sets for nodes that actually need accordion preview state, trimming per-node render allocation on dense home pages while preserving accordion reveal offsets and direct drag behavior.
- `M166-SW`: Canvas stage node tree rendering now exports `CanvasStageNodes` through `React.memo`, so drag feedback, alignment-guide, and drop-highlight state changes no longer force the 391-node home canvas tree to re-sort and remap while node props remain unchanged.
- `M166-SX`: Canvas editor rail section-template lookups now reuse the store `nodesById` map passed through the workspace instead of rebuilding node maps or scanning `document.nodes`, reducing design-rail work around dense-page selection and section-template focus.
- `M166-SY`: Builder topbar rendering now scopes canvas-store subscriptions to selected id, derived responsive-override state, and reset action, exports the topbar through `React.memo`, and stabilizes the SandboxPage topbar callbacks so dense-canvas drag/click churn does not wake the full topbar when its visible props are unchanged.
- `M166-SZ`: Builder sandbox page shell now splits its canvas-store reads into selector subscriptions for document, indexes, selection, save, clipboard, mutation base, and actions instead of subscribing to the entire store object, reducing unrelated store-publish rerenders across the editor shell during dense-page interactions.
- `M166-TA`: Canvas container now splits selection, history, clipboard, grouping, mutation, rect-update, content-update, and viewport store reads into individual selector subscriptions instead of subscribing to the whole canvas store, reducing unrelated store-publish rerenders in the primary canvas interaction layer.
- `M166-TB`: Sandbox inspector now splits document-node, locale, selection, surface, node-map, children-map, viewport, responsive, alignment, z-order, duplicate, and update actions into selector subscriptions instead of subscribing to the whole canvas store, reducing unrelated inspector rerenders during dense-page selection and movement.
- `M166-TC`: Sandbox layers panel now splits document-node, node-map, selection, active-group, reorder, reparent, update, focus, toggle-selection, and enter-group store reads into individual selector subscriptions instead of subscribing to the whole canvas store, reducing unrelated layer-tree rerenders while preserving search, collapse defaults, drag intent, and selection checks.
- `M166-TD`: Sandbox catalog panel now splits document-node, document-existence, locale, add-node, add-nodes, selected-node, and draft-save store reads into individual selector subscriptions, removing the last broad `useBuilderCanvasStore()` subscription from the canvas editor chrome while preserving quick-add, app widgets, and built-in section insertion behavior.
- `M166-TE`: Canvas feedback, drop-highlight, overlap-picker, selection-toolbar, context-menu, stage-toolbar, and zoom-dock overlays now export memoized component boundaries, reducing parent-render spillover from drag/selection/feedback state changes while preserving localized chrome, context actions, direct move preview, and home-page smoke behavior.
- `M166-TF`: Canvas feedback geometry now reuses a module-level empty rect array when move/resize feedback is inactive, so selection, pan, and unrelated parent renders do not hand fresh unused ghost/snap rect arrays to the memoized feedback overlay while preserving active move/resize feedback and home-page smoke behavior.
- `M166-TG`: Canvas context menu now memoizes locale copy, shortcut helpers, close handling, and the full action array, and the menu component itself exports through `React.memo`, so opened menu chrome keeps stable props across unrelated parent renders while preserving localized actions, home content, header layout, and direct drag previews.
- `M166-TH`: Canvas selection toolbar now memoizes localized copy, shortcut title helpers, action buttons, and its action array, while the selection-toolbar layer passes stable edit/replace/link/more callbacks, reducing selected-node drag chrome churn while preserving toolbar more-menu, home content, header layout, and direct drag previews.
- `M166-TI`: Drag ghost origin/current overlays, multi-selection bbox, stage toolbar, and zoom dock now have stronger memoized render/handler boundaries, and the stage toolbar chrome is raised above high-z home hero layers so grid/undo/redo clicks are not intercepted by canvas nodes while preserving zoom controls, home content, header layout, and direct drag previews.
- `M166-TJ`: Snap-distance labels, resize readouts, and alignment guides now use memoized render/calculation boundaries, with snap-distance measurement covered by a pure helper test, reducing remaining per-frame feedback overlay work while preserving drag ghosts, zoom/grid controls, home content, header layout, and direct drag previews.
- `M166-TK`: Canvas snap computation now precomputes other-node edge geometry once per `computeSnap` call and reuses it for snap selection plus alignment/spacing guide generation, reducing per-frame drag snap work while preserving nearest-edge behavior, spacing labels, home content, header layout, zoom/grid controls, and direct drag previews.
- `M166-TL`: Move interactions now precompute snap candidate edge geometry once at drag start and store it on the active interaction, so pointer-move frames call `computeSnapFromEdges` without rebuilding candidate edges while preserving snap rects for feedback labels, nearest-edge behavior, home content, header layout, zoom/grid controls, and direct drag previews.
- `M166-TM`: Container hover hit-testing now reuses the previous hit candidate before scanning all container hit rects, cutting another repeated pointer-frame loop for dense single-node drags while preserving container hover behavior, snap feedback, home content, header layout, zoom/grid controls, and direct drag previews.
- `M166-TN`: Single-node direct move preview now stores only the pending rect during pointer-move frames and creates the commit `Map` only on pointerup, removing a per-frame `Map` allocation from the most common drag path while preserving multi-node moves, direct preview cleanup, home content, header layout, zoom/grid controls, and snap feedback.
- `M166-TO`: Pointerup reparent detection now resolves the hovered container from the pending move rect and precomputed container hit candidates when available, avoiding a full document node map rebuild and container scan on the common direct-preview drop path while preserving fallback behavior, home content, header layout, zoom/grid controls, and direct preview cleanup.
- `M166-TP`: Pointerup hover fallback, flow-reorder commit, and responsive flow-resize commit now reuse the canvas store's maintained `nodesById` index instead of rebuilding a full node map or scanning fresh nodes for the moved/resized node, reducing drop-time work while preserving home content, single Home row, header layout, direct preview cleanup, and flow commit behavior.
- `M166-TQ`: Canvas interactions now drop the unused large `nodes` and `visibleNodes` props from the `useCanvasInteractions` boundary, narrowing drag/resize hook inputs to the indexed and prefiltered data it actually uses while preserving home content, single Home row, header layout, direct preview cleanup, and canvas feedback behavior.
- `M166-TR`: Canvas snap-distance feedback now uses the live direct resize preview rect while resizing, so measurement labels track the visible DOM resize preview instead of the stale committed rect while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and dense-canvas smoke behavior.
- `M166-TS`: Canvas container rendering now buckets visible, root-visible, and visible-container node lists in a single pass instead of filtering the dense home node array three separate times, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and canvas feedback behavior.
- `M166-TT`: Canvas interaction geometry snapshots now carry only the indexed node map and absolute rect map they actually use, dropping the unused visible-node array from drag/resize snapshot capture while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and canvas feedback behavior.
- `M166-TU`: Canvas stage root rendering now sorts root nodes once and splits flow-section vs non-flow roots in a single pass instead of filtering the sorted root list twice, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and flow-section preview gaps.
- `M166-TV`: Canvas move start now reuses the store-maintained `selectedNodeIdSet` for target membership before walking ordered selected ids, avoiding another drag-start `includes` scan while preserving selected-id order, locked-node filtering, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-TW`: Canvas move-start snap candidate selection now returns the already-visible root node bucket directly for root-scope drags instead of allocating a second filtered root array, preserving selected-id order, locked-node filtering, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-TX`: Canvas node context-menu selection retention now reuses the store-maintained `selectedNodeIdSet` instead of scanning selected ids with `includes`, preserving multi-select context menus, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-TY`: Canvas store selection toggling now uses the maintained `selectedNodeIdSet` to detect existing selections instead of scanning `selectedNodeIds`, preserving selection order, selected-id set synchronization, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-TZ`: Canvas context-menu rendering now receives the maintained `selectedNodeIdSet` and uses it for menu-target membership instead of scanning `selectedNodeIds`, preserving multi-select menu counts, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UA`: Canvas overlap-picker rendering now receives the maintained `selectedNodeIdSet` directly instead of allocating a fresh Set from `selectedNodeIds`, preserving overlap selection highlighting, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UB`: Canvas layers panel selection highlighting now subscribes to the maintained `selectedNodeIdSet` directly instead of rebuilding a Set from `selectedNodeIds`, preserving layer row selection state, home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UC`: Canvas move interactions now carry a precomputed `nodeIdSet`, letting every CanvasNode check active move membership with `Set.has` instead of scanning `interaction.nodeIds`, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UD`: Canvas resize interactions now carry precomputed `snapRects`, so resize feedback reuses start-time candidate rects instead of scanning all visible nodes during feedback renders, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UE`: Canvas feedback snap-rect derivation now depends only on the active interaction and no longer accepts or tracks unused visible-node, viewport, selected-id, or absolute-rect inputs, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UF`: Canvas resize feedback active-rect derivation now uses the single `resizeCurrentRect` directly and only builds current-rect arrays for move interactions, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UG`: Canvas feedback overlay now receives primitive zoom and pan props instead of the whole `zoomState` object, narrowing memoized overlay dependencies while preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UH`: Canvas move start no longer writes resize pointer state, and feedback overlay receives `resizePointer` only during resize interactions, reducing move-start overlay churn while preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UI`: Canvas move interactions now precompute `canDirectPreview` at drag start so pointer frames check a boolean instead of revalidating every moving node id against the geometry map, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UJ`: Canvas move interactions now store `snapBounds` on the active interaction and reuse it in pointer-frame snap computation instead of allocating a fresh canvas-size object each frame, preserving home content, single Home row, header layout, and direct move/resize preview cleanup.
- `M166-UK`: Canvas alignment-guide updates now keep an interaction-local guides ref and skip React setter calls entirely when the computed guide list is unchanged, reducing another pointer-frame state-update path while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UL`: Single-node direct move previews now store only the pending rect in the active interaction ref and reuse the interaction node id at drop time, removing a per-frame `{nodeId, rect}` object allocation while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and reparent/drop behavior.
- `M166-UM`: Canvas interaction pointer and resize-preview rect updates now use ref-backed fast paths to skip React setter calls entirely when cleanup, resize readout, or pointer frames produce unchanged values, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UN`: Canvas resize interactions now store the viewport origin at resize start and reuse it for resize-readout pointer coordinates, removing a per-frame `getBoundingClientRect()` layout read while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UO`: Canvas alignment-guide reset paths now reuse a module-level empty guide array for initial state, refs, cancellation, viewport cleanup, and pointer-end cleanup, avoiding repeated empty-array allocations while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UP`: Canvas direct resize previews now apply primitive left/top/width/height style strings directly instead of allocating a temporary style snapshot object on every resize frame, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UQ`: Canvas direct resize previews now skip DOM preview reapplication when the snapped/clamped preview rect is unchanged and the target already has an active resize style snapshot, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UR`: Single-node direct move previews now skip DOM preview reapplication when the snapped/clamped move rect is unchanged and the target already has an active move preview marker, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-US`: Canvas hover-container hit updates now return early when the preferred hit candidate or null state is unchanged, avoiding redundant ref/id-update work during dense single-node move frames while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UT`: Canvas snap computation now lazily allocates alignment-guide arrays and reuses a shared empty guide array when no guides are emitted, reducing no-guide pointer-frame allocation while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UU`: Canvas snap computation now derives moving and final edge geometry directly from primitive x/y/width/height values instead of allocating intermediate rect objects in the pointer-frame snap path, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UV`: Canvas snap reference-guide processing now builds each vertical or horizontal reference edge object once per guide and reuses it across the three alignment checks, preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UW`: Canvas snap candidate bounds filtering now lazily allocates a pruned array only when an offscreen candidate is actually removed, so dense-page drag starts can reuse the already-built candidate array when every snap target remains in bounds while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UX`: Canvas move-start candidate creation now builds snap edge geometry in the same bounded pass that collects snap rects, removing the extra `createSnapCandidateEdges(moveCandidates.snapRects)` pass from drag setup while preserving home content, single Home row, header layout, direct move/resize preview cleanup, and resize readouts.
- `M166-UY`: Canvas move activation now compares squared pointer distance against the 4px threshold instead of calling `Math.hypot` during pre-drag pointer frames, preserving the same activation boundary while trimming another drag-start calculation.
- `M166-UZ`: Canvas snap candidate edge creation now calls the primitive edge factory directly and removes the extra `edges(rect)` wrapper, trimming another drag-start helper call while preserving snap geometry, home content, header layout, and direct move/resize previews.
- `M166-VA`: Canvas snap candidate edge array creation now uses a preallocated for-loop instead of a `.map(...)` callback, preserving candidate order and snap geometry while trimming another drag-start allocation path.
- `M166-VB`: Canvas snap-distance labels now lazily allocate measurement arrays only when a visible distance label exists, track nearest gaps with primitive best-rect/gap variables, and render screen coordinates directly instead of building a second mapped array each feedback render.
- `M166-VC`: Canvas drag ghost overlays now compute screen-space styles directly while rendering origin/current rects instead of first building mapped screen-rect arrays, removing another feedback-render allocation path while preserving direct move previews.
- `M166-VD`: Canvas resize readouts now compute primitive left/top coordinates directly after the null guard instead of allocating a memoized position object, trimming resize feedback render work while preserving live readout text.
- `M166-VE`: Canvas alignment-guide equality now uses an indexed loop instead of an `.every(...)` callback while preserving order-sensitive guide comparison, trimming another pointer-frame state-update guard path.
- `M166-VF`: Canvas snap-distance measurement now precomputes the active rect center once and removes center helper calls from measurement emission, preserving label positions while trimming repeated feedback math.
- `M166-VG`: Canvas feedback overlay now only mounts snap-distance labels when an interaction has both an active rect and snap candidates, skipping the distance-label component path for empty feedback states.
- `M166-VH`: Canvas feedback overlay now also gates drag ghosts, resize readouts, and multi-selection boxes before mounting their child components, skipping remaining null-render paths while preserving move/resize feedback.
- `M166-VI`: Canvas feedback current-rect derivation now uses a lazy loop and shared empty rect array instead of `map(...).filter(...)`, removing another move-feedback allocation path when rects are missing or inactive.
- `M166-VJ`: Canvas move feedback active-rect derivation now returns null for zero current rects and reuses the single current rect by reference before falling back to `unionRects`, avoiding a common single-node drag allocation.
- `M166-VK`: Canvas selection bbox derivation now keeps a single-selection rect fast path for the quick toolbar and only scans selected rects when two or more nodes are selected, avoiding a toolbar regression while trimming single-selection work.
- `M166-VL`: Canvas feedback geometry now returns `multiSelectionBboxScreen` separately from `selectionBboxScreen`, so the quick toolbar receives single-selection geometry while the feedback overlay only receives true multi-selection bbox geometry.
- `M166-VM`: Canvas move-start selection reuse now compares selected node ids with an indexed loop instead of an `.every(...)` callback, trimming another small drag-start allocation while preserving order-sensitive selection refresh behavior.
- `M166-VN`: Canvas direct-move preview eligibility now checks moving node ids with a loop instead of `.every(...)`, preserving the all-nodes-present requirement while trimming another drag-start callback path.
- `M166-VO`: Canvas move-start unlocked-selection resolution now reuses the existing selected id array when every selected node can move and lazily allocates only after encountering a locked node, reducing another common drag-start allocation.
- `M166-VP`: Canvas move-start node id set derivation now reuses the store-maintained selected id set whenever unlocked move ids reuse the selected id array, and `createMoveInteractionCandidates` accepts a readonly set because it only needs membership checks.
- `M166-VQ`: Canvas snap computation now inlines the x/y best-snap comparisons inside `computeSnapFromEdges` instead of creating per-call `considerX`/`considerY` closures on the pointer-frame path, preserving snap output while trimming drag-frame allocation work.
- `M166-VR`: Canvas snap guide emission now groups final left/right/center comparisons by candidate guide value, reducing alignment helper calls for each snap candidate and custom guide while preserving guide order and snap output.
- `M166-VS`: Canvas spacing-guide selection now creates spacing guide objects and `${gap}px` labels only after the rounded gap is inside the visible spacing-guide range and beats the current candidate, avoiding rejected guide allocations on drag frames.
- `M166-VT`: Canvas alignment-guide helper emission now checks all matching final edges before computing guide ranges, returning early when no guide will be emitted and sharing the range values across duplicate guide pushes.
- `M166-VU`: Canvas candidate spacing guide selection now computes overlap midpoint positions only after a spacing gap is valid and beats the current best guide, skipping midpoint math for discarded candidate gaps.
- `M166-VV`: Canvas snap computation now uses indexed loops for candidate edges and reference guides inside `computeSnapFromEdges`, avoiding `for...of` iterator work on drag-frame snap passes.
- `M166-VW`: Added a reusable `/ko/admin-builder` Playwright interaction regression smoke that locks down home-node presence, duplicate home-control count, top-menu overlap absence, single-selection toolbar visibility, and direct move/resize preview cleanup.
- `M166-VX`: Canvas single-node move/resize transient commits now use a dedicated store fast path instead of wrapping one rect in a `Map`, preserving selection/index reuse while trimming drop-time work for the common direct interaction path.
- `M166-VY`: Canvas overlap-click candidate resolution now maintains a capped ranked candidate list instead of filtering, sorting, and slicing every hit node, reducing click-time work while preserving z-index/depth ordering and overlap-picker behavior.
- `M166-VZ`: Canvas responsive inner-flow sibling metrics now compute once at the stage level and flow down to CanvasNode instances, avoiding per-node `visibleNodes.filter(...)` plus metric recomputation while preserving responsive reflow and nested preview-gap behavior.
- `M166-WA`: Canvas flow-layout child detection now computes once in the stage-level inner-flow state and flows down as a node-id set, so CanvasNode instances use `Set.has` instead of repeatedly resolving parent layout mode.
- `M166-WB`: Canvas visible child grouping now computes once in CanvasStageNodes and flows down to recursive CanvasNode renders, avoiding per-node `childrenMap` lookups plus `nodesById` map/filter work while preserving nested node rendering and inner flow preview gaps.
- `M166-WC`: Canvas inner-flow preview gap resolution now computes once in CanvasStageNodes and passes a compact parent/insertion object through recursive CanvasNode renders, removing the remaining full `visibleNodes` prop from CanvasNode while preserving responsive inner flow drag gap behavior.
- `M166-WD`: Top-level flow section metrics now build the node index and children map once per metric pass, reusing them across section content-height resolution instead of rebuilding descendant indexes per section while preserving overflowing descendant sizing.
- `M166-WE`: CanvasStageNodes now reuses the CanvasContainer/store `nodesById` and `childrenMap` for top-level flow section metrics through an indexed flow helper, avoiding a fresh stage-local child map build while preserving hidden-descendant traversal semantics and public flow metric callers.
- `M166-WF`: Top-level flow preview gap resolution now reuses the already-split flowSectionNodes list instead of running the generic rootVisibleNodes flow-sibling scan, preserving responsive section drag gap placement while trimming another pointer-frame stage calculation.
- `M166-WG`: CanvasStageNodes now derives visible child groups, flow-layout child membership, and responsive inner-flow sibling metrics in one visible-node relationship pass, replacing separate visibleNodes scans while preserving recursive child rendering and inner-flow drag gaps.
- `M166-WH`: Canvas flow sibling metrics now fast-path single siblings and use an indexed preallocated entries loop instead of map/sort callback allocation, preserving responsive flow margins while reducing stage/flow reorder metric work.
- `M166-WI`: Canvas flow preview gap insertion now uses a linear order-count pass instead of allocating and sorting preview siblings, preserving y/zIndex/id ordering while reducing pointer-frame work for section and responsive inner-flow drags.
- `M166-WJ`: Single-node transient rect updates now preserve selection, active group, and child indexes by only publishing the updated document and node index, avoiding redundant selection resolution on direct move/resize frames.
- `M166-WK`: Canvas pointermove RAF batching now reuses one mutable pending pointer snapshot plus a boolean flag instead of allocating a new event snapshot object for every pointermove event.
- `M166-WL`: Multi-select direct move preview now reuses a draft rect map across pointer frames, clearing and refilling it for direct DOM preview while preserving fresh maps for non-direct transient store updates.
- `M166-WM`: Multi-select direct move preview now reuses per-node draft rect objects while refilling the direct-preview rect map, reducing per-frame allocations for group moves while keeping non-direct store updates immutable.
- `M166-WN`: The builder top bar now switches to its two-row compact layout at 1080px and below, keeping viewport buttons, settings, theme, and publish controls from overlapping on compact desktop widths while preserving the single-row desktop layout.
- `M166-WO`: The home case-results section now uses `home-case-results-*` node IDs, and legacy `case-results-*` draft nodes are migrated on load so home-section audits, layers, and regression checks no longer miss the results section.
- `M166-WP`: Canvas move interactions now defer mutation-session start, geometry snapshot capture, and snap/container candidate construction until the pointer actually crosses the drag threshold, so simple clicks stay lighter while real drags keep direct DOM previews; the builder chrome also received a professional polish pass for top-bar density, viewport controls, inspector panel framing, and flatter canvas workspace styling with verified zero top-menu overlaps.
- `M166-WQ`: Builder chrome iconography now uses a shared SVG icon system for the editor rail, top-bar viewport controls, page selector, settings, theme, and locale switcher instead of ad hoc text glyphs, and the page selector now labels itself as `Page/페이지` so the top bar no longer creates a second bare `홈` control.
- `M166-WR`: Editor preferences now render as a CSS-module popover with Wix-style toggle switches, a fixed numeric grid-size control, and a shared SVG keyboard action instead of inline styles and default checkboxes, while preserving the shortcut-map and editor-preference storage contracts.
- `M166-WS`: The builder locale switcher now uses localized status labels and CSS-module dropdown/confirmation surfaces instead of inline styles and English `current/no translation/linked` badges, keeping the top-bar language control aligned with the polished editor chrome.
- `M166-WT`: The top-bar Quick Jump command palette now renders its search box, grouped results, active row, and empty state through CSS-module editor surfaces instead of inline styles, preserving keyboard traversal and direct admin navigation while matching the polished builder chrome.
- `M166-WU`: Top-bar history, preview, SEO, and publish actions now use shared SVG+text action chips, compact 1440px viewport buttons stay icon-only to prevent overlap, and the empty inspector state now renders as a polished CSS-module panel with a compact target icon and Esc shortcut chip.
- `M166-WV`: Single-node direct move frames now reuse draft absolute/local rect objects and write the snapped local clamp result in place, avoiding extra per-frame rect allocations while preserving direct DOM preview, transient commit, home content, and cleanup behavior.
- `M166-WW`: Snap computation now exposes a mutable `writeSnapFromEdges` path and single-node direct move frames write snapped positions into a reusable rect, avoiding the extra `SnapResult.snappedRect` allocation on the drag hot path while keeping the existing `computeSnapFromEdges` API intact.
- `M166-WX`: The mutable snap path now accepts reusable edge scratch objects, and single-node direct move frames reuse moving/final/reference-guide edge geometry instead of allocating those edge objects during every snap frame.
- `M166-WY`: Builder chrome received a Wix-grade visual pass: the top bar now groups navigation and preview controls so compact desktop widths do not collide, secondary actions collapse to icon buttons at 1280px, and the Pages drawer renders page rows as title + metadata lines instead of a cramped `홈대표/` single-line cluster.
- `M166-WZ`: Direct resize frames now reuse draft resize and clamped preview rect objects through mutable resize clamp helpers, reducing per-frame allocation during node resizing while preserving DOM resize previews, pointerup transient commits, and cleanup behavior.
- `M166-XA`: Resize readout state now snapshots mutable preview rects before passing them to React, and direct move/resize preview nodes disable CSS transitions while active, fixing stale resize readouts and removing drag animation lag while preserving the optimized mutable hot paths.
- `M166-XB`: Legacy home hero search nodes now repair their parent hierarchy on editor load, so the search bar no longer renders at the hero top like an overlapping menu; the global header edit badge is hidden until hover/focus/editing for a cleaner Wix-style canvas.
- `M166-XC`: Home hero editorial rhythm now widens the search experience, separates the title/subtitle/link stack, tones the scroll cue into a premium circular control, and hides closed header overlays in the editor so hidden menu text no longer reads as duplicate Home controls.
- `M166-XD`: The selection quick toolbar now uses the shared SVG editor icon system for text, link, image, duplicate, layer-order, delete, and more actions instead of raw text glyphs, keeping the most common canvas-editing affordance visually consistent with the Wix-grade top-bar chrome.
- `M166-XE`: Selected canvas nodes now suppress the hover node badge while keeping the selection size label, removing duplicate blue dimension labels from the editing surface and reducing content obstruction during normal element selection.
- `M166-XF`: The single-node selection overlay now hides its default size label, leaving only the outline, resize handles, toolbar, and live resize readout so selected content is no longer covered by a static dimension pill.
- `M166-XG`: The layout inspector viewport controls now render as full-width Wix-style device rows instead of cramped 3-column cards, the show-on-device toggles use CSS-module tool controls instead of inline styling, Korean `데스크톱` copy is consistent with the top bar, and direct-preview nodes get an extra compositing hint while preserving zero top-menu overlaps and direct move-preview cleanup.
- `M166-XH`: The Pages drawer now uses polished card-style page rows, a stable grid list gap, stronger page title/slug hierarchy, and a cleaner loading row; the top page picker now separates the current page label with a dedicated truncation class so the editor no longer reads like a cramped `페이지홈` control while preserving zero top-menu overlaps.
- `M166-XI`: The Add/catalog panel now uses polished quick buttons, structured category headers, and fixed-height vertical text/media widget tiles with line-clamped copy, making the left tool surface feel closer to a professional Wix-style editor while preserving top-menu overlap and builder interaction regressions.
- `M166-XJ`: The Add drawer's reusable component library now uses localized Wix-style saved-design chrome, CSS-module cards, stable save controls, empty states, and saved-item rows instead of inline temporary styling; rail buttons now expose stable data attributes for localized Playwright coverage of layers/history flows.
- `M166-XK`: The Design drawer audit surface now caps its recommendation panel at 560px with contained scrolling, clamps verbose recommendation copy, compacts quality/system-fit/count/change chips, and keeps priority/change-detail lists scrollable so design presets are reachable much sooner while preserving zero top-menu overlap.
- `M166-XL`: The Design drawer and Site Settings component preset cards now use localized designer-system labels, recommendations, descriptions, variant-change labels, quality signals, and action copy for ko/zh/en instead of exposing internal English preset names, while preserving stable preset keys and regression coverage.
- `M166-XM`: The Layers drawer row chrome now uses fixed SVG icon buttons, localized tooltips/meta labels, and semantic Korean/Chinese/English layer names for technical home-section labels, removing visible `BOX`/`eye`/`open`/`...` debug text and repeated `home` labels while preserving layer drag, visibility, lock, and top-bar regression coverage.
- `M166-XN`: The Navigation drawer now uses CSS-module card rows, fixed SVG action buttons, localized count/add/path copy, and stable row-id selectors instead of inline styles and visible `↑`/`↓`/`✎`/`✕`/`+ Mega` action text, while preserving real navigation save, reorder, child-menu, delete, published HTML, and top-bar regression coverage.
- `M166-XO`: The History drawer undo timeline now uses localized ko/zh/en copy, SVG undo/redo controls, CSS-module timeline rows, localized snapshot/node/current labels, and a cleaned version-history drawer description instead of visible `Undo stack`/`Undo`/`Redo`/`current` developer text and inline timeline styling, while preserving undo/redo and advanced-panel regression coverage.
- `M166-XP`: The Version History modal now uses an independent CSS module with a wider two-column revision/compare layout, SVG close control, card-style timeline rows, source/diff badges, polished confirmation overlay, and stable revision-source hooks instead of the deleted `VersionHistoryPanel.styles.ts` inline style object layer, while preserving focus trap, restore confirmation, and top-bar regression coverage.
- `M166-XQ`: The Save Section modal now uses a dedicated CSS module, SVG close control, structured preview/form/action regions, localized copy, and stable regression hooks instead of inline JSX styling and hard-coded Korean/English labels; saved-section thumbnails no longer emit `style=` attributes, and the context-menu modal focus trap remains covered by Playwright.
- `M166-XR`: The Add drawer's built-in section template market and saved-section library now share a CSS-module section-library chrome with polished two-column cards, stable category filters, SVG saved-section action buttons, zero inline styles inside built-in/saved section surfaces, and localized/stable Playwright selectors for template insertion and saved-section reuse.
- `M166-XS`: The builder top bar now constrains quick-jump/member-preview selects in compact desktop widths, Pages drawer filtering hides known save-section test pages so Home remains the only home row, and CanvasNode drag start avoids a duplicate selection update before direct DOM move preview begins.
- `M166-XT`: The Pages drawer row controls now use shared SVG drag-handle and more-menu icons instead of raw glyph text, with stable style helpers and render-contract coverage so the page list reads as polished product chrome instead of debug UI.
- `M166-XU`: The top-bar page selector now exposes separated `Page/Home` text and a stable selector hook, and the Pages member-access settings dialog uses shared polished style helpers for its overlay, panel, fields, page picker, and primary action.
- `M166-XV`: The Pages new-page slug prompt now uses polished shared style helpers for its overlay, panel, input, navigation toggle, and actions, and its visible Korean/Chinese/English title now uses user-facing page path/address wording instead of developer `Slug` terminology.
- `M166-XW`: The page template showroom now routes category, template-card, preview-button, action-button, and blank-page-card hover chrome through shared style helpers, removing another set of ad hoc JSX hover styles from the new-page flow.
- `M166-XX`: The page template showroom preview panel now routes the viewport toolbar, active viewport buttons, preview body/frame, detail aside, definition list, quality chip, and tag chips through shared style helpers, while the preview canvas receives a more professional framed presentation and verified desktop frame sizing.
- `M166-XY`: The page template showroom cards now route category, featured, and meta chips through shared style helpers with bordered product-style chip chrome, removing the remaining inline JSX chip color spreads from the new-page template card surface.
- `M166-XZ`: The page template showroom search and filter controls now render as a polished toolbar panel with a bordered gradient surface, focused search ring, result-count pill, denser filter selects, and keyboard-visible focus styling.
- `M166-YA`: The page template showroom now has narrow-screen responsive polish: the content panel tightens, filters become full-width, the category sidebar hides cleanly, and the nested template preview collapses from two columns into a stacked canvas/details layout with mobile-safe toolbar overflow.
- `M166-YB`: The right inspector panel now moves the anchor helper, active tab-pane animation, and composite-surface editor chrome onto CSS-module classes instead of inline JSX styling, with render-contract coverage and verified local editor readiness after restarting the stale 3001 dev server.
- `M166-YC`: The inspector's style-origin and breakpoint override badges now use dedicated CSS modules instead of inline JSX styles, with specificity hardened against generic inspector span rules so origin colors and dot markers render correctly in the Style tab.
- `M166-YD`: The Style tab's source visualizer, source rows, binding summary, and section wrappers now use `StyleTab.module.css` instead of inline style objects, keeping the right inspector's style panel visually consistent with the polished badge chrome.
- `M166-YE`: The Style tab's theme-binding badge now uses `StyleTab.module.css` data-tone selectors instead of `getThemeBindingBadgeStyle(...)` inline JSX styling, keeping button variant binding summaries aligned with the polished source-panel chrome.
- `M166-YF`: Theme binding badges now use one shared `ThemeBindingBadge` CSS-module component across StyleTab, ColorPickerAdvanced, ThemeTextPresetPicker, and InspectorControls, removing the old inline `getThemeBindingBadgeStyle(...)` helper entirely.
- `M166-YG`: ColorPickerAdvanced now renders its trigger, popover, swatch grid, inputs, EyeDropper action, and error text through `ColorPickerAdvanced.module.css`, leaving only runtime color CSS variables inline.
- `M166-YH`: ThemeTextPresetPicker now renders its trigger, popover, option rows, no-preset label, preview text, and preset names through `ThemeTextPresetPicker.module.css`, leaving only dynamic preview typography CSS variables inline.
- `M166-YI`: FontPickerAdvanced now renders its trigger, popover, filters, inputs, list rows, notes, highlight, and empty state through `FontPickerAdvanced.module.css`, and SiteSettingsModal no longer loops custom-theme preset loading from an unstable function dependency.
- `M166-YJ`: SiteSettingsTypographyTab now renders site fonts, typography scale controls/preview rows, and theme text preset cards through `SiteSettingsTypographyTab.module.css`, leaving only live preview typography CSS variables inline.
- `M166-YK`: SiteSettingsGeneralTab now renders the first site-settings form through `SiteSettingsGeneralTab.module.css`, replacing imperative input focus border mutations with CSS focus-visible rings and restoring the local builder interaction regression after a broken Reveal JSX wrapper was fixed.
- `M166-YL`: SiteSettingsAdvancedTab now renders motion controls and theme color rows through `SiteSettingsAdvancedTab.module.css`, removing shared modal inline styles and imperative focus border mutations from the advanced settings pane.
- `M166-YM`: SiteSettingsDarkModeTab now renders runtime controls, light/dark preview cards, chips, and dark color rows through `SiteSettingsDarkModeTab.module.css`, leaving only live preview theme values as CSS variables.
- `M166-YN`: SiteSettingsMobileTab now renders mobile header controls, bottom action toggles, and bottom CTA action cards through `SiteSettingsMobileTab.module.css`, removing the remaining static inline styles from the mobile settings pane.
- `M166-YO`: SiteSettingsPresetsTab now renders component presets, design token import/export, radius/shadow presets, My Themes, pending preset confirmation, and built-in theme presets through `SiteSettingsPresetsTab.module.css`, leaving only live preset preview values as CSS variables.
- `M166-YP`: SiteSettingsModal now renders its split shell, tab rail, tab buttons, active indicator, content scroll area, loading state, and footer notices through `SiteSettingsModal.module.css`, removing the last inline styles from the Site Settings modal shell itself.
- `M166-YQ`: SeoPanel and SeoPanelBasicsTab now render the SEO modal shell, tabs, footer actions, base search fields, counters, checkboxes, and Google preview through CSS modules instead of shared inline style objects.
- `M166-YR`: SeoPanelSocialTab and SeoPanelHreflangTab now render social share fields, OG/Twitter preview, hreflang rows, missing-locale notices, and sitemap status cards through CSS modules instead of shared inline style objects.
- `M166-YS`: SeoPanelAdvancedTab and SeoPanelAssistantTab now render advanced meta/JSON-LD controls, assistant tasks, validation status, and issue cards through CSS modules with data-tone states, and the obsolete shared `SeoPanel.styles.ts` inline-style layer is removed.
- `M166-YT`: PublishModal and PublishModalPreflight now render preflight cards, diff summary, scheduled publish controls, issue rows, success/error states, and actions through `PublishModal.module.css`, removing the obsolete `PublishModal.styles.ts` inline-style layer.
- `M166-YU`: PreviewModal now renders its full-screen preview chrome, device switcher, action buttons, stage shell, desktop browser frame, tablet/mobile device frame, iframe, unpublished state, and footer through `PreviewModal.module.css`, leaving only scale/device dimensions as CSS variables and removing the embedded JSX `<style>` block.
- `M166-YV`: A11yPanel now renders the inspector accessibility summary, pass state, issue list, severity dots, suggestions, and node metadata through `A11yPanel.module.css`, removes static inline styles and emoji severity markers, and counts issue severities in one pass.
- `M166-YW`: CanvasStageToolbar now renders the floating grid/undo/redo controls through `CanvasStageToolbar.module.css`, removing the active-grid inline style object and grid-size label/input inline styles while preserving memoized handlers and compact desktop overlap coverage.
- `M166-YX`: CanvasNodeBadge now renders locked, sticky, anchor, animation, and link badge states through `CanvasNodeBadge.module.css` data-tone classes, removes the remaining inline badge colors/pointer styles, and promotes link badges to keyboard-focusable buttons without breaking canvas hover or drag regressions.
- `M166-YY`: Single-node direct move preview now skips repeated pointer frames when the rounded canvas rect input has not changed, avoiding redundant snap/container-hit work and DOM writes while preserving direct move previews, transient commit cleanup, home content, and compact top-bar overlap coverage.
- `M166-YZ`: ElementCommentsPanel now renders the selected-node comments thread, empty state, resolved badges, action buttons, textarea, and submit state through `ElementCommentsPanel.module.css`, removing static inline styles while preserving localized copy and the advanced-panel comment workflow.
- `M166-ZA`: ShortcutsHelpModal now renders its shortcut grid, group headings, rows, keycaps, and close hint through `ShortcutsHelpModal.module.css`, removing the remaining static inline styles while preserving localized shortcut copy and the live help-modal event path.
- `M166-ZB`: KeybindingsModal now renders the shortcut-remapping dialog, table, combo inputs, reset/cancel/save actions, and responsive narrow-screen layout through `KeybindingsModal.module.css`, removing static inline JSX styles while preserving localized shortcut-setting copy, focus behavior, and editor-preference persistence.
- `M166-ZC`: MoveToPageModal now renders its error notice, target page list, empty state, destination rows, icon badge, status copy, hover, disabled, and narrow-screen states through `MoveToPageModal.module.css`, removing direct hover DOM style mutation while preserving localized move-to-page copy and move submission behavior.
- `M166-ZD`: Added a non-destructive admin-builder click audit that opens the live local builder and clicks through the top-bar/page selector, left rail drawers, columns page shortcut plus writing drawer, layer search, viewport switching, member preview, quick jump, editor preferences/keybindings, preview, SEO, and publish preflight surfaces, preserving top-bar overlap and console-error guards.
- `M166-ZE`: Added a right-inspector and selection-toolbar click audit that creates a temporary builder page, drags a node with direct DOM preview, edits layout/style/content/animation/comment controls, duplicates from the quick toolbar, edits a button link popover, and guards console/page errors; LinkPicker now de-duplicates anchor/page datalist options so repeated `/ko` page paths no longer emit React duplicate-key warnings during inspector or toolbar link editing.
- `M166-ZF`: LinkPicker now renders its link, target, advanced metadata, invalid-state, and clear-link controls through `LinkPicker.module.css` instead of inline style objects, with duplicate-safe page/anchor suggestions, localized copy, and real inspector/toolbar link-edit browser coverage preserved.
- `M166-ZG`: ButtonInspector now renders label, link, variant, HTML tag, CSS class, hints, and focus states through `ButtonInspector.module.css` instead of static inline style objects, preserving localized button copy, LinkPicker integration, and real button link-edit browser coverage.
- `M166-ZH`: ContactFormInspector now renders field chips, localized labels, submit/action controls, active/disabled/focus states, and stable inspector hooks through `ContactFormInspector.module.css` instead of inline style objects, preserving localized conversion-widget copy and the live home/menu/direct-node movement regressions.
- `M166-ZI`: CTA banner, custom embed, and form-submit inspectors now render their labels, controls, textarea/code fields, checkbox row, focus-visible states, and stable inspector hooks through CSS modules instead of legacy static inline style constants, leaving only the CTA's live background swatch value inline while preserving localized conversion/form copy and live builder interaction regressions.
- `M166-ZJ`: Form input, select, textarea, checkbox, radio, date, and file inspectors now share `FormControlInspector.module.css` for field stacks, controls, checkboxes, focus-visible states, disabled states, and stable field-inspector hooks, removing the remaining `selectStyle` inline constants from the field inspector cluster while preserving localized form copy and live builder home/menu/direct-node regressions.
- `M166-ZK`: Form payment and signature advanced-widget inspectors now reuse the shared `FormControlInspector.module.css` chrome with stable advanced-inspector hooks and no inline checkbox row styles, preserving localized advanced form copy plus the live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZL`: MapInspector now renders office presets, address textarea, zoom controls, disabled states, and focus-visible rings through `MapInspector.module.css` instead of static inline style constants, preserving localized map copy plus the live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZM`: FloatingChat and NotificationBar inspectors now render provider/message/link/tone/placement controls and checkbox rows through dedicated CSS modules with stable inspector hooks, preserving localized social/notification copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZN`: Breadcrumbs and AnchorMenu inspectors now render item textarea, separator/sticky/home/offset/color controls, disabled states, and focus-visible rings through dedicated CSS modules with stable inspector hooks, preserving localized navigation copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZO`: AddressBlock and BusinessHours inspectors now render address/contact/directions fields, rows/note textareas, checkbox rows, disabled states, and focus-visible rings through dedicated CSS modules with stable inspector hooks, preserving localized location copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZP`: Video and ShareButtons inspectors now render URL/mode/poster/title/provider/layout/size controls, checkbox chips, disabled states, and focus-visible rings through dedicated CSS modules with stable inspector hooks, preserving localized media/social copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZQ`: SocialBar and SocialEmbed inspectors now render social item textareas, provider/layout/style/size/color/count controls, header toggles, disabled states, and focus-visible rings through dedicated CSS modules with stable inspector hooks, preserving localized social copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZR`: Progress and Rating inspectors now render label/value/style/color/max controls, percentage/value toggles, disabled states, and focus-visible rings through dedicated CSS modules with stable inspector hooks, preserving localized interactive copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZS`: BarChart, LineChart, PieChart, and Counter inspectors now share `DataWidgetInspector.module.css` for label/input/textarea/checkbox/inline-field chrome with stable data-widget inspector hooks, preserving localized data widget copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZT`: Countdown and Timeline inspectors now render datetime/text/select/checkbox and timeline textarea controls through dedicated CSS modules with stable inspector hooks, preserving localized interactive/navigation copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZU`: PricingTable and ServiceFeatureCard inspectors now render plan textarea plus service icon/title/description/CTA/variant controls through dedicated CSS modules with stable inspector hooks, preserving localized marketing/service copy plus live builder home/menu/direct-node and inspector-click regressions; Computer Use still timed out for direct Chrome control, so the local Chrome URL was opened separately for manual review.
- `M166-ZV`: MenuBar and MultiLocationMap inspectors now render orientation/style/active/items/mobile toggles and title/locations/active-index/list controls through dedicated CSS modules with stable inspector hooks, preserving localized navigation/location copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZW`: FaqList and ColumnList inspectors now render source/category/limit/toggle/Q&A item controls and locale/limit/category/hint controls through dedicated CSS modules with stable inspector hooks, preserving localized FAQ/domain-card copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZX`: GalleryInspector now renders layout/range/caption/filter/pro/autoplay/thumbnail and image-list add/remove controls through a dedicated CSS module with a stable gallery inspector hook, preserving localized gallery copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZY`: CodeBlock and BookingWidget inspectors now render title/language/code/line-number/caption and booking section/filter/completion/form controls through dedicated CSS modules with stable inspector hooks, preserving localized utility/booking copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZ`: AttorneyCard and ColumnCard inspectors now render name/title/variant/photo/specialty and slug/locale/title/date/summary controls through dedicated CSS modules with stable inspector hooks, preserving localized domain-card copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZA`: BlogArchive, BlogFeed, and BlogPostCard inspectors now share polished CSS-module section/field/control/checkbox chrome with stable inspector hooks, preserving localized blog copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZB`: BlogCategories and FeaturedPosts inspectors now join the shared blog CSS-module chrome for layout/display/color/limit controls with stable inspector hooks, preserving localized blog category/featured copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZC`: ContainerInspector now renders label/padding/card variant/link/layout/layout-items/anchor/sticky/flex/grid controls through a dedicated CSS module with a stable container inspector hook, preserving localized container/gallery copy plus live builder home/menu/direct-node, inspector-click, and headed drag-preview timing checks.
- `M166-ZZD`: HeadingInspector now renders theme preset, heading text, font, level, color, size, weight, decoration, line-height, spacing, and alignment controls through a dedicated CSS module with a stable heading inspector hook, preserving localized typography copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZE`: TextInspector now renders rich-text shortcuts, text, typography, text effects, background, link, and text-shadow controls through a dedicated CSS module with a stable text inspector hook, removing its sandbox CSS dependency while preserving localized text copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZF`: ImageInspector now renders asset actions, source/alt/fit/click/link controls, media interactions, before/after compare, SVG/GIF settings, and crop notices through a dedicated CSS module with a stable image inspector hook, removing its sandbox CSS dependency while preserving localized image copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZG`: FormInspector now renders form name, submission target, success, redirect, captcha, steps, auto-reply, layout, flex, and grid controls through a dedicated CSS module with a stable form inspector hook, removing inline select/section styles while preserving localized form copy plus live builder home/menu/direct-node and inspector-click regressions, including headed Chromium click/drag reruns; Computer Use was retried for direct local Chrome control but still timed out, so Playwright remains the reliable local UI verification path.
- `M166-ZZH`: TestimonialCarousel Inspector now renders testimonial textarea, autoplay control, and star toggle through a dedicated CSS module with a stable testimonial inspector hook, removing its remaining inline textarea and checkbox row styles while preserving localized marketing copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZI`: VideoEmbed Inspector now renders provider, source, poster, autoplay, loop, muted, and controls fields through a dedicated CSS module with a stable video-embed inspector hook, removing inline checkbox row styles while preserving localized utility/video copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZJ`: ComparisonTable Inspector now renders columns and rows textareas through a dedicated CSS module with a stable comparison-table inspector hook, removing inline textarea font styles while preserving localized marketing table copy plus live builder home/menu/direct-node and inspector-click regressions.
- `M166-ZZK`: Lottie Inspector now renders URL, label, speed, autoplay, loop, empty-host guidance, and unsupported-host warning chrome through a dedicated CSS module with a stable lottie inspector hook, removing static inline help/link/warning/checkbox styles while preserving localized media copy plus live builder home/menu/direct-node and inspector-click regressions; after adding the new module the local Next dev server was restarted so the CSS file entered the module graph, and a direct Chromium timing probe measured selection toolbar visibility at 127ms, then 61-66ms on repeated warmed clicks.
- `M166-ZZL`: Audio Inspector now renders provider, source URL, title, artist/source, show-controls, and autoplay chrome through a dedicated CSS module with a stable audio inspector hook, removing static inline checkbox row styles while preserving localized media copy plus live builder home/menu/direct-node and inspector-click regressions; after adding the new module the local Next dev server was restarted, the first cold browser pass still hit the known fixed 150ms selection-toolbar sample once, and the warmed rerun passed the full interaction regression.
- `M166-ZZM`: TeamMemberCard Inspector now renders name, role, bio, avatar, social links, and card style controls through a dedicated CSS module with a stable team-member-card inspector hook, removing the remaining social textarea inline font style while preserving localized marketing copy plus live builder home/menu/direct-node and inspector-click regressions; after adding the new module the local Next dev server was restarted, the first cold browser pass again hit the known fixed 150ms selection-toolbar sample once, and the warmed rerun passed the full interaction regression.
- `M166-ZZN`: Decomposed standard lawyer pages now preserve attorney staff-card child coordinates as card-relative instead of double-offsetting right-column/lower-row descendants, preventing hidden or overlapping public content when nodes sit between sections. Claude Code Opus 4.8 ran a read-only audit of the suspected regression, and Codex locked the invariant with `decompose-attorney-grid.test.ts` plus a live published-page Playwright proof that current decomposed lawyer cards keep photo/info/name/role/email/action children inside their cards without card overlap. The same Playwright proof now also guards the page-header accent line: a red-first assertion caught `.page-header::after` crossing the Korean H1, and `ContainerElement` gives published page-header sections their canvas rect as inner min-height so the divider renders below the title.
- `M166-ZZO`: Home builder fidelity now keeps composite/public sections and decomposed editor sections from regressing into missing content or overlap: internal `g-editor-*` slugs no longer drive public-route fidelity probes, the composite `home-offices` flow height was remeasured to 909px, decomposed home root sections inherit the published body font inside the editor canvas, and Codex verified 6 Playwright home-fidelity checks plus a fresh `/ko/admin-builder` screenshot/metric probe showing 9 home root sections with zero section overlap and zero top-bar control overlap. Claude Code Opus 4.8 returned a read-only `VERDICT: PASS`; Codex resolved its evidence caveat with the recorded Playwright run and local draft metadata showing the active builder home is decomposed.
- `M166-ZZP`: F77 pay-later booking now has public-widget browser proof: `bookings-pay-later.playwright.ts` creates a paid collect-later service, publishes a real `pw-*` builder page with a booking widget, drives the public Korean flow, verifies no payment panel or `/api/booking/payment-intent` request appears, and confirms `/api/booking/book` persists the reservation as `unpaid` with full balance and no `paymentIntentId`.
- `M166-ZZQ`: F77 deposit refunds now use captured online-payment amounts instead of service total as the refund base: shared cancellation refund logic treats `partially_paid` deposit bookings as refundable, caps full/partial refund math to `onlinePaidAmount`/due-now fallback, and `bookings-deposit-refund.playwright.ts` proves the real webhook-to-customer-cancel API surface returns `refundAmountCents: 1500` for a 5000 TWD service with a 1500 TWD deposit.
- `M166-ZZR`: F77 staff-specific pricing now has a real API path: `BookingService.staffPriceOverrides` is schema-validated for paid services, `bookingServicePriceSnapshot(service, { staffId })` uses the selected staff total, the public widget sends `staffId` when creating payment intents, and `bookings-staff-pricing.playwright.ts` proves a 5000 TWD base service charges/books 8000 TWD for the premium staff through payment-intent, public booking, and admin booking APIs.
- `M166-ZZS`: F77 staff/resource pricing editor depth now has real services-editor proof: `bookings-staff-pricing-editor.playwright.ts` opens `/ko/admin-builder/bookings/services?edit=...`, enters both a premium staff override and required-resource override through the UI, verifies service card summary chips, reload-persisted inputs, API maps, and then clears the last staff and resource overrides to prove PATCH sends explicit empty maps and removes stale summary chips. Fresh visual evidence under `.omo/evidence/bookings-staff-pricing-editor/visual/` covers desktop/mobile cards plus separate staff/resource price fieldsets, and Claude Code Opus final delta gate returned `PASS`.
- `M172-AD`: Publish modal site-wide translation review now requires a separate other-page warning acknowledgement before direct or scheduled publish can proceed; Playwright creates two source-only pages to prove page-warning override alone keeps publish/schedule disabled until the site-wide review is acknowledged.
- `M172-AE`: Direct and scheduled publish now persist acknowledged site-wide translation review snapshots as `publish.translation_site_review` audit events; Playwright schedules a source-only Korean page and reads `/api/builder/site/audit` to prove the scheduled job records action, source locale, counts, review link, and job metadata.
- `M172-AF`: Organization-level translation release policy now stores a site policy, adds a publish-check blocker and modal policy panel for `block-other-page-warnings`, and enforces the same blocker on direct and scheduled publish routes before write operations.
- `M172-AG`: Role-scoped translation release approvals now store warning-fingerprinted approval requests, expose guarded request/decision APIs, add required/pending publish-check blockers, render a modal request panel, and enforce the same approval gate on direct and scheduled publish routes before writes.
- `M172-AH`: Site settings Advanced now includes a translation release approval settings panel for role requirements plus a pending reviewer queue, and `/api/builder/site/translation-release-approvals?status=pending` supports status-filtered queue reads.
- `M172-AI`: Translation Editor line-segment review now includes per-line source apply controls that copy a selected source row into the matching target row while preserving surrounding translated rows; Playwright proves apply, edit, save, reload, and aligned status.
- `M172-AJ`: Translation Editor line-segment review now includes per-target source-line remap selects, so source row N can be applied to target row M while preserving source line count alignment and surrounding translated rows.
- `M172-AK`: Translation Editor line-segment review now includes per-target append-source selects, so translators can merge multiple source rows into one target row while preserving surrounding rows and save/reload alignment.
- `M172-AL`: Translation Editor line-segment review now includes per-target split `+` buttons, so translators can insert a blank target line below a row while preserving source-line structure and surrounding target rows.
- `M172-AM`: Translation Editor line-segment review now includes per-target delete `-` buttons, so translators can remove a target line after split/non-proportional edits while preserving source-aligned row count and surrounding rows.
- `M172-AP`: Manual rich-text translation patches now preserve spaces between adjacent translated link/underline words as unmarked separator nodes instead of expanding the neighboring mark boundary; unit and edit-endpoint integration proof cover `Visit Again` through `/api/builder/translations/edit`.
- `M172-AQ`: Manual rich-text translation patches now preserve punctuation-plus-space separators between adjacent translated link/underline runs as unmarked text nodes; mapper internals were split into `rich-text-node-replacements.ts`, and edit-endpoint integration proof covers `Visit, Again` through `/api/builder/translations/edit`.
- `M172-AR`: Manual rich-text translation patches now preserve trailing terminal punctuation after the final adjacent translated mark as an unmarked text node; unit and edit-endpoint integration proof cover `Visit Again.` through `/api/builder/translations/edit`.
- `M172-AS`: Manual rich-text translation patches now preserve paired punctuation around adjacent translated link/underline runs as unmarked text nodes; unit and edit-endpoint integration proof cover `(Visit Again)` through `/api/builder/translations/edit`.
- `M172-AT`: Manual rich-text translation patches now preserve double quotes around adjacent translated link/underline runs as unmarked text nodes; unit and edit-endpoint integration proof cover `"Visit Again"` through `/api/builder/translations/edit` while apostrophes remain outside the boundary separator set.
- `M172-AU`: Manual rich-text translation patches now preserve square brackets around adjacent translated link/underline runs as unmarked text nodes; unit and edit-endpoint integration proof cover `[Visit Again]` through `/api/builder/translations/edit`.
- `M172-AV`: Manual rich-text translation patches now preserve curly braces around adjacent translated link/underline runs as unmarked text nodes; unit and edit-endpoint integration proof cover `{Visit Again}` through `/api/builder/translations/edit`.
- `M172-AW`: Manual rich-text translation patches now preserve source separator runs that are split across multiple plain text nodes between adjacent link/underline runs; unit and edit-endpoint integration proof cover `Visit / Again` through `/api/builder/translations/edit`.
- `M172-AX`: Manual rich-text translation patches now preserve compact source separator nodes between adjacent link/underline runs; boundary parsing was split into `rich-text-boundary-parser.ts`, and unit/edit-endpoint integration proof cover `Visit/Again` through `/api/builder/translations/edit`.
- `M172-AY`: Manual rich-text translation patches now preserve Unicode dash separators between adjacent link/underline runs; unit and Playwright HTTP route proof cover `Visit–Again` through `/api/builder/translations/edit`.
- `M172-AZ`: Manual rich-text translation patches now preserve curly double quotes around adjacent marked runs as unmarked text nodes; unit and Playwright HTTP route proof cover `“Visit Again”` through `/api/builder/translations/edit` while apostrophe-style contractions remain outside the separator set.
- `M172-BA`: Manual rich-text translation patches now preserve ampersand symbol separators between adjacent marked runs as unmarked text nodes; unit and Playwright HTTP route proof cover `Visit & Again` through `/api/builder/translations/edit` while compact ampersand words still require explicit source separators.
- `M172-BB`: Translation batch SSE stream frames now carry ordered top-level sequence telemetry; unit and Playwright API proof cover the real `/api/builder/translations/translate-batch/stream` route emitting progress as `sequence: 1` before result as `sequence: 2`.
- `M172-BE`: OpenAI native batch translation now requests upstream streaming when Translation Manager progress is attached and surfaces `provider-partial` SSE/UI telemetry with chunk count and partial-character counts before final result/save; unit, real route, and browser proof cover the stream path while F115 remains yellow for live credential smoke, broader provider stream/token QA, and reviewer reporting depth.
- `M172-BF`: Provider readiness reports now retain current-process smoke-run history newest-first and render it in the Translation Manager `최근 점검` panel; unit, real route, browser, typecheck, lint, security, build, no-excuse, LOC, and diff gates passed while F115 remains yellow for durable/live production credential smoke, broader provider stream/token QA, and reviewer reporting depth.
- `M172-BG`: Provider readiness smoke history now persists through a file-backed/private-Blob-capable store and is overlaid into guarded GET/POST readiness reports; unit, real route, typecheck, lint, security, no-excuse, and Playwright API proof passed while F115 remains yellow for live production credential smoke, scheduled/provider-wide aggregation, broader provider stream/token QA, and reviewer reporting depth.
- `M172-BH`: Daily scheduled provider smoke aggregation now runs all OpenAI/DeepL checks through `/api/cron/translation-provider-smoke`, records durable provider-wide history, and has real HTTP proof that readiness reports the scheduled history; F115 remains yellow for live production credential evidence, broader provider stream/token QA, and reviewer reporting depth.
- `M172-BI`: Provider readiness now returns reviewer-readable `smokeSummary` totals plus per-provider latest status, and Translation Manager renders the localized `점검 리포트`; unit, real route, UI, typecheck, lint, security, build, no-excuse, and LOC gates passed while F115 remains yellow for live production credential evidence, broader provider stream/token QA, and deeper reviewer workflow reporting.
- `M172-BJ`: Provider readiness smoke summaries now include `freshness` (`missing`/`fresh`/`stale`) and `ageMinutes`, and the Translation Manager `점검 리포트` shows localized freshness copy such as `최근 점검 18분 전 · 정상`; red-first unit/route proof, real API/UI Playwright, screenshot inspection, typecheck, lint, security, build, no-excuse, and diff gates passed while F115 remains yellow for live production credential evidence, broader provider stream/token QA, and deeper reviewer workflow reporting.
- `M172-BK`: Provider readiness smoke summaries now include reviewer status (`no_history`/`healthy`/`needs_attention`/`stale`) and action items (`run_provider_smoke`, `check_scheduled_smoke`, `inspect_failures`, `configure_provider`), and the Translation Manager `점검 리포트` shows localized `검토 상태` plus `다음 조치`; red-first unit/route proof, real API/UI Playwright, visual QA, typecheck, lint, security, build, no-excuse, and diff gates passed while F115 remains yellow for live production credential evidence, broader provider stream/token QA, and real multi-user reviewer reporting depth.
- `M172-BL`: OpenAI streaming batch telemetry now carries cumulative stream totals into the final `provider-response` event, so `/api/builder/translations/translate-batch/stream` and the Translation Manager progress panel show `응답 84ms · 부분 42자 · 청크 2` at stream completion; red-first router/route proof, parser regression, browser UI proof, and focused visual QA passed while F115 remains yellow for live production credential evidence, true token/cost accounting, and real multi-user reviewer reporting depth.
- `M172-BM`: Manual rich-text translation patches now preserve compact dash separators between adjacent marked runs even without explicit source separator nodes; red-first unit proof showed `Visit–Again` previously persisted as `Visit–` inside the first mark, and Playwright HTTP route proof now saves bold/link `Visit`, plain `–`, and italic/underlined `Again` through `/api/builder/translations/edit`.
- `M172-BN`: Manual rich-text translation patches now preserve compact slash separators between adjacent link/underline runs inside nested ordered/bullet-list items; red-first unit proof showed `Visit/Again` previously kept `/` inside the link mark, and Playwright HTTP route proof now saves linked `Visit`, plain `/`, and underlined `Again` while preserving ordered-list start attrs and nested bullet wrappers.
- `M172-BO`: OpenAI batch streaming now requests upstream usage frames with `stream_options.include_usage`, preserves prompt/completion/total tokens plus configured-rate estimated USD cost into final `provider-response` telemetry, and renders `토큰 30 · $0.000013` in the Translation Manager; red-first router/route proof, client parser regression, browser UI proof, visual QA screenshot inspection, typecheck, and lint passed while F115 remains yellow for live production credential evidence and real multi-user reviewer reporting depth.
- `M172-BP`: Manual rich-text translation patches now preserve compact colon separators between adjacent marked runs without explicit source separator nodes; red-first edit-store proof showed `Visit:Again` previously persisted as `Visit:` inside the first mark, and Playwright HTTP route proof now saves marked `Visit`, plain `:`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BQ`: Manual rich-text translation patches now preserve compact semicolon separators between adjacent marked runs without explicit source separator nodes; red-first edit-store proof showed `Visit;Again` previously persisted as `Visit;` inside the first mark, and Playwright HTTP route proof now saves marked `Visit`, plain `;`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BR`: Manual rich-text translation patches now preserve compact pipe separators between adjacent marked runs without explicit source separator nodes; red-first edit-store proof showed `Visit|Again` previously persisted as `Visit|` inside the first mark, and Playwright HTTP route proof now saves marked `Visit`, plain `|`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BS`: Manual rich-text translation patches now preserve compact ampersand separators between adjacent multi-character marked runs without explicit source separator nodes while keeping single-letter abbreviations like `R&D` out of the unmarked separator path; red-first edit-store proof showed `Visit&Again` previously persisted as `Visit&` inside the first mark, and Playwright HTTP route proof now saves marked `Visit`, plain `&`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BT`: Manual rich-text translation patches now preserve compact plus separators between adjacent multi-character marked runs without explicit source separator nodes while keeping single-letter abbreviations like `R+D` out of the unmarked separator path; red-first edit-store proof showed `Visit+Again` previously persisted as `Visit+` inside the first mark, and Playwright HTTP route proof now saves marked `Visit`, plain `+`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BU`: Manual rich-text translation patches now collapse split source separator runs when translators compact the target separator; red-first edit-store proof showed source `link + space + slash + space + underline` with target `Visit/Again` previously split proportionally as `Vis`/`it`/`/`/`Ag`/`ain`, and Playwright HTTP route proof now saves marked `Visit`, plain `/`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BV`: Manual rich-text translation patches now run explicit source-separator remapping before the short-target proportional guard, so compact translations shorter than the source text-node count preserve marks; red-first edit-store proof showed `A/B` from source `link + space + slash + space + underline` previously collapsed to one plain rich-text node, and Playwright HTTP route proof now saves linked `A`, plain `/`, and underlined `B` through `/api/builder/translations/edit`.
- `M172-BW`: Manual rich-text translation patches now preserve compact middle-dot separators between adjacent marked runs without explicit source separator nodes; red-first edit-store proof showed `Visit·Again` previously persisted as `Visit·` inside the first link mark, and Playwright HTTP route proof now saves marked `Visit`, plain `·`, and marked `Again` through `/api/builder/translations/edit`.
- `M172-BX`: Translation release approval reviewer queues now report full-context pending/approved/rejected totals, pending-by-role counts, and oldest pending age while status filters still return the requested row subset; the Site Settings approval panel renders the localized report and Playwright drives request/approve through the live builder UI.
- `M172-BY`: Translation release approval reviewer reports now include per-user request/review activity counts, approved/rejected decision breakdowns, and latest activity timestamps; the Site Settings approval panel renders the localized actor activity line during the same live request/approve flow.
- `M172-BZ`: Translation release approval reviewer reports now include a 24-hour stale-pending escalation summary with stale totals, stale-by-role counts, and oldest stale request age; the Site Settings panel renders the localized escalation line during the same live request/approve flow.
- `M172-CA`: Translation release approval reports now assign pending and stale approvals to real builder users with settings permission while excluding the requester; the Site Settings panel renders the localized assignment line, and Playwright creates a separate reviewer user through the guarded users API before verifying the live modal.
- `M172-CB`: Translation release approval decisions now block requester self-review in the store transition and return a 409 API error without notification; Playwright verifies the live settings request stays pending after the blocked self-review path.
- `M172-CC`: Translation release approval settings now receive the current actor from the queue API and disable self-review approve/reject controls with localized guidance when the requester is the active actor; Playwright verifies the live modal affordance and pending status after the blocked self-review path.
- `M172-CD`: Builder authentication now supports optional secondary Basic Auth credentials through `BUILDER_BASIC_AUTH_USERS`, while mutation guards enforce permissions through the authenticated username's role-store record; local HTTP smoke verifies reviewer credentials are denied before role assignment, accepted after admin role grant, and recorded as the mutation actor.
- `M172-CE`: Builder page-entry middleware now accepts `BUILDER_BASIC_AUTH_USERS` for `/admin-builder` while keeping `/admin-consultation` legacy-only; opt-in Playwright smoke verifies a separate reviewer browser can enter Site Settings, approve an admin-requested translation release approval, and have the API report the approval as `approved`.
- `M172-CF`: Middleware unit coverage now locks the auth split from M172-CE: reviewer secondary credentials enter `/admin-builder`, cannot enter `/admin-consultation`, legacy CMS credentials still enter builder, malformed builder credential JSON fails builder paths closed, malformed builder-only config does not break consultation admin entry, and explicit admin matchers keep dotted admin subpaths covered before the public static-asset exclusion.
