# WO-I18N-COL010-EMBEDDINGS — Generated semantic-search synchronization

Date: 2026-07-25 KST
Manager: Codex `/root`
Unit: `010-taiwan-gym-injury-lawsuit`

## Goal

Replace the stale committed and Blob-backed semantic-search records after the
accepted KO, ZH-Hant, and EN article rewrites. The generated store must contain
all 17 columns for each currently supported consultation locale, with no
skipped records, duplicate locale/slug keys, invalid vectors, stale column 010
titles, stale snippets, or former attorney-identity leakage.

Japanese is not silently declared supported by this lane. The current
generator accepts the builder/consultation `Locale` set (`ko`, `zh-hant`,
`en`). Whether the public Japanese site exposes consultation retrieval is a
separate architecture audit and must be resolved before widening the generated
schema.

## Generation evidence

Use only the canonical development endpoint:

```text
POST /api/consultation/build-embeddings
```

The accepted generation result must be:

- HTTP 200 and `success: true`;
- `total: 51`;
- `written: 51`;
- `skipped: 0`;
- 17 records each for `ko`, `zh-hant`, and `en`;
- model `text-embedding-3-small`;
- schema field `dim: 1536`;
- a 1536-element finite numeric vector for every record;
- exact local/remote Blob SHA-256 equality after a no-cache private Blob read.

A partial response must never be committed. The first 2026-07-25 attempt
returned `written: 47`, `skipped: 4` and was rejected. The accepted retry
returned all 51 records with no skips and replaced both local and Blob copies.

## Owned files

- `src/content/column-embeddings.json` (generated by the canonical endpoint,
  never manually edited)
- `src/lib/__tests__/column-embeddings-content-sync.test.ts` (new)

Do not edit the generator, consultation API, locale definitions, article
Markdown, public copy, or any unrelated test in this lane.

## Exact column 010 records

- KO title:
  `대만 헬스장 부상 손해배상: 1심 사례·청구기한·증거·배상항목`
- ZH-Hant title:
  `台灣健身房受傷求償：一審案例、期限、證據與賠償項目`
- EN title:
  `Taiwan Gym Injury Claims: Case Study, Deadlines, Evidence, and Damages`

Each snippet must come from the current localized article and must describe the
gym-injury guide's actual scope. The three records must not contain:

- `대만 헬스장 부상 소송`;
- `台灣健身房受傷訴訟`;
- `Taiwan Gym Injury Lawsuit`;
- `증준외 대만 변호사입니다`;
- `曾俊瑋`;
- the former archive sentence that treats the media-reported appellate
  settlement as an unqualified result.

## Regression-test contract

The focused test must:

1. parse the generated JSON and assert version, model, `dim === 1536`, and a
   valid ISO `builtAt`;
2. assert exactly 51 unique records and 17 records per supported locale;
3. assert each locale/slug set and title matches `getAllColumnPosts(locale)`;
4. assert every vector contains exactly 1536 finite numbers;
5. assert the exact three column 010 titles and current localized snippet
   anchors;
6. reject the stale titles, snippets, and wrong identity listed above.

The test must not call OpenAI, mutate Blob, rewrite the JSON, or depend on a
secret.

## Gates

1. focused embeddings sync test passes;
2. the focused test covers the generated-store schema and vectors; the
   existing `src/app/api/consultation/knowledge/__tests__/route.test.ts` and
   `src/lib/consultation/__tests__/engine.test.ts` regressions pass;
3. TypeScript, scoped ESLint, and `git diff --check` pass;
4. independent implementation review passes;
5. local generated file and private Blob checksum remain identical, verified
   by reading
   `consultation-embeddings/column-embeddings.json` with
   `@vercel/blob get(..., { access: 'private', useCache: false })` and comparing
   SHA-256 hashes; the lane is not accepted when the required Blob token is
   unavailable;
6. production build remains green.

Do not push or deploy.
