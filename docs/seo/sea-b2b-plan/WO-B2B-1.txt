WO-B2B-1 — 안내 4언어(vi/id/th/fil) B2B 인텐트 페이지 2종: `company-setup`(대만 회사설립·투자) · `debt-collection`(대만 기업 상대 계약분쟁·미수금 회수·소송)

작업트리: /Users/son7/Projects/tseng-law-sea-b2b-20260911 (브랜치 seo/sea-b2b-20260911, base origin/main 3f0c5e1f). 이 디렉터리 밖 쓰기 금지. git add/commit/push 금지. 전체 테스트 스위트·Playwright 실행 금지(지정 vitest만).
node: export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH

## 1. 문제
vi/id/th/fil 안내 로케일에는 코어 10페이지(home·services·about·lawyers·pricing·contact·faq·privacy·disclaimer·columns)만 있다. 사무소의 핵심 수임 의도인 ① 대만 회사설립·투자, ⑤ 대만 기업 상대 계약분쟁·미수금·소송에 대해 현지어로 답하는 전용 페이지가 0이다(`/{l}/services`의 한 절이 범위만 안내). EN에는 `/en/taiwan-company-setup-lawyer`, `/en/taiwan-litigation-lawyer`(`src/data/intent-pages.ts` en 블록)가 있다.

## 2. 원인(구조)
- `src/lib/public-guidance.ts`: `GUIDANCE_PAGE_KEYS`(10개 고정) · `GUIDANCE_CORE_ROUTE_KEYS` · `resolveGuidanceMiddlewareRewrite()`가 코어 키 외 슬러그를 `__public-guidance-unavailable`로 보낸다.
- 본문 파일 `src/data/international-guidance-content.ts`는 번역 레인 소유 → 수정 금지. 따라서 본문은 **별도 파일**에 두고 병합 조회해야 한다.
- 참고 구현(구 베이스, 그대로 cherry-pick 불가 — 파일들이 이후 변경됨): `git show 11ce641d` (드래프트 브랜치 seo/sea-s3-drafts-20260909, 같은 레포 객체DB에서 조회 가능). `GUIDANCE_EXTRA_PAGE_KEYS`·`GUIDANCE_ALL_PAGE_KEYS`·`isGuidanceExtraPageKey`·별도 데이터 파일 `international-guidance-extra.ts`·sitemap/llms.txt/[[...slug]] 연동·테스트 패턴을 **참고만** 하고 현 파일 상태에 맞게 재구현하라. 그 커밋의 work-permit-renewal 본문·마커는 가져오지 않는다.

## 3. 수정 사양
3.1 `src/lib/public-guidance.ts`
  a. `GUIDANCE_EXTRA_PAGE_KEYS = ['company-setup', 'debt-collection'] as const` 신설. `GUIDANCE_PAGE_KEYS`(10개)는 불변. `GuidancePageKey = core | extra`, `GUIDANCE_ALL_PAGE_KEYS`, `isGuidanceExtraPageKey()`.
  b. `GUIDANCE_CORE_ROUTE_KEYS`에 두 슬러그 추가(라우트 화이트리스트). `PAGE_KEY_BY_ROUTE` 매핑 추가. `classifyGuidanceSlug`·`guidancePublicPath`·`guidanceCanonicalUrl`이 두 키에 대해 동작.
  c. hreflang: extra 키는 **vi/id/th/fil 4개 상호 참조만**. ko/zh-hant/en/ja URL·x-default를 만들지 않는다(존재하지 않는 URL 금지, 비상호 x-default 금지 — 9/9 Grok 검토 결정). 기존 코어 10키의 8로케일 클러스터·x-default=en 동작은 불변.
  d. 상단 주석에 extra 키 규약(본문 파일·nav 미노출·4로케일 클러스터) 추가.

3.2 `src/data/international-guidance-extra.ts` (신규) — `guidanceExtraContent: Record<GuidanceLocale, Record<GuidanceExtraPageKey, GuidancePage>>` (타입은 international-guidance-content.ts에서 import만). 추가로 `guidanceExtraRelated: Record<GuidanceCorePageKey 일부, GuidanceExtraPageKey[]>`와 4로케일 `relatedLabel`(예: vi "Hướng dẫn liên quan")을 export — 코어 페이지 home·services·contact·pricing에서 새 페이지로 가는 내부 링크용.
  본문 사실 규칙(하드 룰 3·YMYL): **아래 원문에 이미 게재된 사실만** 각 언어로 옮긴다. 새 법률 주장·수치·기간 약속·결과 언급 금지. 원문:
   - company-setup ← `intentPages.en['taiwan-company-setup-lawyer']` (heroPoints·idealFor·reviewPoints·processFlow·prepareChecklist·cautionPoints·faq) + `guidanceContent[l].pages.services`의 투자·설립 절.
   - debt-collection ← `intentPages.en['taiwan-litigation-lawyer']` 중 계약분쟁·미수금·해외기업 관련 항목(idealFor 5·6, reviewPoints 5, processFlow 1~3, prepareChecklist, cautionPoints 3, faq 1·3·4·5·7·8) + `guidanceContent[l].pages.services`의 민사 절. 교통사고·이혼·형사 세부는 넣지 않는다(⑤ 인텐트 집중).
   - **금액 금지**: NT$3,000·NT$50,000 등 수임료 숫자는 4로케일 안내 페이지 어디에도 없으므로 쓰지 않는다. 요금은 "`/{l}/pricing` 페이지 참조" 문장으로만.
   - EN 원문의 "English-speaking attorneys are available" 류는 "상담은 영어·중국어·일본어·한국어로만 진행" 문장으로 옮긴다(그 언어 상담 암시 금지). 각 언어의 **기존 FAQ 거절 문장**(vi: international-guidance-content.ts 410행 "Không. Phần hướng dẫn này được viết bằng tiếng Việt, nhưng việc tư vấn với luật sư chỉ được thực hiện bằng tiếng Anh, tiếng Trung, tiếng Nhật và tiếng Hàn."; id/th/fil은 같은 파일 faq에서 대응 문장 찾아 그대로)을 각 페이지 faqs 마지막 항목으로 **그대로** 재사용.
   - 구조(GuidancePage): eyebrow(해당 언어 "HƯỚNG DẪN"류 기존 표기 재사용), title(질의형 키워드 포함 — 예 vi: "Thành lập công ty tại Đài Loan — hướng dẫn cho doanh nghiệp nước ngoài"), description(140~160자), intro, sections 4~5개(company-setup: 법인 형태 선택 자회사/지사/대표사무소 · 투자심사·자본금 송금·등기 순서 · 등기 이후(은행·세무회계·거류허가·상표·노동) · 첫 이메일에 넣을 것 · 주의점 / debt-collection: 어떤 분쟁을 다루나(계약·미수금·해외 기업) · 대만 밖에서 시작하는 방법(위임장·원격 초기 검토·출석 여부) · 합의·민사소송·집행 비교 절차 · 첫 이메일에 넣을 것 · 주의점), faqs 5~7개.
   - 각 페이지에 EN 랜딩으로 가는 링크 문장 포함(예 vi: "Bản tiếng Anh dành cho tư vấn: /en/taiwan-company-setup-lawyer") — 본문 paragraphs에 경로 문자열로. GuidancePageBody가 링크를 자동 렌더하지 않으면 본문 내 텍스트 경로로 두되, 3.4 related 블록에서 실제 <a>로 렌더.
   - 광고규정: 승소율·성공 보장·최고·최초·유일·1위 표현 0. 대만 변호사 광고규정 요약은 docs/seo/taiwan-lawyer-ad-rules-2026-08-18.md.
   - 현지어 품질: 자연스러운 격식체. th는 문장 사이 공백 규칙 준수. fil은 Filipino(Tagalog 기반) — 영어 차용은 기존 fil 콘텐츠 톤에 맞춤.
   - `[변호사 검수 필요]` 마커: 위 원문 밖 사실을 넣지 않으면 마커가 필요 없다. 원문에 없는 문장을 꼭 써야 한다면 넣지 말고 보고서에 "누락 후보"로만 적는다. 마커가 있는 산출물은 반려된다.

3.3 `src/data/international-guidance-answers.ts` — `guidanceAnswers[l]['company-setup']`·`['debt-collection']` 답변 블록(40~80단어, th 120~400자; 기존 규약 주석 준수: 페이지 사실 재진술 + 상담 언어 문장, sources 2개: `/{l}/pricing`, `/{l}/contact`). 타입이 `Partial<Record<GuidancePageKey,…>>`이므로 GuidancePageKey가 확장되면 그대로 수용되는지 확인. 기존 항목 불변. 추가로 코어 `services` 항목의 `sources`에 `/{l}/company-setup`·`/{l}/debt-collection`을 **추가**(기존 항목은 유지, 최대 4개까지 허용하도록 주석 갱신).

3.4 렌더 `src/app/[locale]/[[...slug]]/page.tsx` + `src/components/GuidancePageBody.tsx`
  - 페이지 조회를 `guidanceContent[l].pages[key] ?? guidanceExtraContent[l][key]` 형태의 헬퍼(`getGuidancePage(locale,key)` — public-guidance.ts 또는 extra 파일에 두되 international-guidance-content.ts는 import만)로 교체. generateMetadata·JSON-LD(LegalService availableLanguage 4개 고정 유지, FAQPage는 faqs 그대로)·답변 블록 모두 extra 키에서 동작.
  - `guidanceExtraRelated`에 해당 코어 페이지(home·services·contact·pricing)일 때 본문 하단에 "관련 안내" 리스트(<nav aria-label> + <a href>)를 렌더. extra 페이지 자체에는 코어 `/services`·`/pricing`·`/contact`와 EN 랜딩 링크 리스트를 렌더. 스타일은 기존 module.css 클래스 재사용(신규 CSS 최소).
  - nav(헤더 메뉴)는 변경하지 않는다(코어 10 고정).

3.5 `src/app/sitemap.ts` — 안내 로케일 엔트리 생성이 `GUIDANCE_PAGE_KEYS`를 돌면 `GUIDANCE_ALL_PAGE_KEYS`로 바꾸되, extra 키의 alternates는 3.1c 규약(4로케일만). priority 0.8.
3.6 `src/lib/llms-txt.ts` — 로케일별 `/{l}/llms.txt`에 extra 2페이지 항목 추가(길이 검사 `GUIDANCE_PAGE_KEYS.length` 하드코딩을 ALL 기준으로). 루트 llms.txt 카탈로그는 불변.
3.7 `src/middleware.ts` — `resolveGuidanceMiddlewareRewrite`가 코어 라우트 키 목록을 쓰므로 3.1b로 자동 통과되는지 확인. 미들웨어 파일 자체는 수정하지 않는 것이 목표(필요하면 보고).

3.8 테스트(vitest) — 기존 갱신 + 신규
  - `src/lib/__tests__/public-guidance.test.ts`: 두 슬러그 classify·canonical·alternates(4개, x-default 없음, en 없음)·코어 10키 클러스터 불변.
  - `src/app/__tests__/sitemap.test.ts`: 8개 신규 URL 존재·alternates 4개.
  - `src/app/[locale]/__tests__/llms-discovery.test.tsx`: 로케일 llms.txt에 두 경로 포함.
  - `src/lib/__tests__/international-guidance-answers.test.ts`(있으면 갱신): 신규 답변 블록 길이·sources 실존.
  - 신규 `src/data/__tests__/international-guidance-extra.test.ts`: 4로케일×2키 (a) faqs ≥5 (b) 각 언어 거절 문장 정확 포함 (c) 금지 토큰 0 — 승소율/勝訴率/win rate/guarantee/保證/최고/유일 + 현지어: vi "tỷ lệ thắng|bảo đảm thắng|tốt nhất|duy nhất", id "tingkat kemenangan|jaminan menang|terbaik|satu-satunya", th "อัตราการชนะ|รับประกัน|ดีที่สุด|แห่งเดียว", fil "win rate|garantiya|pinakamahusay|nag-iisa" (d) 숫자 금지: `NT\$|\d{1,3}(,\d{3})+` 0건 (e) `[변호사 검수 필요]` 0 (f) 현지어 상담 암시 문구 0: vi "tư vấn bằng tiếng Việt" 는 거절 문장 안에서만 허용(정확 문장 제외 후 0), id "konsultasi dalam bahasa Indonesia" 동일, th "ปรึกษาเป็นภาษาไทย" 동일, fil "konsultasyon sa Filipino|sa Tagalog" 동일 (g) 본문에 EN 랜딩 경로 문자열 포함.

## 4. 수정 허용 파일 (그 외 금지)
src/lib/public-guidance.ts · src/data/international-guidance-extra.ts(신규) · src/data/international-guidance-answers.ts · src/app/[locale]/[[...slug]]/page.tsx · src/components/GuidancePageBody.tsx · src/components/InternationalGuidance.module.css(최소) · src/app/sitemap.ts · src/lib/llms-txt.ts · 위 3.8 테스트 파일들.
금지: src/data/international-guidance-content.ts · src/data/intent-pages.ts · src/content/** · src/middleware.ts(확인만) · 헤더/네비 · docs/**(보고서는 stdout으로).

## 5. 검증 명령 (전부 실행하고 출력 그대로 보고)
cd /Users/son7/Projects/tseng-law-sea-b2b-20260911 && export PATH=/Users/son7/.nvm/versions/node/v24.14.1/bin:$PATH
git status --short && git diff --stat
npm run typecheck 2>&1 | tail -3
npx vitest run src/lib/__tests__/public-guidance.test.ts src/app/__tests__/sitemap.test.ts "src/app/[locale]/__tests__/llms-discovery.test.tsx" src/data/__tests__/international-guidance-extra.test.ts src/lib/__tests__/international-guidance-answers.test.ts src/lib/__tests__/seo-faq-jsonld.test.ts 2>&1 | tail -8
npx eslint --max-warnings=0 --ext .ts,.tsx src/lib/public-guidance.ts src/data/international-guidance-extra.ts src/data/international-guidance-answers.ts "src/app/[locale]/[[...slug]]/page.tsx" src/components/GuidancePageBody.tsx src/app/sitemap.ts src/lib/llms-txt.ts 2>&1 | tail -5
grep -rn "\[변호사 검수 필요\]" src/data/international-guidance-extra.ts src/data/international-guidance-answers.ts | wc -l   # 0
grep -rnE "NT\\$|[0-9]{1,3},[0-9]{3}" src/data/international-guidance-extra.ts | wc -l   # 0
(빌드·next start 렌더는 총괄이 수행 — 워커는 실행하지 않는다.)

## 6. 보고 형식
① 변경 파일 목록(git diff --stat 그대로) ② 명령 출력 그대로 ③ 원문에 없어 넣지 않은 "누락 후보" 문장 목록 ④ 언어별 자기점검(거절 문장 원문 인용 위치, 금지 토큰 0 확인) ⑤ 미해결·의문점. 추측으로 "완료" 표기 금지.
