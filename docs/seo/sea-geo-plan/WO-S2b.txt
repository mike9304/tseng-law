# WO-S2b — GEO 구조: llms.txt 4로케일 + FAQPage JSON-LD + @id 참조 (Opus 5)

## 공통 규칙 (위반 시 반려)
- 작업트리: ~/Projects/tseng-law-sea-seo-20260909 에서만 읽고 쓴다. 다른 워크트리·main 작업트리 접근 금지. 인박스·타 세션 확인 불필요(단일 작성자 보장).
- 실행 금지: 전체 테스트 스위트, playwright, npm run build, 커밋, push. 지정된 vitest 파일 1~2개만 실행.
- 변경 금지 파일: src/data/international-guidance-content.ts, src/data/intent-pages.ts 의 기존 본문·기존 키, src/content/columns*/**, 팀·수임료·연락처·사례 데이터. (신규 키 추가만 WO가 허용한 경우 가능)
- 언어 계약: vi/id/th/fil 은 안내 언어. 해당 언어로 상담·통역 가능을 암시하는 문구·메타·JSON-LD availableLanguage 금지. 상담 언어 표기는 "English / Chinese / Japanese / Korean" 만.
- 광고 규정: 승소율·성공 보장·최고/유일 표현 금지.
- 사실: 사이트에 이미 게재된 사실만 사용. 새 법률 주장은 문장 앞에 [변호사 검수 필요] 마커. 숫자·통계는 출처 URL+조회일+등급(A 공식통계/B 정부·협회/C 업계리서치/D 언론/E 추정) 없으면 "미확인".
- 보고 형식(마지막에 반드시): (1) 변경 파일 목록 (2) 실행한 명령과 마지막 출력 3줄 (3) 완료 기준 항목별 PASS/FAIL (4) 미해결·못 한 것 — 못 한 것은 못 했다고 쓴다. 완료 기준을 느슨하게 바꾸지 않는다.

## 목표
① src/lib/llms-txt.ts: vi/id/th/fil 섹션 추가 — 각 로케일 안내 페이지 10키 URL(guidancePublicPath)과 1줄 설명(해당 언어, 본문 title 재사용). 기존 4로케일 구조·주의문(publicNotice 등)을 같은 형식으로 4언어 추가. "검색 순위·추천 보장 없음" 주의문 유지.
② src/components/InternationalGuidance.tsx: page.faqs 가 있으면 <script type="application/ld+json"> 로 FAQPage(mainEntity Question/Answer) 출력. src/lib/seo.ts 의 기존 FAQ JSON-LD 빌더가 있으면 재사용(seo-faq-jsonld.test.ts 참고), 없으면 buildGuidanceFaqJsonLd 추가.
③ 같은 컴포넌트에서 LegalService JSON-LD 출력하되 provider/@id 는 src/lib/seo.ts 기존 상수(ATTORNEY_PERSON_ID 등) 참조만(새 @id 생성 금지). availableLanguage 는 ["en","zh-Hant","ja","ko"] 고정 — vi/id/th/fil 절대 포함 금지. inLanguage 는 페이지 로케일. JSON 직렬화 시 </script> 이스케이프.
## 허용 파일
src/lib/llms-txt.ts, src/lib/seo.ts(추가만), src/components/InternationalGuidance.tsx, src/app/[locale]/__tests__/llms-discovery.test.tsx(케이스 추가), src/lib/__tests__/seo-faq-jsonld.test.ts(케이스 추가), 신규 src/lib/__tests__/guidance-jsonld.test.ts
## 테스트(지정 파일만 실행)
- llms.txt 출력에 /vi/ /id/ /th/ /fil/ 각 10 URL, 주의문 4언어
- FAQPage: faqs 있는 로케일·키에서 mainEntity 길이 = faqs 길이, JSON.parse 성공, 텍스트가 faqs 와 글자 그대로 동일
- LegalService availableLanguage 정확히 4개·vi/id/th/fil 불포함, provider @id === ATTORNEY_PERSON_ID
## 완료 기준
1. 지정 vitest 통과(출력 첨부) 2. 허용 파일 밖 변경 0 3. 보고에 /th/faq 의 FAQPage JSON 예시 첨부
